window.BACKEND={"tree": [{"p": "README.md", "g": "README.md"}, {"p": "nodejs-backend/README.md", "g": "nodejs-backend"}, {"p": "nodejs-backend/package.json", "g": "nodejs-backend"}, {"p": "nodejs-backend/01-server-side-architecture/01-client-server-architecture.js", "g": "nodejs-backend/01-server-side-architecture"}, {"p": "nodejs-backend/01-server-side-architecture/02-basic-http-server.js", "g": "nodejs-backend/01-server-side-architecture"}, {"p": "nodejs-backend/01-server-side-architecture/03-request-response-cycle.js", "g": "nodejs-backend/01-server-side-architecture"}, {"p": "nodejs-backend/02-nodejs-fundamentals/01-builtin-modules.js", "g": "nodejs-backend/02-nodejs-fundamentals"}, {"p": "nodejs-backend/02-nodejs-fundamentals/02-fs-module.js", "g": "nodejs-backend/02-nodejs-fundamentals"}, {"p": "nodejs-backend/02-nodejs-fundamentals/03-path-module.js", "g": "nodejs-backend/02-nodejs-fundamentals"}, {"p": "nodejs-backend/02-nodejs-fundamentals/04-custom-modules.js", "g": "nodejs-backend/02-nodejs-fundamentals"}, {"p": "nodejs-backend/02-nodejs-fundamentals/04a-mathUtils.js", "g": "nodejs-backend/02-nodejs-fundamentals"}, {"p": "nodejs-backend/02-nodejs-fundamentals/05-npm-packages.js", "g": "nodejs-backend/02-nodejs-fundamentals"}, {"p": "nodejs-backend/02-nodejs-fundamentals/06-callbacks-promises-async.js", "g": "nodejs-backend/02-nodejs-fundamentals"}, {"p": "nodejs-backend/02-nodejs-fundamentals/07-error-handling.js", "g": "nodejs-backend/02-nodejs-fundamentals"}, {"p": "nodejs-backend/03-express-framework/01-basic-express-app.js", "g": "nodejs-backend/03-express-framework"}, {"p": "nodejs-backend/03-express-framework/02-get-post-requests.js", "g": "nodejs-backend/03-express-framework"}, {"p": "nodejs-backend/03-express-framework/03-static-files.js", "g": "nodejs-backend/03-express-framework"}, {"p": "nodejs-backend/03-express-framework/04-html-json-responses.js", "g": "nodejs-backend/03-express-framework"}, {"p": "nodejs-backend/03-express-framework/05-modular-project-structure/app.js", "g": "nodejs-backend/03-express-framework"}, {"p": "nodejs-backend/03-express-framework/05-modular-project-structure/controllers/itemController.js", "g": "nodejs-backend/03-express-framework"}, {"p": "nodejs-backend/03-express-framework/05-modular-project-structure/routes/items.js", "g": "nodejs-backend/03-express-framework"}, {"p": "nodejs-backend/03-express-framework/public/app.js", "g": "nodejs-backend/03-express-framework"}, {"p": "nodejs-backend/03-express-framework/public/index.html", "g": "nodejs-backend/03-express-framework"}, {"p": "nodejs-backend/03-express-framework/public/style.css", "g": "nodejs-backend/03-express-framework"}, {"p": "nodejs-backend/04-routing/01-basic-routing.js", "g": "nodejs-backend/04-routing"}, {"p": "nodejs-backend/04-routing/02-route-and-query-parameters.js", "g": "nodejs-backend/04-routing"}, {"p": "nodejs-backend/04-routing/03-express-router.js", "g": "nodejs-backend/04-routing"}, {"p": "nodejs-backend/04-routing/04-not-found-404-handling.js", "g": "nodejs-backend/04-routing"}, {"p": "nodejs-backend/04-routing/05-modular-routes/app.js", "g": "nodejs-backend/04-routing"}, {"p": "nodejs-backend/04-routing/05-modular-routes/routes/products.js", "g": "nodejs-backend/04-routing"}, {"p": "nodejs-backend/04-routing/05-modular-routes/routes/students.js", "g": "nodejs-backend/04-routing"}, {"p": "nodejs-backend/04-routing/05-modular-routes/routes/users.js", "g": "nodejs-backend/04-routing"}, {"p": "nodejs-backend/05-middleware/01-custom-middleware.js", "g": "nodejs-backend/05-middleware"}, {"p": "nodejs-backend/05-middleware/02-logging-middleware.js", "g": "nodejs-backend/05-middleware"}, {"p": "nodejs-backend/05-middleware/03-request-validation-middleware.js", "g": "nodejs-backend/05-middleware"}, {"p": "nodejs-backend/05-middleware/04-authentication-middleware.js", "g": "nodejs-backend/05-middleware"}, {"p": "nodejs-backend/05-middleware/05-error-handling-middleware.js", "g": "nodejs-backend/05-middleware"}, {"p": "nodejs-backend/05-middleware/06-middleware-execution-order.js", "g": "nodejs-backend/05-middleware"}, {"p": "nodejs-backend/06-session-and-cookie-management/01-create-cookies.js", "g": "nodejs-backend/06-session-and-cookie-management"}, {"p": "nodejs-backend/06-session-and-cookie-management/02-read-and-delete-cookies.js", "g": "nodejs-backend/06-session-and-cookie-management"}, {"p": "nodejs-backend/06-session-and-cookie-management/03-session-management.js", "g": "nodejs-backend/06-session-and-cookie-management"}, {"p": "nodejs-backend/06-session-and-cookie-management/04-login-and-logout-with-sessions.js", "g": "nodejs-backend/06-session-and-cookie-management"}, {"p": "nodejs-backend/06-session-and-cookie-management/05-protected-routes-session-auth.js", "g": "nodejs-backend/06-session-and-cookie-management"}, {"p": "nodejs-backend/07-database-connectivity/01-connect-database.js", "g": "nodejs-backend/07-database-connectivity"}, {"p": "nodejs-backend/07-database-connectivity/02-create-tables.js", "g": "nodejs-backend/07-database-connectivity"}, {"p": "nodejs-backend/07-database-connectivity/03-insert-data.js", "g": "nodejs-backend/07-database-connectivity"}, {"p": "nodejs-backend/07-database-connectivity/04-retrieve-data.js", "g": "nodejs-backend/07-database-connectivity"}, {"p": "nodejs-backend/07-database-connectivity/05-update-data.js", "g": "nodejs-backend/07-database-connectivity"}, {"p": "nodejs-backend/07-database-connectivity/06-delete-data.js", "g": "nodejs-backend/07-database-connectivity"}, {"p": "nodejs-backend/08-crud-operations/01-crud-express-nodejs.js", "g": "nodejs-backend/08-crud-operations"}, {"p": "nodejs-backend/08-crud-operations/02-student-management-crud.js", "g": "nodejs-backend/08-crud-operations"}, {"p": "nodejs-backend/08-crud-operations/03-user-management-crud.js", "g": "nodejs-backend/08-crud-operations"}, {"p": "nodejs-backend/08-crud-operations/04-product-management-crud.js", "g": "nodejs-backend/08-crud-operations"}, {"p": "nodejs-backend/08-crud-operations/05-crud-with-database-connectivity.js", "g": "nodejs-backend/08-crud-operations"}, {"p": "nodejs-backend/08-crud-operations/06-input-validation-crud.js", "g": "nodejs-backend/08-crud-operations"}, {"p": "nodejs-backend/09-authentication/01-user-registration.js", "g": "nodejs-backend/09-authentication"}, {"p": "nodejs-backend/09-authentication/02-login-and-logout.js", "g": "nodejs-backend/09-authentication"}, {"p": "nodejs-backend/09-authentication/03-password-hashing.js", "g": "nodejs-backend/09-authentication"}, {"p": "nodejs-backend/09-authentication/04-password-verification.js", "g": "nodejs-backend/09-authentication"}, {"p": "nodejs-backend/09-authentication/05-jwt-authentication.js", "g": "nodejs-backend/09-authentication"}, {"p": "nodejs-backend/09-authentication/06-protected-api-routes.js", "g": "nodejs-backend/09-authentication"}, {"p": "nodejs-backend/10-authorization/01-role-based-authorization.js", "g": "nodejs-backend/10-authorization"}, {"p": "nodejs-backend/10-authorization/02-admin-and-user-roles.js", "g": "nodejs-backend/10-authorization"}, {"p": "nodejs-backend/10-authorization/03-restrict-routes-by-role.js", "g": "nodejs-backend/10-authorization"}, {"p": "nodejs-backend/10-authorization/04-authorization-middleware.js", "g": "nodejs-backend/10-authorization"}, {"p": "nodejs-backend/11-restful-api-development/01-basic-restful-api.js", "g": "nodejs-backend/11-restful-api-development"}, {"p": "nodejs-backend/11-restful-api-development/02-get-rest-api.js", "g": "nodejs-backend/11-restful-api-development"}, {"p": "nodejs-backend/11-restful-api-development/03-post-rest-api.js", "g": "nodejs-backend/11-restful-api-development"}, {"p": "nodejs-backend/11-restful-api-development/04-put-patch-rest-api.js", "g": "nodejs-backend/11-restful-api-development"}, {"p": "nodejs-backend/11-restful-api-development/05-delete-rest-api.js", "g": "nodejs-backend/11-restful-api-development"}, {"p": "nodejs-backend/11-restful-api-development/06-http-status-codes.js", "g": "nodejs-backend/11-restful-api-development"}, {"p": "nodejs-backend/11-restful-api-development/07-api-error-handling.js", "g": "nodejs-backend/11-restful-api-development"}, {"p": "nodejs-backend/11-restful-api-development/08-api-request-validation.js", "g": "nodejs-backend/11-restful-api-development"}, {"p": "nodejs-backend/11-restful-api-development/09-postman-collection.json", "g": "nodejs-backend/11-restful-api-development"}, {"p": "nodejs-backend/11-restful-api-development/09-test-rest-api-postman.js", "g": "nodejs-backend/11-restful-api-development"}, {"p": "nodejs-backend/11-restful-api-development/10-student-rest-api-with-database.js", "g": "nodejs-backend/11-restful-api-development"}, {"p": "nodejs-backend/11-restful-api-development/11-product-rest-api-crud.js", "g": "nodejs-backend/11-restful-api-development"}, {"p": "nodejs-backend/11-restful-api-development/12-user-rest-api-auth-and-authorization.js", "g": "nodejs-backend/11-restful-api-development"}, {"p": "nodejs-backend/common/crudFactory.js", "g": "nodejs-backend/common"}, {"p": "nodejs-backend/common/db.js", "g": "nodejs-backend/common"}, {"p": "nodejs-backend/common/validators.js", "g": "nodejs-backend/common"}, {"p": "supabase/README.md", "g": "supabase"}, {"p": "supabase/package.json", "g": "supabase"}, {"p": "supabase/00-setup/schema.sql", "g": "supabase/00-setup"}, {"p": "supabase/01-supabase-fundamentals/01-create-supabase-project.md", "g": "supabase/01-supabase-fundamentals"}, {"p": "supabase/01-supabase-fundamentals/02-explore-supabase-dashboard.md", "g": "supabase/01-supabase-fundamentals"}, {"p": "supabase/01-supabase-fundamentals/03-configure-project-settings-and-api-keys.md", "g": "supabase/01-supabase-fundamentals"}, {"p": "supabase/01-supabase-fundamentals/04-create-and-manage-postgresql-database.sql", "g": "supabase/01-supabase-fundamentals"}, {"p": "supabase/01-supabase-fundamentals/05-demonstrate-table-editor.md", "g": "supabase/01-supabase-fundamentals"}, {"p": "supabase/01-supabase-fundamentals/06-demonstrate-sql-editor.sql", "g": "supabase/01-supabase-fundamentals"}, {"p": "supabase/01-supabase-fundamentals/07-connect-application-to-supabase.js", "g": "supabase/01-supabase-fundamentals"}, {"p": "supabase/02-database-and-tables/01-create-tables.sql", "g": "supabase/02-database-and-tables"}, {"p": "supabase/02-database-and-tables/02-columns-and-postgresql-data-types.sql", "g": "supabase/02-database-and-tables"}, {"p": "supabase/02-database-and-tables/03-primary-and-foreign-keys.sql", "g": "supabase/02-database-and-tables"}, {"p": "supabase/02-database-and-tables/04-constraints-not-null-unique-default-check.sql", "g": "supabase/02-database-and-tables"}, {"p": "supabase/02-database-and-tables/05-insert-records.sql", "g": "supabase/02-database-and-tables"}, {"p": "supabase/02-database-and-tables/06-retrieve-records.sql", "g": "supabase/02-database-and-tables"}, {"p": "supabase/02-database-and-tables/07-update-records.sql", "g": "supabase/02-database-and-tables"}, {"p": "supabase/02-database-and-tables/08-delete-records.sql", "g": "supabase/02-database-and-tables"}, {"p": "supabase/03-sql-operations/00-sample-data.sql", "g": "supabase/03-sql-operations"}, {"p": "supabase/03-sql-operations/01-basic-select-queries.sql", "g": "supabase/03-sql-operations"}, {"p": "supabase/03-sql-operations/02-filtering-with-where.sql", "g": "supabase/03-sql-operations"}, {"p": "supabase/03-sql-operations/03-sorting-with-order-by.sql", "g": "supabase/03-sql-operations"}, {"p": "supabase/03-sql-operations/04-group-by-and-having.sql", "g": "supabase/03-sql-operations"}, {"p": "supabase/03-sql-operations/05-aggregate-functions.sql", "g": "supabase/03-sql-operations"}, {"p": "supabase/03-sql-operations/06-sql-joins.sql", "g": "supabase/03-sql-operations"}, {"p": "supabase/03-sql-operations/07-subqueries.sql", "g": "supabase/03-sql-operations"}, {"p": "supabase/03-sql-operations/08-database-views.sql", "g": "supabase/03-sql-operations"}, {"p": "supabase/04-supabase-crud/01-create-operation.js", "g": "supabase/04-supabase-crud"}, {"p": "supabase/04-supabase-crud/02-read-operation.js", "g": "supabase/04-supabase-crud"}, {"p": "supabase/04-supabase-crud/03-update-operation.js", "g": "supabase/04-supabase-crud"}, {"p": "supabase/04-supabase-crud/04-delete-operation.js", "g": "supabase/04-supabase-crud"}, {"p": "supabase/04-supabase-crud/05-student-management-crud.js", "g": "supabase/04-supabase-crud"}, {"p": "supabase/04-supabase-crud/06-product-management-crud.js", "g": "supabase/04-supabase-crud"}, {"p": "supabase/04-supabase-crud/07-user-management-crud.js", "g": "supabase/04-supabase-crud"}, {"p": "supabase/05-authentication/01-configure-supabase-authentication.md", "g": "supabase/05-authentication"}, {"p": "supabase/05-authentication/02-user-registration.js", "g": "supabase/05-authentication"}, {"p": "supabase/05-authentication/03-user-login.js", "g": "supabase/05-authentication"}, {"p": "supabase/05-authentication/04-user-logout.js", "g": "supabase/05-authentication"}, {"p": "supabase/05-authentication/05-get-current-user.js", "g": "supabase/05-authentication"}, {"p": "supabase/05-authentication/06-email-password-authentication.js", "g": "supabase/05-authentication"}, {"p": "supabase/05-authentication/07-password-reset.js", "g": "supabase/05-authentication"}, {"p": "supabase/05-authentication/08-manage-authentication-sessions.js", "g": "supabase/05-authentication"}, {"p": "supabase/05-authentication/09-protected-routes.js", "g": "supabase/05-authentication"}, {"p": "supabase/06-authorization-and-security/01-enable-row-level-security.sql", "g": "supabase/06-authorization-and-security"}, {"p": "supabase/06-authorization-and-security/02-create-rls-policies.sql", "g": "supabase/06-authorization-and-security"}, {"p": "supabase/06-authorization-and-security/03-restrict-users-to-own-records.sql", "g": "supabase/06-authorization-and-security"}, {"p": "supabase/06-authorization-and-security/04-create-admin-and-user-roles.sql", "g": "supabase/06-authorization-and-security"}, {"p": "supabase/06-authorization-and-security/05-role-based-access-control.sql", "g": "supabase/06-authorization-and-security"}, {"p": "supabase/06-authorization-and-security/06-authenticated-user-policies.sql", "g": "supabase/06-authorization-and-security"}, {"p": "supabase/06-authorization-and-security/07-test-database-access-with-different-users.js", "g": "supabase/06-authorization-and-security"}, {"p": "supabase/07-storage/00-storage-setup-and-policies.sql", "g": "supabase/07-storage"}, {"p": "supabase/07-storage/01-create-storage-bucket.js", "g": "supabase/07-storage"}, {"p": "supabase/07-storage/02-upload-files.js", "g": "supabase/07-storage"}, {"p": "supabase/07-storage/03-retrieve-files.js", "g": "supabase/07-storage"}, {"p": "supabase/07-storage/04-download-files.js", "g": "supabase/07-storage"}, {"p": "supabase/07-storage/05-delete-files.js", "g": "supabase/07-storage"}, {"p": "supabase/07-storage/06-generate-public-file-urls.js", "g": "supabase/07-storage"}, {"p": "supabase/07-storage/07-image-upload-with-authentication.html", "g": "supabase/07-storage"}, {"p": "supabase/08-realtime/01-enable-supabase-realtime.sql", "g": "supabase/08-realtime"}, {"p": "supabase/08-realtime/02-listen-for-insert-events.js", "g": "supabase/08-realtime"}, {"p": "supabase/08-realtime/03-listen-for-update-events.js", "g": "supabase/08-realtime"}, {"p": "supabase/08-realtime/04-listen-for-delete-events.js", "g": "supabase/08-realtime"}, {"p": "supabase/08-realtime/05-realtime-chat-application.html", "g": "supabase/08-realtime"}, {"p": "supabase/08-realtime/06-realtime-student-record-application.html", "g": "supabase/08-realtime"}, {"p": "supabase/09-edge-functions/00-edge-functions-setup.md", "g": "supabase/09-edge-functions"}, {"p": "supabase/09-edge-functions/01-create-edge-function/hello-world/index.ts", "g": "supabase/09-edge-functions"}, {"p": "supabase/09-edge-functions/02-deploy-edge-function/deploy-guide.md", "g": "supabase/09-edge-functions"}, {"p": "supabase/09-edge-functions/03-pass-data-to-edge-function/greet/index.ts", "g": "supabase/09-edge-functions"}, {"p": "supabase/09-edge-functions/04-return-json-responses/json-api/index.ts", "g": "supabase/09-edge-functions"}, {"p": "supabase/09-edge-functions/05-authentication-inside-edge-function/whoami/index.ts", "g": "supabase/09-edge-functions"}, {"p": "supabase/09-edge-functions/06-connect-edge-function-with-database/students-api/index.ts", "g": "supabase/09-edge-functions"}, {"p": "supabase/10-rest-api/01-access-tables-through-generated-rest-api.js", "g": "supabase/10-rest-api"}, {"p": "supabase/10-rest-api/02-get-operations.js", "g": "supabase/10-rest-api"}, {"p": "supabase/10-rest-api/03-post-operations.js", "g": "supabase/10-rest-api"}, {"p": "supabase/10-rest-api/04-patch-update-operations.js", "g": "supabase/10-rest-api"}, {"p": "supabase/10-rest-api/05-delete-operations.js", "g": "supabase/10-rest-api"}, {"p": "supabase/10-rest-api/06-api-filtering-and-pagination.js", "g": "supabase/10-rest-api"}, {"p": "supabase/10-rest-api/07-supabase-rest.postman_collection.json", "g": "supabase/10-rest-api"}, {"p": "supabase/10-rest-api/07-test-rest-api-with-postman.md", "g": "supabase/10-rest-api"}, {"p": "supabase/11-project-based-experiments/01-student-management-system.html", "g": "supabase/11-project-based-experiments"}, {"p": "supabase/11-project-based-experiments/02-user-authentication-system.html", "g": "supabase/11-project-based-experiments"}, {"p": "supabase/11-project-based-experiments/03-product-management-system.html", "g": "supabase/11-project-based-experiments"}, {"p": "supabase/11-project-based-experiments/04-realtime-chat-application.html", "g": "supabase/11-project-based-experiments"}, {"p": "supabase/11-project-based-experiments/05-file-upload-application.html", "g": "supabase/11-project-based-experiments"}, {"p": "supabase/11-project-based-experiments/06-student-rest-api.js", "g": "supabase/11-project-based-experiments"}, {"p": "supabase/11-project-based-experiments/07-role-based-admin-dashboard-rls.sql", "g": "supabase/11-project-based-experiments"}, {"p": "supabase/11-project-based-experiments/07-role-based-admin-dashboard.html", "g": "supabase/11-project-based-experiments"}, {"p": "supabase/config/crudApp.js", "g": "supabase/config"}, {"p": "supabase/config/rest.js", "g": "supabase/config"}, {"p": "supabase/config/supabaseClient.js", "g": "supabase/config"}], "src": {"README.md": "# Backend Lab Manual\n- `nodejs-backend/`  - 65 experiments (Node.js + Express), topic-wise folders\n- `supabase/`        - 79 experiments (Supabase), topic-wise folders (start with `00-setup/`)\nRead the README inside each folder for setup.\n", "nodejs-backend/README.md": "# Node.js Backend Lab\n\nSetup: `npm install` (Node 18+). Every experiment is one file; run with `node <folder>/<file>.js`\n(all servers use http://localhost:3000, so run one at a time). Each file's header comment lists the aim and test commands.\n\n| Folder | Experiments |\n|---|---|\n| 01-server-side-architecture | client-server, HTTP server, request-response cycle |\n| 02-nodejs-fundamentals | built-in modules, fs, path, custom modules, npm, async patterns, error handling |\n| 03-express-framework | basic app, GET/POST, static files, HTML/JSON, modular structure |\n| 04-routing | routing, route/query params, Router, 404, modular routes |\n| 05-middleware | custom, logging, validation, authentication, error handling, execution order |\n| 06-session-and-cookie-management | cookies, read/delete, sessions, login/logout, protected routes |\n| 07-database-connectivity | connect, create tables, insert, retrieve, update, delete (SQLite) |\n| 08-crud-operations | CRUD, Student/User/Product apps, DB CRUD, validation |\n| 09-authentication | register, login/logout, hashing, verification, JWT, protected API |\n| 10-authorization | RBAC, admin/user roles, restricted routes, authorization middleware |\n| 11-restful-api-development | GET/POST/PUT/PATCH/DELETE, status codes, errors, validation, Postman, Student/Product/User APIs |\n\n`common/` holds the shared SQLite connection, CRUD router factory and validators.\nFiles ending `04a-`/`09-postman-collection.json` are helpers for the experiment with the same number.\n", "nodejs-backend/package.json": "{\n  \"name\": \"nodejs-backend-lab\",\n  \"version\": \"1.0.0\",\n  \"private\": true,\n  \"description\": \"Node.js Backend experiments (topic-wise)\",\n  \"dependencies\": {\n    \"bcryptjs\": \"^2.4.3\",\n    \"better-sqlite3\": \"^11.3.0\",\n    \"cookie-parser\": \"^1.4.6\",\n    \"dayjs\": \"^1.11.13\",\n    \"express\": \"^4.19.2\",\n    \"express-session\": \"^1.18.0\",\n    \"jsonwebtoken\": \"^9.0.2\",\n    \"lodash\": \"^4.17.21\"\n  }\n}\n", "nodejs-backend/01-server-side-architecture/01-client-server-architecture.js": "/**\n * Experiment: Implement a basic Client-Server Architecture using Node.js.\n * Run: node 01-client-server-architecture.js\n * The SERVER listens on a port; the CLIENT sends a request and prints the response.\n */\nconst http = require('http');\nconst PORT = 3000;\n\n// ---------- SERVER ----------\nconst server = http.createServer((req, res) => {\n  console.log(`[SERVER] ${req.method} ${req.url}`);\n  res.writeHead(200, { 'Content-Type': 'application/json' });\n  res.end(JSON.stringify({ message: 'Hello from the server', time: new Date().toISOString() }));\n});\n\nserver.listen(PORT, () => {\n  console.log(`[SERVER] listening on http://localhost:${PORT}`);\n\n  // ---------- CLIENT ----------\n  http.get(`http://localhost:${PORT}/greeting`, (res) => {\n    let body = '';\n    res.on('data', (chunk) => (body += chunk));\n    res.on('end', () => {\n      console.log('[CLIENT] status :', res.statusCode);\n      console.log('[CLIENT] body   :', body);\n      server.close(() => console.log('[SERVER] closed'));\n    });\n  });\n});\n", "nodejs-backend/01-server-side-architecture/02-basic-http-server.js": "/**\n * Experiment: Create a basic HTTP Server using Node.js.\n * Run: node 02-basic-http-server.js   ->  open http://localhost:3000 , /about , /api\n */\nconst http = require('http');\n\nconst server = http.createServer((req, res) => {\n  if (req.url === '/') {\n    res.writeHead(200, { 'Content-Type': 'text/html' });\n    res.end('<h1>Welcome to my first Node.js server</h1>');\n  } else if (req.url === '/about') {\n    res.writeHead(200, { 'Content-Type': 'text/plain' });\n    res.end('About page');\n  } else if (req.url === '/api') {\n    res.writeHead(200, { 'Content-Type': 'application/json' });\n    res.end(JSON.stringify({ status: 'ok' }));\n  } else {\n    res.writeHead(404, { 'Content-Type': 'text/plain' });\n    res.end('404 Not Found');\n  }\n});\n\nserver.listen(3000, () => console.log('Server running at http://localhost:3000'));\n", "nodejs-backend/01-server-side-architecture/03-request-response-cycle.js": "/**\n * Experiment: Demonstrate the Request-Response Cycle in a server-side application.\n * Run: node 03-request-response-cycle.js\n * Test: curl -X POST \"http://localhost:3000/echo?x=1\" -H \"Content-Type: application/json\" -d '{\"a\":1}'\n */\nconst http = require('http');\n\nhttp.createServer((req, res) => {\n  const started = Date.now();\n  console.log('\\n1) REQUEST RECEIVED');\n  console.log('   method :', req.method);\n  console.log('   url    :', req.url);\n  console.log('   headers:', req.headers['content-type'] || '(none)');\n\n  let body = '';\n  req.on('data', (chunk) => (body += chunk));            // 2) body arrives in chunks\n  req.on('end', () => {\n    console.log('2) BODY COMPLETE  :', body || '(empty)');\n\n    const payload = {                                     // 3) server processes the request\n      method: req.method,\n      path: new URL(req.url, 'http://localhost').pathname,\n      query: Object.fromEntries(new URL(req.url, 'http://localhost').searchParams),\n      body: body ? JSON.parse(body) : null,\n    };\n\n    res.writeHead(200, { 'Content-Type': 'application/json', 'X-Powered-By': 'NodeLab' }); // 4) response\n    res.end(JSON.stringify(payload, null, 2));\n    console.log(`3) RESPONSE SENT in ${Date.now() - started} ms`);\n  });\n}).listen(3000, () => console.log('Listening on http://localhost:3000'));\n", "nodejs-backend/02-nodejs-fundamentals/01-builtin-modules.js": "/**\n * Experiment: Create a Node.js application using built-in modules.\n * Run: node 01-builtin-modules.js\n */\nconst os = require('os');\nconst util = require('util');\nconst EventEmitter = require('events');\nconst crypto = require('crypto');\n\nconsole.log('Platform :', os.platform());\nconsole.log('CPUs     :', os.cpus().length);\nconsole.log('Free mem :', (os.freemem() / 1024 ** 3).toFixed(2), 'GB');\n\nconsole.log(util.format('Hello %s, you are %d years old', 'Student', 20));\nconsole.log('SHA-256  :', crypto.createHash('sha256').update('node').digest('hex'));\n\nclass Notifier extends EventEmitter {}\nconst notifier = new Notifier();\nnotifier.on('greet', (name) => console.log(`Event received: Hello ${name}`));\nnotifier.emit('greet', 'Node.js');\n", "nodejs-backend/02-nodejs-fundamentals/02-fs-module.js": "/**\n * Experiment: Demonstrate the File System (fs) module.\n * Run: node 02-fs-module.js\n */\nconst fs = require('fs');\nconst path = require('path');\n\nconst dir = path.join(__dirname, 'fs-demo');\nconst file = path.join(dir, 'notes.txt');\n\nfs.mkdirSync(dir, { recursive: true });                         // create folder\nfs.writeFileSync(file, 'Line 1\\n');                              // write\nfs.appendFileSync(file, 'Line 2\\n');                             // append\nconsole.log('READ  :', fs.readFileSync(file, 'utf8'));           // read\nconsole.log('EXISTS:', fs.existsSync(file));\nconsole.log('STATS :', fs.statSync(file).size, 'bytes');\nfs.renameSync(file, path.join(dir, 'renamed.txt'));              // rename\nconsole.log('LIST  :', fs.readdirSync(dir));\n\n// asynchronous version\nfs.readFile(path.join(dir, 'renamed.txt'), 'utf8', (err, data) => {\n  if (err) return console.error(err);\n  console.log('ASYNC READ:', data.trim());\n  fs.unlinkSync(path.join(dir, 'renamed.txt'));                  // delete file\n  fs.rmdirSync(dir);                                             // delete folder\n  console.log('Cleaned up.');\n});\n", "nodejs-backend/02-nodejs-fundamentals/03-path-module.js": "/**\n * Experiment: Demonstrate the Path (path) module.\n * Run: node 03-path-module.js\n */\nconst path = require('path');\nconst p = '/home/user/projects/app/index.js';\n\nconsole.log('basename      :', path.basename(p));\nconsole.log('basename (ext):', path.basename(p, '.js'));\nconsole.log('dirname       :', path.dirname(p));\nconsole.log('extname       :', path.extname(p));\nconsole.log('join          :', path.join('folder', 'sub', '..', 'file.txt'));\nconsole.log('resolve       :', path.resolve('folder', 'file.txt'));\nconsole.log('normalize     :', path.normalize('/a//b/../c/./d.txt'));\nconsole.log('isAbsolute    :', path.isAbsolute(p), path.isAbsolute('a/b'));\nconsole.log('relative      :', path.relative('/a/b/c', '/a/d/e'));\nconsole.log('parse         :', path.parse(p));\nconsole.log('format        :', path.format({ dir: '/tmp', name: 'file', ext: '.txt' }));\nconsole.log('separator     :', path.sep);\n", "nodejs-backend/02-nodejs-fundamentals/04-custom-modules.js": "/**\n * Experiment: Create and use custom Node.js modules.\n * Run: node 04-custom-modules.js\n * Module file: 04a-mathUtils.js  (exported with module.exports)\n */\nconst { add, subtract, multiply, divide } = require('./04a-mathUtils');\n\nconsole.log('10 + 5 =', add(10, 5));\nconsole.log('10 - 5 =', subtract(10, 5));\nconsole.log('10 * 5 =', multiply(10, 5));\nconsole.log('10 / 5 =', divide(10, 5));\n\ntry { divide(1, 0); } catch (e) { console.log('Caught:', e.message); }\n\n// A module is cached: requiring it twice returns the same object\nconsole.log('Cached module reused:', require('./04a-mathUtils') === require('./04a-mathUtils'));\n", "nodejs-backend/02-nodejs-fundamentals/04a-mathUtils.js": "// Custom module used by 04-custom-modules.js\nconst add = (a, b) => a + b;\nconst subtract = (a, b) => a - b;\nconst multiply = (a, b) => a * b;\nconst divide = (a, b) => {\n  if (b === 0) throw new Error('Division by zero');\n  return a / b;\n};\n\nmodule.exports = { add, subtract, multiply, divide };\n", "nodejs-backend/02-nodejs-fundamentals/05-npm-packages.js": "/**\n * Experiment: Install and use packages using npm.\n * Setup : npm init -y            (creates package.json)\n *         npm install lodash dayjs   (adds packages to dependencies)\n * Other : npm install --save-dev nodemon | npm uninstall lodash | npm ls | npm update\n * Run   : node 05-npm-packages.js\n */\nconst _ = require('lodash');\nconst dayjs = require('dayjs');\n\nconst numbers = [5, 3, 8, 1, 9, 2];\nconsole.log('sorted   :', _.sortBy(numbers));\nconsole.log('chunked  :', _.chunk(numbers, 2));\nconsole.log('max      :', _.max(numbers));\nconsole.log('capitalize:', _.capitalize('hello NODE'));\n\nconsole.log('today    :', dayjs().format('DD-MM-YYYY HH:mm'));\nconsole.log('+7 days  :', dayjs().add(7, 'day').format('DD-MM-YYYY'));\n", "nodejs-backend/02-nodejs-fundamentals/06-callbacks-promises-async.js": "/**\n * Experiment: Demonstrate Callbacks, Promises, and Async/Await in Node.js.\n * Run: node 06-callbacks-promises-async.js\n */\n// ---- 1. CALLBACK ----\nfunction getUserCb(id, callback) {\n  setTimeout(() => callback(null, { id, name: 'Asha' }), 300);\n}\n\n// ---- 2. PROMISE ----\nfunction getUserPromise(id) {\n  return new Promise((resolve, reject) => {\n    setTimeout(() => (id > 0 ? resolve({ id, name: 'Asha' }) : reject(new Error('Invalid id'))), 300);\n  });\n}\n\n// ---- 3. ASYNC / AWAIT ----\nasync function run() {\n  try {\n    const user = await getUserPromise(1);\n    console.log('[async/await]', user);\n    await getUserPromise(-1);\n  } catch (err) {\n    console.log('[async/await] caught:', err.message);\n  }\n  const all = await Promise.all([getUserPromise(1), getUserPromise(2)]);\n  console.log('[Promise.all]', all);\n}\n\ngetUserCb(1, (err, user) => {\n  console.log('[callback]', user);\n  getUserPromise(1)\n    .then((u) => console.log('[promise]', u))\n    .then(run);\n});\n", "nodejs-backend/02-nodejs-fundamentals/07-error-handling.js": "/**\n * Experiment: Implement Error Handling in a Node.js application.\n * Run: node 07-error-handling.js\n */\nconst fs = require('fs');\n\n// 1. try / catch / finally (sync)\ntry {\n  JSON.parse('{ bad json }');\n} catch (err) {\n  console.log('1) caught:', err.name, '-', err.message);\n} finally {\n  console.log('1) finally always runs');\n}\n\n// 2. custom error class\nclass ValidationError extends Error {\n  constructor(message, field) { super(message); this.name = 'ValidationError'; this.field = field; }\n}\ntry { throw new ValidationError('Email is invalid', 'email'); }\ncatch (e) { console.log('2)', e.name, e.field, e.message); }\n\n// 3. error-first callback\nfs.readFile('missing.txt', (err) => { if (err) console.log('3) callback error:', err.code); });\n\n// 4. async/await\n(async () => {\n  try { await fs.promises.readFile('missing.txt'); }\n  catch (e) { console.log('4) async error:', e.code); }\n})();\n\n// 5. global handlers (last line of defence)\nprocess.on('uncaughtException', (e) => { console.error('5) uncaughtException:', e.message); });\nprocess.on('unhandledRejection', (r) => { console.error('5) unhandledRejection:', r.message); });\nsetTimeout(() => { Promise.reject(new Error('forgotten promise')); }, 200);\nsetTimeout(() => { throw new Error('unexpected crash'); }, 400);\n", "nodejs-backend/03-express-framework/01-basic-express-app.js": "/**\n * Experiment: Create a basic Express.js application.\n * Run: node 01-basic-express-app.js   -> http://localhost:3000\n */\nconst express = require('express');\nconst app = express();\n\napp.get('/', (req, res) => res.send('Hello Express!'));\n\napp.listen(3000, () => console.log('Express app running on http://localhost:3000'));\n", "nodejs-backend/03-express-framework/02-get-post-requests.js": "/**\n * Experiment: Handle GET and POST requests using Express.\n * Run  : node 02-get-post-requests.js\n * Test : curl http://localhost:3000/greet?name=Ravi\n *        curl -X POST http://localhost:3000/students -H \"Content-Type: application/json\" -d '{\"name\":\"Ravi\"}'\n */\nconst express = require('express');\nconst app = express();\napp.use(express.json());                        // parse JSON body\napp.use(express.urlencoded({ extended: true })); // parse form body\n\nconst students = [];\n\napp.get('/greet', (req, res) => res.send(`Hello ${req.query.name || 'Guest'}`));\napp.get('/students', (req, res) => res.json(students));\n\napp.post('/students', (req, res) => {\n  const student = { id: students.length + 1, ...req.body };\n  students.push(student);\n  res.status(201).json(student);\n});\n\napp.listen(3000, () => console.log('http://localhost:3000'));\n", "nodejs-backend/03-express-framework/03-static-files.js": "/**\n * Experiment: Serve static files using Express.\n * Run: node 03-static-files.js  -> http://localhost:3000  and  /style.css\n */\nconst express = require('express');\nconst path = require('path');\nconst app = express();\n\napp.use(express.static(path.join(__dirname, 'public')));                 // served at /\napp.use('/assets', express.static(path.join(__dirname, 'public')));      // also served at /assets\n\napp.listen(3000, () => console.log('http://localhost:3000'));\n", "nodejs-backend/03-express-framework/04-html-json-responses.js": "/**\n * Experiment: Send HTML and JSON responses using Express.\n * Run: node 04-html-json-responses.js\n */\nconst express = require('express');\nconst path = require('path');\nconst app = express();\n\napp.get('/text', (req, res) => res.send('Plain text response'));\napp.get('/html', (req, res) => res.send('<h1 style=\"color:teal\">HTML response</h1><p>Sent with res.send()</p>'));\napp.get('/html-file', (req, res) => res.sendFile(path.join(__dirname, 'public', 'index.html')));\napp.get('/json', (req, res) => res.json({ id: 1, name: 'Asha', skills: ['node', 'express'] }));\napp.get('/status', (req, res) => res.status(201).json({ created: true }));\napp.get('/redirect', (req, res) => res.redirect('/json'));\n\napp.listen(3000, () => console.log('http://localhost:3000'));\n", "nodejs-backend/03-express-framework/05-modular-project-structure/app.js": "/**\n * Experiment: Create an Express application with a modular project structure.\n * Structure:\n *   app.js -> routes/items.js -> controllers/itemController.js\n * Run: node app.js  ->  GET/POST http://localhost:3000/api/items\n */\nconst express = require('express');\nconst itemRoutes = require('./routes/items');\n\nconst app = express();\napp.use(express.json());\napp.get('/', (req, res) => res.send('Modular Express App'));\napp.use('/api/items', itemRoutes);\n\napp.listen(3000, () => console.log('http://localhost:3000'));\n", "nodejs-backend/03-express-framework/05-modular-project-structure/controllers/itemController.js": "const items = [{ id: 1, name: 'Notebook' }, { id: 2, name: 'Pen' }];\n\nexports.list = (req, res) => res.json(items);\n\nexports.get = (req, res) => {\n  const item = items.find((i) => i.id === Number(req.params.id));\n  item ? res.json(item) : res.status(404).json({ error: 'Item not found' });\n};\n\nexports.create = (req, res) => {\n  const item = { id: items.length + 1, name: req.body.name };\n  items.push(item);\n  res.status(201).json(item);\n};\n", "nodejs-backend/03-express-framework/05-modular-project-structure/routes/items.js": "const router = require('express').Router();\nconst controller = require('../controllers/itemController');\n\nrouter.get('/', controller.list);\nrouter.get('/:id', controller.get);\nrouter.post('/', controller.create);\n\nmodule.exports = router;\n", "nodejs-backend/03-express-framework/public/app.js": "console.log('Static JavaScript file loaded');\n", "nodejs-backend/03-express-framework/public/index.html": "<!DOCTYPE html>\n<html>\n<head><title>Static Page</title><link rel=\"stylesheet\" href=\"/style.css\"></head>\n<body>\n  <h1>Served by express.static()</h1>\n  <p>This HTML, its CSS and script are static files.</p>\n  <script src=\"/app.js\"></script>\n</body>\n</html>\n", "nodejs-backend/03-express-framework/public/style.css": "body { font-family: Arial, sans-serif; background: #f4f6fb; text-align: center; margin-top: 60px; }\nh1 { color: #2b6cb0; }\n", "nodejs-backend/04-routing/01-basic-routing.js": "/**\n * Experiment: Implement basic routing using Express.\n * Run: node 01-basic-routing.js\n */\nconst express = require('express');\nconst app = express();\n\napp.get('/', (req, res) => res.send('Home'));\napp.get('/about', (req, res) => res.send('About'));\napp.post('/contact', (req, res) => res.send('POST /contact'));\napp.put('/profile', (req, res) => res.send('PUT /profile'));\napp.delete('/profile', (req, res) => res.send('DELETE /profile'));\napp.all('/any', (req, res) => res.send(`Handled ${req.method} on /any`));\napp.route('/book')                                   // chained handlers, one path\n  .get((req, res) => res.send('Get book'))\n  .post((req, res) => res.send('Add book'));\n\napp.listen(3000, () => console.log('http://localhost:3000'));\n", "nodejs-backend/04-routing/02-route-and-query-parameters.js": "/**\n * Experiment: Implement route parameters and query parameters.\n * Run  : node 02-route-and-query-parameters.js\n * Test : /users/5            (route param)\n *        /users/5/posts/9    (multiple params)\n *        /search?q=node&page=2&limit=10   (query params)\n */\nconst express = require('express');\nconst app = express();\n\napp.get('/users/:id', (req, res) => res.json({ id: req.params.id }));\napp.get('/users/:userId/posts/:postId', (req, res) => res.json(req.params));\napp.get('/products/:id(\\\\d+)', (req, res) => res.json({ numericId: Number(req.params.id) })); // regex constraint\n\napp.get('/search', (req, res) => {\n  const { q = '', page = 1, limit = 10 } = req.query;\n  res.json({ query: q, page: Number(page), limit: Number(limit) });\n});\n\napp.param('id', (req, res, next, id) => { console.log('param id =', id); next(); });\n\napp.listen(3000, () => console.log('http://localhost:3000'));\n", "nodejs-backend/04-routing/03-express-router.js": "/**\n * Experiment: Create multiple routes using Express Router.\n * Run: node 03-express-router.js  -> /api/users , /api/products , /api/orders\n */\nconst express = require('express');\nconst app = express();\n\nconst userRouter = express.Router();\nuserRouter.get('/', (req, res) => res.json(['Asha', 'Ravi']));\nuserRouter.get('/:id', (req, res) => res.json({ user: req.params.id }));\n\nconst productRouter = express.Router();\nproductRouter.get('/', (req, res) => res.json(['Laptop', 'Phone']));\n\nconst orderRouter = express.Router();\norderRouter.get('/', (req, res) => res.json([{ id: 1, total: 500 }]));\n\napp.use('/api/users', userRouter);\napp.use('/api/products', productRouter);\napp.use('/api/orders', orderRouter);\n\napp.listen(3000, () => console.log('http://localhost:3000'));\n", "nodejs-backend/04-routing/04-not-found-404-handling.js": "/**\n * Experiment: Implement 404 / Not Found route handling.\n * Run: node 04-not-found-404-handling.js  -> try /  and  /does-not-exist\n */\nconst express = require('express');\nconst app = express();\n\napp.get('/', (req, res) => res.send('Home page'));\napp.get('/hello', (req, res) => res.send('Hello'));\n\n// must be the LAST middleware -> reached only when no route matched\napp.use((req, res) => {\n  res.status(404);\n  if (req.accepts('html')) return res.send(`<h1>404</h1><p>${req.originalUrl} was not found.</p>`);\n  res.json({ error: 'Not Found', path: req.originalUrl });\n});\n\napp.listen(3000, () => console.log('http://localhost:3000'));\n", "nodejs-backend/04-routing/05-modular-routes/app.js": "/**\n * Experiment: Create modular routes for users, products, or students.\n * Run: node app.js -> /users , /products , /students\n */\nconst express = require('express');\nconst app = express();\napp.use(express.json());\n\napp.use('/users', require('./routes/users'));\napp.use('/products', require('./routes/products'));\napp.use('/students', require('./routes/students'));\napp.use((req, res) => res.status(404).json({ error: 'Not Found' }));\n\napp.listen(3000, () => console.log('http://localhost:3000'));\n", "nodejs-backend/04-routing/05-modular-routes/routes/products.js": "const router = require('express').Router();\nconst products = [{ id: 1, name: 'Laptop', price: 55000 }, { id: 2, name: 'Mouse', price: 500 }];\n\nrouter.get('/', (req, res) => res.json(products));\nrouter.get('/:id', (req, res) => {\n  const p = products.find((x) => x.id === +req.params.id);\n  p ? res.json(p) : res.status(404).json({ error: 'Product not found' });\n});\nrouter.post('/', (req, res) => { const p = { id: products.length + 1, ...req.body }; products.push(p); res.status(201).json(p); });\n\nmodule.exports = router;\n", "nodejs-backend/04-routing/05-modular-routes/routes/students.js": "const router = require('express').Router();\nconst students = [{ id: 1, name: 'Meena', course: 'CSE' }];\n\nrouter.get('/', (req, res) => res.json(students));\nrouter.get('/:id', (req, res) => {\n  const s = students.find((x) => x.id === +req.params.id);\n  s ? res.json(s) : res.status(404).json({ error: 'Student not found' });\n});\nrouter.post('/', (req, res) => { const s = { id: students.length + 1, ...req.body }; students.push(s); res.status(201).json(s); });\n\nmodule.exports = router;\n", "nodejs-backend/04-routing/05-modular-routes/routes/users.js": "const router = require('express').Router();\nconst users = [{ id: 1, name: 'Asha' }, { id: 2, name: 'Ravi' }];\n\nrouter.get('/', (req, res) => res.json(users));\nrouter.get('/:id', (req, res) => {\n  const u = users.find((x) => x.id === +req.params.id);\n  u ? res.json(u) : res.status(404).json({ error: 'User not found' });\n});\nrouter.post('/', (req, res) => { const u = { id: users.length + 1, ...req.body }; users.push(u); res.status(201).json(u); });\n\nmodule.exports = router;\n", "nodejs-backend/05-middleware/01-custom-middleware.js": "/**\n * Experiment: Create and implement custom middleware.\n * Run: node 01-custom-middleware.js\n */\nconst express = require('express');\nconst app = express();\n\n// application-level middleware: adds a timestamp to every request\nconst addTimestamp = (req, res, next) => {\n  req.requestTime = new Date().toISOString();\n  next();                                   // pass control to the next handler\n};\n\n// middleware factory (configurable)\nconst addHeader = (name, value) => (req, res, next) => { res.setHeader(name, value); next(); };\n\napp.use(addTimestamp);\napp.use(addHeader('X-App-Name', 'MiddlewareLab'));\n\napp.get('/', (req, res) => res.send(`Request received at ${req.requestTime}`));\n\n// route-level middleware\napp.get('/secret', (req, res, next) => { req.secret = 42; next(); }, (req, res) => res.json({ secret: req.secret }));\n\napp.listen(3000, () => console.log('http://localhost:3000'));\n", "nodejs-backend/05-middleware/02-logging-middleware.js": "/**\n * Experiment: Implement logging middleware.\n * Run: node 02-logging-middleware.js  -> logs also written to requests.log\n */\nconst express = require('express');\nconst fs = require('fs');\nconst path = require('path');\nconst app = express();\n\nconst logFile = fs.createWriteStream(path.join(__dirname, 'requests.log'), { flags: 'a' });\n\nfunction logger(req, res, next) {\n  const start = Date.now();\n  res.on('finish', () => {                 // runs after the response is sent\n    const line = `${new Date().toISOString()} ${req.method} ${req.originalUrl} ${res.statusCode} ${Date.now() - start}ms`;\n    console.log(line);\n    logFile.write(line + '\\n');\n  });\n  next();\n}\n\napp.use(logger);\napp.get('/', (req, res) => res.send('Home'));\napp.get('/slow', (req, res) => setTimeout(() => res.send('Slow response'), 500));\n\napp.listen(3000, () => console.log('http://localhost:3000'));\n", "nodejs-backend/05-middleware/03-request-validation-middleware.js": "/**\n * Experiment: Implement request validation middleware.\n * Run  : node 03-request-validation-middleware.js\n * Test : curl -X POST localhost:3000/register -H \"Content-Type: application/json\" -d '{\"name\":\"A\",\"email\":\"bad\"}'\n */\nconst express = require('express');\nconst app = express();\napp.use(express.json());\n\nconst validateRegistration = (req, res, next) => {\n  const { name, email, age } = req.body;\n  const errors = [];\n  if (!name || name.trim().length < 3) errors.push('name must be at least 3 characters');\n  if (!email || !/^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/.test(email)) errors.push('email is invalid');\n  if (age !== undefined && (!Number.isInteger(age) || age < 0)) errors.push('age must be a positive integer');\n  if (errors.length) return res.status(400).json({ success: false, errors });\n  next();\n};\n\napp.post('/register', validateRegistration, (req, res) => res.status(201).json({ success: true, user: req.body }));\n\napp.listen(3000, () => console.log('http://localhost:3000'));\n", "nodejs-backend/05-middleware/04-authentication-middleware.js": "/**\n * Experiment: Implement authentication middleware.\n * Run  : node 04-authentication-middleware.js\n * Test : curl localhost:3000/dashboard                      -> 401\n *        curl localhost:3000/dashboard -H \"Authorization: Bearer secret-token-123\"   -> 200\n */\nconst express = require('express');\nconst app = express();\n\nconst VALID_TOKENS = { 'secret-token-123': { id: 1, name: 'Asha' } };\n\nfunction authenticate(req, res, next) {\n  const header = req.headers.authorization || '';\n  const [scheme, token] = header.split(' ');\n  if (scheme !== 'Bearer' || !token) return res.status(401).json({ error: 'Missing bearer token' });\n  const user = VALID_TOKENS[token];\n  if (!user) return res.status(403).json({ error: 'Invalid token' });\n  req.user = user;\n  next();\n}\n\napp.get('/public', (req, res) => res.send('Anyone can see this'));\napp.get('/dashboard', authenticate, (req, res) => res.json({ message: `Welcome ${req.user.name}` }));\n\napp.listen(3000, () => console.log('http://localhost:3000'));\n", "nodejs-backend/05-middleware/05-error-handling-middleware.js": "/**\n * Experiment: Implement error-handling middleware.\n * Run: node 05-error-handling-middleware.js -> /ok , /bad-request , /crash , /async-crash\n */\nconst express = require('express');\nconst app = express();\n\nclass HttpError extends Error {\n  constructor(status, message) { super(message); this.status = status; }\n}\n\napp.get('/ok', (req, res) => res.send('fine'));\napp.get('/bad-request', (req, res, next) => next(new HttpError(400, 'Bad request example')));\napp.get('/crash', () => { throw new Error('Synchronous crash'); });\napp.get('/async-crash', async (req, res, next) => {\n  try { await Promise.reject(new Error('Async failure')); } catch (e) { next(e); }\n});\n\napp.use((req, res, next) => next(new HttpError(404, `Route ${req.originalUrl} not found`)));\n\n// error-handling middleware: exactly FOUR parameters\napp.use((err, req, res, next) => {\n  console.error('[ERROR]', err.message);\n  res.status(err.status || 500).json({ success: false, message: err.message });\n});\n\napp.listen(3000, () => console.log('http://localhost:3000'));\n", "nodejs-backend/05-middleware/06-middleware-execution-order.js": "/**\n * Experiment: Demonstrate the execution order of middleware.\n * Run: node 06-middleware-execution-order.js -> GET /  and watch the console order\n * Rule: middleware runs in the order it is registered; next() moves on; a response ends the chain.\n */\nconst express = require('express');\nconst app = express();\n\napp.use((req, res, next) => { console.log('1. first global middleware'); next(); });\napp.use((req, res, next) => { console.log('2. second global middleware'); next(); });\n\napp.get('/', (req, res, next) => { console.log('3. route middleware A'); next(); },\n               (req, res, next) => { console.log('4. route middleware B'); next(); },\n               (req, res) => { console.log('5. final route handler -> response sent'); res.send('Check the console order'); });\n\napp.use((req, res, next) => { console.log('6. NEVER printed for \"/\" (response already sent)'); next(); });\n\napp.get('/stop', (req, res, next) => { console.log('/stop handler'); res.send('stopped'); });\napp.use((req, res) => { console.log('7. fallback (404) middleware'); res.status(404).send('Not found'); });\n\napp.listen(3000, () => console.log('http://localhost:3000'));\n", "nodejs-backend/06-session-and-cookie-management/01-create-cookies.js": "/**\n * Experiment: Create and manage cookies using Express.\n * Run: node 01-create-cookies.js  -> open /set-cookie then check DevTools > Application > Cookies\n */\nconst express = require('express');\nconst cookieParser = require('cookie-parser');\nconst app = express();\napp.use(cookieParser());\n\napp.get('/set-cookie', (req, res) => {\n  res.cookie('username', 'asha');                                                // session cookie\n  res.cookie('theme', 'dark', { maxAge: 60 * 60 * 1000, httpOnly: true });        // 1 hour, not readable by JS\n  res.cookie('cart', JSON.stringify({ items: 3 }), { path: '/', sameSite: 'lax' });\n  res.send('Cookies have been set');\n});\n\napp.listen(3000, () => console.log('http://localhost:3000/set-cookie'));\n", "nodejs-backend/06-session-and-cookie-management/02-read-and-delete-cookies.js": "/**\n * Experiment: Read and delete cookies using Express.\n * Run: node 02-read-and-delete-cookies.js\n * Flow: /set -> /read -> /delete -> /read\n */\nconst express = require('express');\nconst cookieParser = require('cookie-parser');\nconst app = express();\napp.use(cookieParser('my-signing-secret'));\n\napp.get('/set', (req, res) => {\n  res.cookie('username', 'asha');\n  res.cookie('token', 'abc123', { signed: true, httpOnly: true });\n  res.send('Cookies set');\n});\n\napp.get('/read', (req, res) => {\n  res.json({ all: req.cookies, signed: req.signedCookies, username: req.cookies.username });\n});\n\napp.get('/delete', (req, res) => {\n  res.clearCookie('username');\n  res.clearCookie('token');\n  res.send('Cookies deleted');\n});\n\napp.listen(3000, () => console.log('http://localhost:3000/set'));\n", "nodejs-backend/06-session-and-cookie-management/03-session-management.js": "/**\n * Experiment: Implement session management in Express.\n * Run: node 03-session-management.js -> refresh http://localhost:3000 repeatedly\n */\nconst express = require('express');\nconst session = require('express-session');\nconst app = express();\n\napp.use(session({\n  secret: 'change-this-secret',\n  resave: false,\n  saveUninitialized: false,\n  cookie: { maxAge: 10 * 60 * 1000, httpOnly: true },   // 10 minutes\n}));\n\napp.get('/', (req, res) => {\n  req.session.views = (req.session.views || 0) + 1;\n  res.send(`Session ${req.sessionID} - you visited ${req.session.views} time(s)`);\n});\n\napp.get('/reset', (req, res) => req.session.destroy(() => res.send('Session destroyed')));\n\napp.listen(3000, () => console.log('http://localhost:3000'));\n", "nodejs-backend/06-session-and-cookie-management/04-login-and-logout-with-sessions.js": "/**\n * Experiment: Implement login and logout using sessions.\n * Run  : node 04-login-and-logout-with-sessions.js\n * Test : curl -c jar.txt -X POST localhost:3000/login -H \"Content-Type: application/json\" -d '{\"username\":\"admin\",\"password\":\"admin123\"}'\n *        curl -b jar.txt localhost:3000/profile ; curl -b jar.txt -X POST localhost:3000/logout\n */\nconst express = require('express');\nconst session = require('express-session');\nconst app = express();\napp.use(express.json());\napp.use(session({ secret: 'lab-secret', resave: false, saveUninitialized: false }));\n\nconst USERS = [{ username: 'admin', password: 'admin123' }, { username: 'asha', password: 'pass123' }];\n\napp.post('/login', (req, res) => {\n  const { username, password } = req.body;\n  const user = USERS.find((u) => u.username === username && u.password === password);\n  if (!user) return res.status(401).json({ error: 'Invalid credentials' });\n  req.session.user = { username: user.username };\n  res.json({ message: 'Logged in', user: req.session.user });\n});\n\napp.get('/profile', (req, res) =>\n  req.session.user ? res.json(req.session.user) : res.status(401).json({ error: 'Not logged in' }));\n\napp.post('/logout', (req, res) => req.session.destroy(() => { res.clearCookie('connect.sid'); res.json({ message: 'Logged out' }); }));\n\napp.listen(3000, () => console.log('http://localhost:3000'));\n", "nodejs-backend/06-session-and-cookie-management/05-protected-routes-session-auth.js": "/**\n * Experiment: Create protected routes using session authentication.\n * Run: node 05-protected-routes-session-auth.js\n * Open /login-demo (auto login) then /dashboard and /admin ; without login you get 401 / redirect\n */\nconst express = require('express');\nconst session = require('express-session');\nconst app = express();\napp.use(session({ secret: 'lab-secret', resave: false, saveUninitialized: false }));\n\nconst requireLogin = (req, res, next) => (req.session.user ? next() : res.status(401).send('401 - Please log in first'));\nconst requireAdmin = (req, res, next) =>\n  req.session.user && req.session.user.role === 'admin' ? next() : res.status(403).send('403 - Admins only');\n\napp.get('/', (req, res) => res.send('Public home. Try /login-demo?role=admin then /dashboard and /admin'));\napp.get('/login-demo', (req, res) => {\n  req.session.user = { name: 'Demo', role: req.query.role === 'admin' ? 'admin' : 'user' };\n  res.send(`Logged in as ${req.session.user.role}`);\n});\napp.get('/dashboard', requireLogin, (req, res) => res.send(`Dashboard for ${req.session.user.name}`));\napp.get('/admin', requireLogin, requireAdmin, (req, res) => res.send('Admin panel'));\napp.get('/logout', (req, res) => req.session.destroy(() => res.redirect('/')));\n\napp.listen(3000, () => console.log('http://localhost:3000'));\n", "nodejs-backend/07-database-connectivity/01-connect-database.js": "/**\n * Experiment: Connect a Node.js application to a database.\n * DB: SQLite via better-sqlite3 (no server needed).  MySQL/MongoDB notes are at the bottom.\n * Run: node 01-connect-database.js\n */\nconst Database = require('better-sqlite3');\nconst path = require('path');\n\ntry {\n  const db = new Database(path.join(__dirname, 'lab.db'));\n  const { version } = db.prepare('SELECT sqlite_version() AS version').get();\n  console.log('Connected to SQLite successfully. Version:', version);\n  db.close();\n} catch (err) {\n  console.error('Connection failed:', err.message);\n}\n\n/* MySQL equivalent:\n   const mysql = require('mysql2/promise');\n   const conn = await mysql.createConnection({ host:'localhost', user:'root', password:'', database:'lab' });\n   MongoDB equivalent:\n   await require('mongoose').connect('mongodb://127.0.0.1:27017/lab');\n*/\n", "nodejs-backend/07-database-connectivity/02-create-tables.js": "/**\n * Experiment: Create and manage database tables/collections.\n * Run: node 02-create-tables.js\n */\nconst Database = require('better-sqlite3');\nconst path = require('path');\nconst db = new Database(path.join(__dirname, 'lab.db'));\n\ndb.exec(`\n  CREATE TABLE IF NOT EXISTS departments (\n    id INTEGER PRIMARY KEY AUTOINCREMENT,\n    name TEXT NOT NULL UNIQUE\n  );\n  CREATE TABLE IF NOT EXISTS students (\n    id INTEGER PRIMARY KEY AUTOINCREMENT,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE NOT NULL,\n    age INTEGER CHECK (age > 0),\n    department_id INTEGER REFERENCES departments(id)\n  );\n`);\n\ndb.exec('ALTER TABLE students ADD COLUMN phone TEXT');                 // manage: add a column\nconsole.log('Tables:', db.prepare(\"SELECT name FROM sqlite_master WHERE type='table'\").all());\nconsole.log('students columns:', db.prepare('PRAGMA table_info(students)').all().map((c) => c.name));\ndb.exec('CREATE TABLE IF NOT EXISTS temp_table (id INTEGER)');\ndb.exec('DROP TABLE temp_table');                                       // manage: drop a table\n", "nodejs-backend/07-database-connectivity/03-insert-data.js": "/**\n * Experiment: Insert data into a database using Node.js.\n * Run: node 03-insert-data.js   (run 02-create-tables.js first)\n */\nconst Database = require('better-sqlite3');\nconst path = require('path');\nconst db = new Database(path.join(__dirname, 'lab.db'));\n\ndb.exec(\"CREATE TABLE IF NOT EXISTS students (id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT NOT NULL, email TEXT UNIQUE NOT NULL, age INTEGER, department_id INTEGER, phone TEXT)\");\n\nconst insert = db.prepare('INSERT OR IGNORE INTO students (name, email, age) VALUES (?, ?, ?)');   // parameterised => no SQL injection\n\nconst info = insert.run('Asha', 'asha@example.com', 20);\nconsole.log('Inserted id:', info.lastInsertRowid, 'changes:', info.changes);\n\n// many rows inside one transaction\nconst insertMany = db.transaction((rows) => rows.forEach((r) => insert.run(r.name, r.email, r.age)));\ninsertMany([\n  { name: 'Ravi', email: 'ravi@example.com', age: 21 },\n  { name: 'Meena', email: 'meena@example.com', age: 19 },\n]);\nconsole.log('Total rows:', db.prepare('SELECT COUNT(*) AS c FROM students').get().c);\n", "nodejs-backend/07-database-connectivity/04-retrieve-data.js": "/**\n * Experiment: Retrieve data from a database using Node.js.\n * Run: node 04-retrieve-data.js   (run 03-insert-data.js first)\n */\nconst Database = require('better-sqlite3');\nconst path = require('path');\nconst db = new Database(path.join(__dirname, 'lab.db'));\n\nconsole.log('ALL       :', db.prepare('SELECT * FROM students').all());\nconsole.log('ONE       :', db.prepare('SELECT * FROM students WHERE id = ?').get(1));\nconsole.log('FILTER    :', db.prepare('SELECT name, age FROM students WHERE age >= ? ORDER BY age DESC').all(20));\nconsole.log('SEARCH    :', db.prepare('SELECT name FROM students WHERE name LIKE ?').all('%a%'));\nconsole.log('PAGINATE  :', db.prepare('SELECT * FROM students LIMIT ? OFFSET ?').all(2, 0));\nconsole.log('AGGREGATE :', db.prepare('SELECT COUNT(*) AS total, AVG(age) AS avg_age FROM students').get());\n", "nodejs-backend/07-database-connectivity/05-update-data.js": "/**\n * Experiment: Update database records using Node.js.\n * Run: node 05-update-data.js\n */\nconst Database = require('better-sqlite3');\nconst path = require('path');\nconst db = new Database(path.join(__dirname, 'lab.db'));\n\nconst before = db.prepare('SELECT * FROM students WHERE email = ?').get('asha@example.com');\nconsole.log('Before:', before);\n\nconst info = db.prepare('UPDATE students SET age = ?, phone = ? WHERE email = ?').run(22, '9876543210', 'asha@example.com');\nconsole.log('Rows updated:', info.changes);\n\ndb.prepare('UPDATE students SET age = age + 1').run();                       // bulk update\nconsole.log('After :', db.prepare('SELECT * FROM students WHERE email = ?').get('asha@example.com'));\n", "nodejs-backend/07-database-connectivity/06-delete-data.js": "/**\n * Experiment: Delete database records using Node.js.\n * Run: node 06-delete-data.js\n */\nconst Database = require('better-sqlite3');\nconst path = require('path');\nconst db = new Database(path.join(__dirname, 'lab.db'));\n\nconsole.log('Before:', db.prepare('SELECT COUNT(*) AS c FROM students').get().c);\nconst one = db.prepare('DELETE FROM students WHERE email = ?').run('meena@example.com');\nconsole.log('Deleted rows (by email):', one.changes);\nconst many = db.prepare('DELETE FROM students WHERE age > ?').run(100);\nconsole.log('Deleted rows (age > 100):', many.changes);\nconsole.log('After :', db.prepare('SELECT COUNT(*) AS c FROM students').get().c);\n// db.prepare('DELETE FROM students').run();   // removes ALL rows - use with care\n", "nodejs-backend/08-crud-operations/01-crud-express-nodejs.js": "/**\n * Experiment: Implement CRUD operations using Node.js and Express (in-memory array).\n * Run  : node 01-crud-express-nodejs.js\n * Test : POST/GET/PUT/DELETE http://localhost:3000/items\n */\nconst express = require('express');\nconst app = express();\napp.use(express.json());\n\nlet items = [{ id: 1, name: 'Book' }];\nlet nextId = 2;\n\napp.post('/items', (req, res) => {                                   // CREATE\n  const item = { id: nextId++, name: req.body.name };\n  items.push(item);\n  res.status(201).json(item);\n});\napp.get('/items', (req, res) => res.json(items));                     // READ all\napp.get('/items/:id', (req, res) => {                                 // READ one\n  const item = items.find((i) => i.id === +req.params.id);\n  item ? res.json(item) : res.status(404).json({ error: 'Not found' });\n});\napp.put('/items/:id', (req, res) => {                                 // UPDATE\n  const item = items.find((i) => i.id === +req.params.id);\n  if (!item) return res.status(404).json({ error: 'Not found' });\n  item.name = req.body.name ?? item.name;\n  res.json(item);\n});\napp.delete('/items/:id', (req, res) => {                              // DELETE\n  const before = items.length;\n  items = items.filter((i) => i.id !== +req.params.id);\n  items.length < before ? res.status(204).end() : res.status(404).json({ error: 'Not found' });\n});\n\napp.listen(3000, () => console.log('http://localhost:3000/items'));\n", "nodejs-backend/08-crud-operations/02-student-management-crud.js": "/**\n * Experiment: Create a Student Management CRUD application (SQLite).\n * Run: node 02-student-management-crud.js\n * Try: curl -X POST localhost:3000/students -H \"Content-Type: application/json\" \\\n *        -d '{\"name\":\"Asha\",\"email\":\"asha@x.com\",\"course\":\"CSE\",\"age\":20}'\n * Also open http://localhost:3000 for a simple HTML page.\n */\nconst express = require('express');\nconst db = require('../common/db');\nconst crud = require('../common/crudFactory');\nconst v = require('../common/validators');\n\nconst app = express();\napp.use(express.json());\napp.use('/students', crud(db, 'students', {\n  name: v.string(2), email: v.email(), course: v.string(2), age: v.int(15),\n}));\n\napp.get('/', (req, res) => res.send(`<h2>Student Management</h2>\n<ul id=\"l\"></ul><script>fetch('/students').then(r=>r.json()).then(d=>l.innerHTML=d.map(s=>'<li>'+s.name+' - '+s.course+'</li>').join(''))</script>`));\n\napp.listen(3000, () => console.log('http://localhost:3000'));\n", "nodejs-backend/08-crud-operations/03-user-management-crud.js": "/**\n * Experiment: Create a User Management CRUD application (SQLite).\n * Run: node 03-user-management-crud.js\n * Try: curl -X POST localhost:3000/users -H \"Content-Type: application/json\" \\\n *        -d '{\"name\":\"Ravi\",\"email\":\"ravi@x.com\",\"role\":\"user\"}'\n */\nconst express = require('express');\nconst db = require('../common/db');\nconst crud = require('../common/crudFactory');\nconst v = require('../common/validators');\n\nconst app = express();\napp.use(express.json());\napp.use('/users', crud(db, 'users', {\n  name: v.string(2), email: v.email(), role: v.oneOf(['user', 'admin']),\n}, ['password']));\n\napp.listen(3000, () => console.log('http://localhost:3000/users'));\n", "nodejs-backend/08-crud-operations/04-product-management-crud.js": "/**\n * Experiment: Create a Product Management CRUD application (SQLite).\n * Run: node 04-product-management-crud.js\n * Try: curl -X POST localhost:3000/products -H \"Content-Type: application/json\" \\\n *        -d '{\"name\":\"Keyboard\",\"price\":999.5,\"stock\":10}'\n */\nconst express = require('express');\nconst db = require('../common/db');\nconst crud = require('../common/crudFactory');\nconst v = require('../common/validators');\n\nconst app = express();\napp.use(express.json());\napp.use('/products', crud(db, 'products', {\n  name: v.string(2), price: v.number(0), stock: v.int(0),\n}));\n\napp.listen(3000, () => console.log('http://localhost:3000/products'));\n", "nodejs-backend/08-crud-operations/05-crud-with-database-connectivity.js": "/**\n * Experiment: Implement CRUD operations with database connectivity.\n * Hand-written SQL (no factory) for a \"tasks\" table so every step is visible.\n * Run: node 05-crud-with-database-connectivity.js\n */\nconst express = require('express');\nconst Database = require('better-sqlite3');\nconst path = require('path');\n\nconst db = new Database(path.join(__dirname, 'tasks.db'));\ndb.exec(`CREATE TABLE IF NOT EXISTS tasks (\n  id INTEGER PRIMARY KEY AUTOINCREMENT,\n  title TEXT NOT NULL,\n  done INTEGER NOT NULL DEFAULT 0\n)`);\n\nconst app = express();\napp.use(express.json());\n\napp.post('/tasks', (req, res) => {\n  const info = db.prepare('INSERT INTO tasks (title) VALUES (?)').run(req.body.title);\n  res.status(201).json({ id: info.lastInsertRowid, title: req.body.title, done: 0 });\n});\napp.get('/tasks', (req, res) => res.json(db.prepare('SELECT * FROM tasks').all()));\napp.get('/tasks/:id', (req, res) => {\n  const t = db.prepare('SELECT * FROM tasks WHERE id = ?').get(req.params.id);\n  t ? res.json(t) : res.status(404).json({ error: 'Task not found' });\n});\napp.put('/tasks/:id', (req, res) => {\n  const { title, done } = req.body;\n  const info = db.prepare('UPDATE tasks SET title = COALESCE(?, title), done = COALESCE(?, done) WHERE id = ?')\n    .run(title ?? null, done === undefined ? null : Number(done), req.params.id);\n  if (!info.changes) return res.status(404).json({ error: 'Task not found' });\n  res.json(db.prepare('SELECT * FROM tasks WHERE id = ?').get(req.params.id));\n});\napp.delete('/tasks/:id', (req, res) => {\n  const info = db.prepare('DELETE FROM tasks WHERE id = ?').run(req.params.id);\n  info.changes ? res.status(204).end() : res.status(404).json({ error: 'Task not found' });\n});\n\napp.listen(3000, () => console.log('http://localhost:3000/tasks'));\n", "nodejs-backend/08-crud-operations/06-input-validation-crud.js": "/**\n * Experiment: Implement input validation for CRUD operations.\n * Run  : node 06-input-validation-crud.js\n * Test : curl -X POST localhost:3000/students -H \"Content-Type: application/json\" -d '{\"name\":\"A\",\"email\":\"x\",\"age\":5}'\n *        -> 400 with a list of every validation error\n */\nconst express = require('express');\nconst app = express();\napp.use(express.json());\n\nconst rules = {\n  name:   (v) => (typeof v === 'string' && v.trim().length >= 2 ? null : 'must be at least 2 characters'),\n  email:  (v) => (/^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/.test(v) ? null : 'must be a valid email'),\n  age:    (v) => (Number.isInteger(v) && v >= 15 && v <= 100 ? null : 'must be an integer between 15 and 100'),\n  course: (v) => (['CSE', 'ECE', 'MECH', 'CIVIL'].includes(v) ? null : 'must be CSE, ECE, MECH or CIVIL'),\n};\n\nfunction validate(required) {\n  return (req, res, next) => {\n    const errors = {};\n    for (const field of Object.keys(rules)) {\n      const value = req.body[field];\n      if (value === undefined) { if (required) errors[field] = 'is required'; continue; }\n      const msg = rules[field](value);\n      if (msg) errors[field] = msg;\n    }\n    Object.keys(req.body).forEach((k) => { if (!rules[k]) errors[k] = 'is not an allowed field'; });\n    return Object.keys(errors).length ? res.status(400).json({ errors }) : next();\n  };\n}\n\nconst students = [];\napp.post('/students', validate(true), (req, res) => { const s = { id: students.length + 1, ...req.body }; students.push(s); res.status(201).json(s); });\napp.put('/students/:id', validate(false), (req, res) => {\n  const s = students.find((x) => x.id === +req.params.id);\n  if (!s) return res.status(404).json({ error: 'Not found' });\n  Object.assign(s, req.body); res.json(s);\n});\napp.get('/students', (req, res) => res.json(students));\n\napp.listen(3000, () => console.log('http://localhost:3000'));\n", "nodejs-backend/09-authentication/01-user-registration.js": "/**\n * Experiment: Implement User Registration.\n * Run  : node 01-user-registration.js\n * Test : curl -X POST localhost:3000/register -H \"Content-Type: application/json\" \\\n *          -d '{\"name\":\"Asha\",\"email\":\"asha@x.com\",\"password\":\"Secret123\"}'\n */\nconst express = require('express');\nconst bcrypt = require('bcryptjs');\nconst app = express();\napp.use(express.json());\n\nconst users = [];\n\napp.post('/register', async (req, res) => {\n  const { name, email, password } = req.body;\n  if (!name || !email || !password) return res.status(400).json({ error: 'name, email and password are required' });\n  if (password.length < 8) return res.status(400).json({ error: 'password must be at least 8 characters' });\n  if (users.some((u) => u.email === email)) return res.status(409).json({ error: 'Email already registered' });\n\n  const hashed = await bcrypt.hash(password, 10);\n  const user = { id: users.length + 1, name, email, password: hashed };\n  users.push(user);\n  res.status(201).json({ id: user.id, name, email });          // never return the password\n});\n\napp.get('/users', (req, res) => res.json(users.map(({ password, ...u }) => u)));\n\napp.listen(3000, () => console.log('http://localhost:3000'));\n", "nodejs-backend/09-authentication/02-login-and-logout.js": "/**\n * Experiment: Implement User Login and Logout (session based).\n * Run: node 02-login-and-logout.js\n * Flow: POST /register -> POST /login -> GET /me -> POST /logout -> GET /me (401)\n * Test with cookies:  curl -c jar -b jar ...\n */\nconst express = require('express');\nconst session = require('express-session');\nconst bcrypt = require('bcryptjs');\nconst app = express();\napp.use(express.json());\napp.use(session({ secret: 'auth-lab-secret', resave: false, saveUninitialized: false }));\n\nconst users = [];\n\napp.post('/register', async (req, res) => {\n  const { email, password } = req.body;\n  if (users.find((u) => u.email === email)) return res.status(409).json({ error: 'Email exists' });\n  users.push({ id: users.length + 1, email, password: await bcrypt.hash(password, 10) });\n  res.status(201).json({ message: 'Registered' });\n});\n\napp.post('/login', async (req, res) => {\n  const { email, password } = req.body;\n  const user = users.find((u) => u.email === email);\n  if (!user || !(await bcrypt.compare(password, user.password))) return res.status(401).json({ error: 'Invalid email or password' });\n  req.session.userId = user.id;\n  res.json({ message: 'Login successful' });\n});\n\napp.get('/me', (req, res) => {\n  const user = users.find((u) => u.id === req.session.userId);\n  user ? res.json({ id: user.id, email: user.email }) : res.status(401).json({ error: 'Not authenticated' });\n});\n\napp.post('/logout', (req, res) => req.session.destroy(() => { res.clearCookie('connect.sid'); res.json({ message: 'Logged out' }); }));\n\napp.listen(3000, () => console.log('http://localhost:3000'));\n", "nodejs-backend/09-authentication/03-password-hashing.js": "/**\n * Experiment: Implement Password Hashing.\n * Run: node 03-password-hashing.js\n * A hash is one-way; bcrypt adds a random salt so equal passwords give different hashes.\n */\nconst bcrypt = require('bcryptjs');\n\n(async () => {\n  const password = 'MyPassword@123';\n\n  const salt = await bcrypt.genSalt(10);                 // cost factor 10 => 2^10 rounds\n  const hash1 = await bcrypt.hash(password, salt);\n  const hash2 = await bcrypt.hash(password, 10);\n\n  console.log('Plain password :', password);\n  console.log('Salt           :', salt);\n  console.log('Hash 1         :', hash1);\n  console.log('Hash 2         :', hash2);\n  console.log('Same hash?     :', hash1 === hash2, '(different salts => different hashes)');\n  console.log('Hash length    :', hash1.length);\n})();\n", "nodejs-backend/09-authentication/04-password-verification.js": "/**\n * Experiment: Implement Password Verification.\n * Run: node 04-password-verification.js\n */\nconst bcrypt = require('bcryptjs');\n\n(async () => {\n  const stored = await bcrypt.hash('MyPassword@123', 10);   // what a database would store\n\n  const attempts = ['MyPassword@123', 'mypassword@123', 'wrong'];\n  for (const attempt of attempts) {\n    const ok = await bcrypt.compare(attempt, stored);         // salt is read from the stored hash\n    console.log(`\"${attempt}\" ->`, ok ? 'ACCESS GRANTED' : 'ACCESS DENIED');\n  }\n\n  // synchronous variants also exist\n  console.log('sync compare:', bcrypt.compareSync('MyPassword@123', stored));\n})();\n", "nodejs-backend/09-authentication/05-jwt-authentication.js": "/**\n * Experiment: Implement JWT-based Authentication.\n * Run  : node 05-jwt-authentication.js\n * Flow : POST /register -> POST /login (returns token) -> GET /profile with \"Authorization: Bearer <token>\"\n */\nconst express = require('express');\nconst jwt = require('jsonwebtoken');\nconst bcrypt = require('bcryptjs');\n\nconst JWT_SECRET = process.env.JWT_SECRET || 'dev-secret-change-me';\nconst app = express();\napp.use(express.json());\nconst users = [];\n\napp.post('/register', async (req, res) => {\n  const { name, email, password } = req.body;\n  if (users.find((u) => u.email === email)) return res.status(409).json({ error: 'Email exists' });\n  users.push({ id: users.length + 1, name, email, password: await bcrypt.hash(password, 10) });\n  res.status(201).json({ message: 'Registered' });\n});\n\napp.post('/login', async (req, res) => {\n  const { email, password } = req.body;\n  const user = users.find((u) => u.email === email);\n  if (!user || !(await bcrypt.compare(password, user.password))) return res.status(401).json({ error: 'Invalid credentials' });\n  const token = jwt.sign({ sub: user.id, email: user.email }, JWT_SECRET, { expiresIn: '1h' });\n  res.json({ token, tokenType: 'Bearer', expiresIn: 3600 });\n});\n\nfunction verifyToken(req, res, next) {\n  const token = (req.headers.authorization || '').replace('Bearer ', '');\n  try { req.auth = jwt.verify(token, JWT_SECRET); next(); }\n  catch (e) { res.status(401).json({ error: e.name === 'TokenExpiredError' ? 'Token expired' : 'Invalid token' }); }\n}\n\napp.get('/profile', verifyToken, (req, res) => {\n  const u = users.find((x) => x.id === req.auth.sub);\n  res.json({ id: u.id, name: u.name, email: u.email });\n});\n\napp.listen(3000, () => console.log('http://localhost:3000'));\n", "nodejs-backend/09-authentication/06-protected-api-routes.js": "/**\n * Experiment: Create Protected API Routes using authentication (JWT).\n * Run  : node 06-protected-api-routes.js\n * Demo user is pre-created: email demo@x.com / password demo1234\n */\nconst express = require('express');\nconst jwt = require('jsonwebtoken');\nconst bcrypt = require('bcryptjs');\nconst SECRET = 'protected-routes-secret';\n\nconst app = express();\napp.use(express.json());\nconst users = [{ id: 1, email: 'demo@x.com', password: bcrypt.hashSync('demo1234', 10) }];\nconst notes = [{ id: 1, ownerId: 1, text: 'my private note' }, { id: 2, ownerId: 2, text: \"someone else's note\" }];\n\nconst auth = (req, res, next) => {\n  const token = (req.headers.authorization || '').split(' ')[1];\n  if (!token) return res.status(401).json({ error: 'Token required' });\n  try { req.user = jwt.verify(token, SECRET); next(); } catch { res.status(401).json({ error: 'Invalid or expired token' }); }\n};\n\napp.get('/api/public', (req, res) => res.json({ message: 'Open to everyone' }));\n\napp.post('/api/login', (req, res) => {\n  const u = users.find((x) => x.email === req.body.email);\n  if (!u || !bcrypt.compareSync(req.body.password || '', u.password)) return res.status(401).json({ error: 'Bad credentials' });\n  res.json({ token: jwt.sign({ id: u.id, email: u.email }, SECRET, { expiresIn: '30m' }) });\n});\n\n// everything below this line requires a valid token\napp.use('/api/private', auth);\napp.get('/api/private/me', (req, res) => res.json(req.user));\napp.get('/api/private/notes', (req, res) => res.json(notes.filter((n) => n.ownerId === req.user.id)));\n\napp.listen(3000, () => console.log('http://localhost:3000'));\n", "nodejs-backend/10-authorization/01-role-based-authorization.js": "/**\n * Experiment: Implement Role-Based Authorization (RBAC).\n * Run  : node 01-role-based-authorization.js\n * Login as admin@x.com/admin123 or user@x.com/user1234 via POST /login, then call the routes with the token.\n */\nconst express = require('express');\nconst jwt = require('jsonwebtoken');\nconst bcrypt = require('bcryptjs');\nconst SECRET = 'rbac-secret';\n\nconst app = express();\napp.use(express.json());\n\nconst users = [\n  { id: 1, email: 'admin@x.com', password: bcrypt.hashSync('admin123', 8), role: 'admin' },\n  { id: 2, email: 'user@x.com', password: bcrypt.hashSync('user1234', 8), role: 'user' },\n];\n\n// role -> permissions map\nconst permissions = { admin: ['read', 'create', 'update', 'delete'], user: ['read'] };\n\napp.post('/login', (req, res) => {\n  const u = users.find((x) => x.email === req.body.email);\n  if (!u || !bcrypt.compareSync(req.body.password || '', u.password)) return res.status(401).json({ error: 'Bad credentials' });\n  res.json({ token: jwt.sign({ id: u.id, role: u.role }, SECRET, { expiresIn: '1h' }) });\n});\n\nconst authenticate = (req, res, next) => {\n  try { req.user = jwt.verify((req.headers.authorization || '').split(' ')[1], SECRET); next(); }\n  catch { res.status(401).json({ error: 'Login required' }); }\n};\nconst can = (action) => (req, res, next) =>\n  permissions[req.user.role].includes(action) ? next() : res.status(403).json({ error: `Role \"${req.user.role}\" cannot ${action}` });\n\nconst docs = [{ id: 1, title: 'Report' }];\napp.get('/docs', authenticate, can('read'), (req, res) => res.json(docs));\napp.post('/docs', authenticate, can('create'), (req, res) => { docs.push({ id: docs.length + 1, ...req.body }); res.status(201).json(docs); });\napp.delete('/docs/:id', authenticate, can('delete'), (req, res) => res.status(204).end());\n\napp.listen(3000, () => console.log('http://localhost:3000'));\n", "nodejs-backend/10-authorization/02-admin-and-user-roles.js": "/**\n * Experiment: Create Admin and User roles.\n * Run  : node 02-admin-and-user-roles.js\n * Registration always creates a normal \"user\". Only an admin can promote another account to admin.\n * Seed admin -> admin@x.com / admin123\n */\nconst express = require('express');\nconst jwt = require('jsonwebtoken');\nconst bcrypt = require('bcryptjs');\nconst SECRET = 'roles-secret';\nconst ROLES = ['user', 'admin'];\n\nconst app = express();\napp.use(express.json());\nconst users = [{ id: 1, email: 'admin@x.com', password: bcrypt.hashSync('admin123', 8), role: 'admin' }];\n\nconst auth = (req, res, next) => {\n  try { req.user = jwt.verify((req.headers.authorization || '').split(' ')[1], SECRET); next(); }\n  catch { res.status(401).json({ error: 'Login required' }); }\n};\nconst onlyAdmin = (req, res, next) => (req.user.role === 'admin' ? next() : res.status(403).json({ error: 'Admins only' }));\n\napp.post('/register', (req, res) => {\n  const { email, password } = req.body;\n  if (users.find((u) => u.email === email)) return res.status(409).json({ error: 'Email exists' });\n  users.push({ id: users.length + 1, email, password: bcrypt.hashSync(password, 8), role: 'user' });   // role is NOT taken from the body\n  res.status(201).json({ message: 'Registered as user' });\n});\n\napp.post('/login', (req, res) => {\n  const u = users.find((x) => x.email === req.body.email);\n  if (!u || !bcrypt.compareSync(req.body.password || '', u.password)) return res.status(401).json({ error: 'Bad credentials' });\n  res.json({ token: jwt.sign({ id: u.id, role: u.role }, SECRET, { expiresIn: '1h' }), role: u.role });\n});\n\napp.get('/me', auth, (req, res) => res.json(req.user));\napp.get('/admin/users', auth, onlyAdmin, (req, res) => res.json(users.map(({ password, ...u }) => u)));\napp.patch('/admin/users/:id/role', auth, onlyAdmin, (req, res) => {\n  const u = users.find((x) => x.id === +req.params.id);\n  if (!u) return res.status(404).json({ error: 'User not found' });\n  if (!ROLES.includes(req.body.role)) return res.status(400).json({ error: `role must be one of ${ROLES}` });\n  u.role = req.body.role;\n  res.json({ id: u.id, email: u.email, role: u.role });\n});\n\napp.listen(3000, () => console.log('http://localhost:3000'));\n", "nodejs-backend/10-authorization/03-restrict-routes-by-role.js": "/**\n * Experiment: Restrict specific routes based on user roles.\n * Run  : node 03-restrict-routes-by-role.js\n * Quick demo without login: send header  x-demo-role: admin | manager | user\n *   curl localhost:3000/reports -H \"x-demo-role: manager\"\n */\nconst express = require('express');\nconst app = express();\n\n// demo authentication: trust a header (real apps decode a JWT / session)\napp.use((req, res, next) => { req.user = { role: req.headers['x-demo-role'] || 'guest' }; next(); });\n\nconst allowRoles = (...roles) => (req, res, next) =>\n  roles.includes(req.user.role) ? next() : res.status(403).json({ error: `Requires role: ${roles.join(' or ')}`, yourRole: req.user.role });\n\napp.get('/', (req, res) => res.send('Open to everyone'));\napp.get('/profile', allowRoles('user', 'manager', 'admin'), (req, res) => res.json({ page: 'profile' }));\napp.get('/reports', allowRoles('manager', 'admin'), (req, res) => res.json({ page: 'reports' }));\napp.delete('/users/:id', allowRoles('admin'), (req, res) => res.json({ deleted: req.params.id }));\napp.get('/admin', allowRoles('admin'), (req, res) => res.send('Admin dashboard'));\n\napp.listen(3000, () => console.log('http://localhost:3000'));\n", "nodejs-backend/10-authorization/04-authorization-middleware.js": "/**\n * Experiment: Implement Authorization Middleware (roles + ownership).\n * Run  : node 04-authorization-middleware.js\n * Demo : send header  x-user-id: 1 | 2   and  x-role: user | admin\n *        curl -X PUT localhost:3000/posts/1 -H \"x-user-id: 2\" -H \"x-role: user\" -H \"Content-Type: application/json\" -d '{\"title\":\"x\"}'  -> 403\n */\nconst express = require('express');\nconst app = express();\napp.use(express.json());\n\napp.use((req, res, next) => {                                   // stand-in for real authentication\n  req.user = { id: Number(req.headers['x-user-id']) || 0, role: req.headers['x-role'] || 'user' };\n  next();\n});\n\nconst posts = [{ id: 1, ownerId: 1, title: 'Post by user 1' }, { id: 2, ownerId: 2, title: 'Post by user 2' }];\n\n// authorization middleware: admin OR owner of the resource\nfunction authorizeOwnerOrAdmin(getOwnerId) {\n  return (req, res, next) => {\n    const ownerId = getOwnerId(req);\n    if (ownerId === undefined) return res.status(404).json({ error: 'Resource not found' });\n    if (req.user.role === 'admin' || req.user.id === ownerId) return next();\n    res.status(403).json({ error: 'You are not allowed to modify this resource' });\n  };\n}\nconst postOwner = (req) => posts.find((p) => p.id === +req.params.id)?.ownerId;\n\napp.get('/posts', (req, res) => res.json(posts));\napp.put('/posts/:id', authorizeOwnerOrAdmin(postOwner), (req, res) => {\n  const post = posts.find((p) => p.id === +req.params.id);\n  post.title = req.body.title ?? post.title;\n  res.json(post);\n});\napp.delete('/posts/:id', authorizeOwnerOrAdmin(postOwner), (req, res) => {\n  posts.splice(posts.findIndex((p) => p.id === +req.params.id), 1);\n  res.status(204).end();\n});\n\napp.listen(3000, () => console.log('http://localhost:3000'));\n", "nodejs-backend/11-restful-api-development/01-basic-restful-api.js": "/**\n * Experiment: Create a basic RESTful API using Express.js.\n * REST rules: resources as nouns (/books), HTTP verbs as actions, JSON in/out, proper status codes.\n * Run: node 01-basic-restful-api.js\n *   GET /api/books | GET /api/books/1 | POST /api/books | PUT /api/books/1 | DELETE /api/books/1\n */\nconst express = require('express');\nconst app = express();\napp.use(express.json());\n\nlet books = [{ id: 1, title: 'Node.js Basics', author: 'A. Author' }];\n\napp.get('/api/books', (req, res) => res.json(books));\napp.get('/api/books/:id', (req, res) => {\n  const b = books.find((x) => x.id === +req.params.id);\n  b ? res.json(b) : res.status(404).json({ error: 'Book not found' });\n});\napp.post('/api/books', (req, res) => {\n  const b = { id: books.length ? books[books.length - 1].id + 1 : 1, ...req.body };\n  books.push(b);\n  res.status(201).location(`/api/books/${b.id}`).json(b);\n});\napp.put('/api/books/:id', (req, res) => {\n  const i = books.findIndex((x) => x.id === +req.params.id);\n  if (i < 0) return res.status(404).json({ error: 'Book not found' });\n  books[i] = { id: books[i].id, ...req.body };\n  res.json(books[i]);\n});\napp.delete('/api/books/:id', (req, res) => {\n  books = books.filter((x) => x.id !== +req.params.id);\n  res.status(204).end();\n});\n\napp.listen(3000, () => console.log('REST API on http://localhost:3000/api/books'));\n", "nodejs-backend/11-restful-api-development/02-get-rest-api.js": "/**\n * Experiment: Implement a GET REST API (collection, single item, filter, sort, paginate).\n * Run  : node 02-get-rest-api.js\n * Test : /api/products\n *        /api/products?category=phone&sort=price&order=desc\n *        /api/products?minPrice=500&page=1&limit=2\n *        /api/products/3\n */\nconst express = require('express');\nconst app = express();\n\nconst products = [\n  { id: 1, name: 'Laptop', category: 'computer', price: 55000 },\n  { id: 2, name: 'Phone', category: 'phone', price: 20000 },\n  { id: 3, name: 'Tablet', category: 'phone', price: 15000 },\n  { id: 4, name: 'Mouse', category: 'computer', price: 500 },\n  { id: 5, name: 'Keyboard', category: 'computer', price: 1200 },\n];\n\napp.get('/api/products', (req, res) => {\n  const { category, minPrice, sort = 'id', order = 'asc', page = 1, limit = 10 } = req.query;\n  let list = [...products];\n  if (category) list = list.filter((p) => p.category === category);\n  if (minPrice) list = list.filter((p) => p.price >= +minPrice);\n  list.sort((a, b) => (a[sort] > b[sort] ? 1 : -1) * (order === 'desc' ? -1 : 1));\n  const start = (page - 1) * limit;\n  res.json({ total: list.length, page: +page, limit: +limit, data: list.slice(start, start + +limit) });\n});\n\napp.get('/api/products/:id', (req, res) => {\n  const p = products.find((x) => x.id === +req.params.id);\n  p ? res.json(p) : res.status(404).json({ error: 'Product not found' });\n});\n\napp.listen(3000, () => console.log('http://localhost:3000/api/products'));\n", "nodejs-backend/11-restful-api-development/03-post-rest-api.js": "/**\n * Experiment: Implement a POST REST API.\n * Run  : node 03-post-rest-api.js\n * Test : curl -i -X POST localhost:3000/api/employees -H \"Content-Type: application/json\" \\\n *          -d '{\"name\":\"Asha\",\"department\":\"IT\",\"salary\":40000}'\n * Returns 201 Created + Location header; 400 for bad body; 409 for duplicates.\n */\nconst express = require('express');\nconst app = express();\napp.use(express.json());\nconst employees = [];\n\napp.post('/api/employees', (req, res) => {\n  const { name, department, salary } = req.body || {};\n  if (!name || !department || typeof salary !== 'number')\n    return res.status(400).json({ error: 'name, department (string) and salary (number) are required' });\n  if (employees.some((e) => e.name.toLowerCase() === name.toLowerCase()))\n    return res.status(409).json({ error: 'Employee already exists' });\n\n  const employee = { id: employees.length + 1, name, department, salary, createdAt: new Date().toISOString() };\n  employees.push(employee);\n  res.status(201).location(`/api/employees/${employee.id}`).json(employee);\n});\n\napp.get('/api/employees', (req, res) => res.json(employees));\n\napp.listen(3000, () => console.log('http://localhost:3000'));\n", "nodejs-backend/11-restful-api-development/04-put-patch-rest-api.js": "/**\n * Experiment: Implement a PUT / PATCH REST API.\n * PUT   = replace the WHOLE resource (all fields required)\n * PATCH = change only the fields that are sent\n * Run  : node 04-put-patch-rest-api.js\n * Test : curl -X PUT   localhost:3000/api/users/1 -H \"Content-Type: application/json\" -d '{\"name\":\"New\",\"email\":\"n@x.com\"}'\n *        curl -X PATCH localhost:3000/api/users/1 -H \"Content-Type: application/json\" -d '{\"email\":\"only@x.com\"}'\n */\nconst express = require('express');\nconst app = express();\napp.use(express.json());\nconst users = [{ id: 1, name: 'Asha', email: 'asha@x.com' }];\nconst find = (id) => users.find((u) => u.id === +id);\n\napp.get('/api/users/:id', (req, res) => (find(req.params.id) ? res.json(find(req.params.id)) : res.status(404).json({ error: 'Not found' })));\n\napp.put('/api/users/:id', (req, res) => {\n  const u = find(req.params.id);\n  if (!u) return res.status(404).json({ error: 'Not found' });\n  if (!req.body.name || !req.body.email) return res.status(400).json({ error: 'PUT needs name and email' });\n  u.name = req.body.name; u.email = req.body.email;\n  res.json(u);\n});\n\napp.patch('/api/users/:id', (req, res) => {\n  const u = find(req.params.id);\n  if (!u) return res.status(404).json({ error: 'Not found' });\n  const allowed = ['name', 'email'];\n  Object.keys(req.body).filter((k) => allowed.includes(k)).forEach((k) => (u[k] = req.body[k]));\n  res.json(u);\n});\n\napp.listen(3000, () => console.log('http://localhost:3000'));\n", "nodejs-backend/11-restful-api-development/05-delete-rest-api.js": "/**\n * Experiment: Implement a DELETE REST API.\n * Run  : node 05-delete-rest-api.js\n * Test : curl -i -X DELETE localhost:3000/api/tasks/1     -> 204 No Content\n *        curl -i -X DELETE localhost:3000/api/tasks/99    -> 404\n *        curl -X DELETE \"localhost:3000/api/tasks?done=true\"  -> bulk delete\n */\nconst express = require('express');\nconst app = express();\n\nlet tasks = [\n  { id: 1, title: 'Learn Node', done: true },\n  { id: 2, title: 'Learn Express', done: false },\n  { id: 3, title: 'Build API', done: true },\n];\n\napp.delete('/api/tasks/:id', (req, res) => {\n  const index = tasks.findIndex((t) => t.id === +req.params.id);\n  if (index === -1) return res.status(404).json({ error: 'Task not found' });\n  tasks.splice(index, 1);\n  res.status(204).end();\n});\n\napp.delete('/api/tasks', (req, res) => {\n  const before = tasks.length;\n  tasks = tasks.filter((t) => String(t.done) !== req.query.done);\n  res.json({ deleted: before - tasks.length });\n});\n\napp.get('/api/tasks', (req, res) => res.json(tasks));\n\napp.listen(3000, () => console.log('http://localhost:3000'));\n", "nodejs-backend/11-restful-api-development/06-http-status-codes.js": "/**\n * Experiment: Implement proper HTTP Status Codes in REST APIs.\n * Run: node 06-http-status-codes.js  -> call each route and read the status line (curl -i)\n */\nconst express = require('express');\nconst app = express();\napp.use(express.json());\n\napp.get('/ok', (req, res) => res.status(200).json({ msg: '200 OK - request succeeded' }));\napp.post('/created', (req, res) => res.status(201).json({ msg: '201 Created - new resource made' }));\napp.delete('/no-content', (req, res) => res.status(204).end());                       // 204 No Content\napp.get('/moved', (req, res) => res.redirect(301, '/ok'));                            // 301 Moved Permanently\napp.get('/bad-request', (req, res) => res.status(400).json({ msg: '400 Bad Request - client sent invalid data' }));\napp.get('/unauthorized', (req, res) => res.status(401).json({ msg: '401 Unauthorized - login required' }));\napp.get('/forbidden', (req, res) => res.status(403).json({ msg: '403 Forbidden - logged in but not allowed' }));\napp.get('/missing', (req, res) => res.status(404).json({ msg: '404 Not Found' }));\napp.post('/conflict', (req, res) => res.status(409).json({ msg: '409 Conflict - duplicate resource' }));\napp.post('/invalid', (req, res) => res.status(422).json({ msg: '422 Unprocessable Entity - validation failed' }));\napp.get('/rate-limited', (req, res) => res.status(429).json({ msg: '429 Too Many Requests' }));\napp.get('/server-error', (req, res) => res.status(500).json({ msg: '500 Internal Server Error' }));\napp.get('/unavailable', (req, res) => res.status(503).json({ msg: '503 Service Unavailable' }));\n\napp.listen(3000, () => console.log('http://localhost:3000'));\n", "nodejs-backend/11-restful-api-development/07-api-error-handling.js": "/**\n * Experiment: Implement API Error Handling (consistent JSON error format).\n * Run: node 07-api-error-handling.js -> /api/items/abc (400) , /api/items/99 (404) , /api/boom (500) , /nowhere (404)\n */\nconst express = require('express');\nconst app = express();\napp.use(express.json());\n\nclass ApiError extends Error {\n  constructor(status, message, details) { super(message); this.status = status; this.details = details; }\n}\nconst asyncHandler = (fn) => (req, res, next) => Promise.resolve(fn(req, res, next)).catch(next);   // forwards async errors\n\nconst items = [{ id: 1, name: 'Pen' }];\n\napp.get('/api/items/:id', asyncHandler(async (req, res) => {\n  if (!/^\\d+$/.test(req.params.id)) throw new ApiError(400, 'id must be a number');\n  const item = items.find((i) => i.id === +req.params.id);\n  if (!item) throw new ApiError(404, `Item ${req.params.id} not found`);\n  res.json(item);\n}));\napp.get('/api/boom', asyncHandler(async () => { throw new Error('database exploded'); }));\n\napp.use((req, res, next) => next(new ApiError(404, `Route ${req.method} ${req.originalUrl} not found`)));\n\napp.use((err, req, res, next) => {\n  if (err instanceof SyntaxError && err.status === 400) err = new ApiError(400, 'Malformed JSON body');\n  const status = err.status || 500;\n  if (status === 500) console.error(err);                                         // log internals, hide from client\n  res.status(status).json({\n    success: false,\n    error: { status, message: status === 500 ? 'Internal Server Error' : err.message, details: err.details },\n  });\n});\n\napp.listen(3000, () => console.log('http://localhost:3000'));\n", "nodejs-backend/11-restful-api-development/08-api-request-validation.js": "/**\n * Experiment: Implement API Request Validation (schema-driven, no external lib).\n * Run  : node 08-api-request-validation.js\n * Test : curl -X POST localhost:3000/api/orders -H \"Content-Type: application/json\" -d '{\"customer\":\"A\",\"quantity\":0,\"email\":\"bad\"}'\n */\nconst express = require('express');\nconst app = express();\napp.use(express.json());\n\nconst schema = {\n  customer: { type: 'string', required: true, min: 2, max: 50 },\n  email:    { type: 'string', required: true, pattern: /^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/, message: 'must be a valid email' },\n  quantity: { type: 'number', required: true, integer: true, min: 1, max: 100 },\n  note:     { type: 'string', required: false, max: 200 },\n};\n\nfunction validateBody(schema) {\n  return (req, res, next) => {\n    const errors = [];\n    for (const [field, r] of Object.entries(schema)) {\n      const v = req.body[field];\n      if (v === undefined || v === null || v === '') { if (r.required) errors.push({ field, message: 'is required' }); continue; }\n      if (typeof v !== r.type) { errors.push({ field, message: `must be of type ${r.type}` }); continue; }\n      if (r.integer && !Number.isInteger(v)) errors.push({ field, message: 'must be an integer' });\n      if (r.min !== undefined && (r.type === 'string' ? v.length : v) < r.min) errors.push({ field, message: `must be >= ${r.min}` });\n      if (r.max !== undefined && (r.type === 'string' ? v.length : v) > r.max) errors.push({ field, message: `must be <= ${r.max}` });\n      if (r.pattern && !r.pattern.test(v)) errors.push({ field, message: r.message || 'has an invalid format' });\n    }\n    if (errors.length) return res.status(422).json({ success: false, errors });\n    next();\n  };\n}\n\napp.post('/api/orders', validateBody(schema), (req, res) => res.status(201).json({ success: true, order: req.body }));\n\napp.listen(3000, () => console.log('http://localhost:3000'));\n", "nodejs-backend/11-restful-api-development/09-postman-collection.json": "{\n  \"info\": {\n    \"name\": \"Todo REST API - Node Lab\",\n    \"schema\": \"https://schema.getpostman.com/json/collection/v2.1.0/collection.json\"\n  },\n  \"variable\": [{ \"key\": \"baseUrl\", \"value\": \"http://localhost:3000\" }],\n  \"item\": [\n    {\n      \"name\": \"GET all todos\",\n      \"request\": { \"method\": \"GET\", \"url\": \"{{baseUrl}}/api/todos\" },\n      \"event\": [{ \"listen\": \"test\", \"script\": { \"type\": \"text/javascript\", \"exec\": [\n        \"pm.test('Status is 200', () => pm.response.to.have.status(200));\",\n        \"pm.test('Body is an array', () => pm.expect(pm.response.json()).to.be.an('array'));\"\n      ] } }]\n    },\n    {\n      \"name\": \"POST create todo\",\n      \"request\": {\n        \"method\": \"POST\",\n        \"header\": [{ \"key\": \"Content-Type\", \"value\": \"application/json\" }],\n        \"body\": { \"mode\": \"raw\", \"raw\": \"{\\\"title\\\":\\\"Write tests\\\"}\" },\n        \"url\": \"{{baseUrl}}/api/todos\"\n      },\n      \"event\": [{ \"listen\": \"test\", \"script\": { \"type\": \"text/javascript\", \"exec\": [\n        \"pm.test('Status is 201', () => pm.response.to.have.status(201));\",\n        \"pm.collectionVariables.set('todoId', pm.response.json().id);\"\n      ] } }]\n    },\n    {\n      \"name\": \"GET todo by id\",\n      \"request\": { \"method\": \"GET\", \"url\": \"{{baseUrl}}/api/todos/{{todoId}}\" },\n      \"event\": [{ \"listen\": \"test\", \"script\": { \"type\": \"text/javascript\", \"exec\": [\n        \"pm.test('Status is 200', () => pm.response.to.have.status(200));\"\n      ] } }]\n    },\n    {\n      \"name\": \"PUT update todo\",\n      \"request\": {\n        \"method\": \"PUT\",\n        \"header\": [{ \"key\": \"Content-Type\", \"value\": \"application/json\" }],\n        \"body\": { \"mode\": \"raw\", \"raw\": \"{\\\"title\\\":\\\"Write more tests\\\",\\\"completed\\\":true}\" },\n        \"url\": \"{{baseUrl}}/api/todos/{{todoId}}\"\n      },\n      \"event\": [{ \"listen\": \"test\", \"script\": { \"type\": \"text/javascript\", \"exec\": [\n        \"pm.test('Completed is true', () => pm.expect(pm.response.json().completed).to.eql(true));\"\n      ] } }]\n    },\n    {\n      \"name\": \"DELETE todo\",\n      \"request\": { \"method\": \"DELETE\", \"url\": \"{{baseUrl}}/api/todos/{{todoId}}\" },\n      \"event\": [{ \"listen\": \"test\", \"script\": { \"type\": \"text/javascript\", \"exec\": [\n        \"pm.test('Status is 204', () => pm.response.to.have.status(204));\"\n      ] } }]\n    },\n    {\n      \"name\": \"POST invalid todo (400)\",\n      \"request\": {\n        \"method\": \"POST\",\n        \"header\": [{ \"key\": \"Content-Type\", \"value\": \"application/json\" }],\n        \"body\": { \"mode\": \"raw\", \"raw\": \"{}\" },\n        \"url\": \"{{baseUrl}}/api/todos\"\n      },\n      \"event\": [{ \"listen\": \"test\", \"script\": { \"type\": \"text/javascript\", \"exec\": [\n        \"pm.test('Status is 400', () => pm.response.to.have.status(400));\"\n      ] } }]\n    }\n  ]\n}\n", "nodejs-backend/11-restful-api-development/09-test-rest-api-postman.js": "/**\n * Experiment: Test REST APIs using Postman.\n * 1. Run this server:            node 09-test-rest-api-postman.js\n * 2. Postman -> Import -> select  09-postman-collection.json\n * 3. Run each request, or use Collection Runner (has test scripts that check status codes/body).\n * Manual steps: New Request -> choose method -> URL -> Body > raw > JSON -> Send -> inspect Status/Time/Body.\n */\nconst express = require('express');\nconst app = express();\napp.use(express.json());\n\nlet todos = [{ id: 1, title: 'Learn Postman', completed: false }];\nlet nextId = 2;\n\napp.get('/api/todos', (req, res) => res.json(todos));\napp.get('/api/todos/:id', (req, res) => {\n  const t = todos.find((x) => x.id === +req.params.id);\n  t ? res.json(t) : res.status(404).json({ error: 'Todo not found' });\n});\napp.post('/api/todos', (req, res) => {\n  if (!req.body.title) return res.status(400).json({ error: 'title is required' });\n  const t = { id: nextId++, title: req.body.title, completed: false };\n  todos.push(t);\n  res.status(201).json(t);\n});\napp.put('/api/todos/:id', (req, res) => {\n  const t = todos.find((x) => x.id === +req.params.id);\n  if (!t) return res.status(404).json({ error: 'Todo not found' });\n  Object.assign(t, { title: req.body.title ?? t.title, completed: req.body.completed ?? t.completed });\n  res.json(t);\n});\napp.delete('/api/todos/:id', (req, res) => {\n  todos = todos.filter((x) => x.id !== +req.params.id);\n  res.status(204).end();\n});\n\napp.listen(3000, () => console.log('http://localhost:3000/api/todos'));\n", "nodejs-backend/11-restful-api-development/10-student-rest-api-with-database.js": "/**\n * Experiment: Create a Student REST API with Database Connectivity.\n * Run  : node 10-student-rest-api-with-database.js\n * Routes: GET/POST /api/students , GET/PUT/PATCH/DELETE /api/students/:id , GET /api/students?course=CSE&search=as&page=1&limit=5\n */\nconst express = require('express');\nconst db = require('../common/db');\n\nconst app = express();\napp.use(express.json());\n\nconst isEmail = (s) => /^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/.test(s);\n\napp.get('/api/students', (req, res) => {\n  const { course, search = '', page = 1, limit = 10 } = req.query;\n  const where = ['name LIKE ?']; const params = [`%${search}%`];\n  if (course) { where.push('course = ?'); params.push(course); }\n  const total = db.prepare(`SELECT COUNT(*) c FROM students WHERE ${where.join(' AND ')}`).get(...params).c;\n  const data = db.prepare(`SELECT * FROM students WHERE ${where.join(' AND ')} ORDER BY id LIMIT ? OFFSET ?`)\n    .all(...params, +limit, (+page - 1) * +limit);\n  res.json({ total, page: +page, limit: +limit, data });\n});\n\napp.get('/api/students/:id', (req, res) => {\n  const s = db.prepare('SELECT * FROM students WHERE id = ?').get(req.params.id);\n  s ? res.json(s) : res.status(404).json({ error: 'Student not found' });\n});\n\napp.post('/api/students', (req, res) => {\n  const { name, email, course, age } = req.body;\n  if (!name || !isEmail(email) || !course || !Number.isInteger(age) || age < 15)\n    return res.status(400).json({ error: 'name, valid email, course and integer age (>=15) are required' });\n  try {\n    const info = db.prepare('INSERT INTO students (name,email,course,age) VALUES (?,?,?,?)').run(name, email, course, age);\n    res.status(201).json(db.prepare('SELECT * FROM students WHERE id=?').get(info.lastInsertRowid));\n  } catch (e) {\n    res.status(e.code === 'SQLITE_CONSTRAINT_UNIQUE' ? 409 : 500).json({ error: e.message });\n  }\n});\n\nconst update = (req, res) => {\n  const cur = db.prepare('SELECT * FROM students WHERE id = ?').get(req.params.id);\n  if (!cur) return res.status(404).json({ error: 'Student not found' });\n  const m = { ...cur, ...req.body, id: cur.id };\n  if (req.method === 'PUT' && ['name', 'email', 'course', 'age'].some((k) => req.body[k] === undefined))\n    return res.status(400).json({ error: 'PUT requires name, email, course and age' });\n  db.prepare('UPDATE students SET name=?, email=?, course=?, age=? WHERE id=?').run(m.name, m.email, m.course, m.age, cur.id);\n  res.json(db.prepare('SELECT * FROM students WHERE id=?').get(cur.id));\n};\napp.put('/api/students/:id', update);\napp.patch('/api/students/:id', update);\n\napp.delete('/api/students/:id', (req, res) => {\n  const info = db.prepare('DELETE FROM students WHERE id = ?').run(req.params.id);\n  info.changes ? res.status(204).end() : res.status(404).json({ error: 'Student not found' });\n});\n\napp.listen(3000, () => console.log('http://localhost:3000/api/students'));\n", "nodejs-backend/11-restful-api-development/11-product-rest-api-crud.js": "/**\n * Experiment: Create a Product REST API with CRUD Operations (SQLite, validation, stock adjustment).\n * Run: node 11-product-rest-api-crud.js\n * Extra endpoint: POST /api/products/:id/stock  {\"change\": -3}   (adjust stock, never below 0)\n */\nconst express = require('express');\nconst db = require('../common/db');\nconst crud = require('../common/crudFactory');\nconst v = require('../common/validators');\n\nconst app = express();\napp.use(express.json());\n\napp.post('/api/products/:id/stock', (req, res, next) => {\n  const p = db.prepare('SELECT * FROM products WHERE id = ?').get(req.params.id);\n  if (!p) return res.status(404).json({ error: 'Product not found' });\n  if (!Number.isInteger(req.body.change)) return res.status(400).json({ error: 'change must be an integer' });\n  if (p.stock + req.body.change < 0) return res.status(409).json({ error: 'Insufficient stock' });\n  db.prepare('UPDATE products SET stock = stock + ? WHERE id = ?').run(req.body.change, p.id);\n  res.json(db.prepare('SELECT * FROM products WHERE id = ?').get(p.id));\n});\n\napp.use('/api/products', crud(db, 'products', { name: v.string(2), price: v.number(0), stock: v.int(0) }));\n\napp.listen(3000, () => console.log('http://localhost:3000/api/products'));\n", "nodejs-backend/11-restful-api-development/12-user-rest-api-auth-and-authorization.js": "/**\n * Experiment: Create a User REST API with Authentication and Authorization.\n * Run  : node 12-user-rest-api-auth-and-authorization.js\n * Rules: - POST /api/auth/register , POST /api/auth/login  are public\n *        - GET /api/users            -> admin only\n *        - GET/PUT /api/users/:id    -> admin OR the user himself\n *        - DELETE /api/users/:id     -> admin only\n * First registered account becomes \"admin\" (demo convenience); the rest are \"user\".\n */\nconst express = require('express');\nconst bcrypt = require('bcryptjs');\nconst jwt = require('jsonwebtoken');\nconst db = require('../common/db');\n\nconst SECRET = process.env.JWT_SECRET || 'user-api-secret';\nconst app = express();\napp.use(express.json());\nconst safe = ({ password, ...u }) => u;\n\n// ---------- authentication ----------\napp.post('/api/auth/register', async (req, res) => {\n  const { name, email, password } = req.body;\n  if (!name || !email || !password || password.length < 8) return res.status(400).json({ error: 'name, email and password (min 8) required' });\n  if (db.prepare('SELECT 1 FROM users WHERE email = ?').get(email)) return res.status(409).json({ error: 'Email already registered' });\n  const first = db.prepare('SELECT COUNT(*) c FROM users').get().c === 0;\n  const info = db.prepare('INSERT INTO users (name,email,password,role) VALUES (?,?,?,?)')\n    .run(name, email, await bcrypt.hash(password, 10), first ? 'admin' : 'user');\n  res.status(201).json(safe(db.prepare('SELECT * FROM users WHERE id=?').get(info.lastInsertRowid)));\n});\n\napp.post('/api/auth/login', async (req, res) => {\n  const u = db.prepare('SELECT * FROM users WHERE email = ?').get(req.body.email || '');\n  if (!u || !(await bcrypt.compare(req.body.password || '', u.password))) return res.status(401).json({ error: 'Invalid credentials' });\n  res.json({ token: jwt.sign({ id: u.id, role: u.role }, SECRET, { expiresIn: '2h' }), user: safe(u) });\n});\n\n// ---------- middleware ----------\nconst authenticate = (req, res, next) => {\n  try { req.user = jwt.verify((req.headers.authorization || '').split(' ')[1], SECRET); next(); }\n  catch { res.status(401).json({ error: 'Authentication required' }); }\n};\nconst adminOnly = (req, res, next) => (req.user.role === 'admin' ? next() : res.status(403).json({ error: 'Admin only' }));\nconst selfOrAdmin = (req, res, next) =>\n  req.user.role === 'admin' || req.user.id === +req.params.id ? next() : res.status(403).json({ error: 'You can only access your own account' });\n\n// ---------- protected resources ----------\napp.get('/api/users', authenticate, adminOnly, (req, res) => res.json(db.prepare('SELECT * FROM users').all().map(safe)));\n\napp.get('/api/users/:id', authenticate, selfOrAdmin, (req, res) => {\n  const u = db.prepare('SELECT * FROM users WHERE id = ?').get(req.params.id);\n  u ? res.json(safe(u)) : res.status(404).json({ error: 'User not found' });\n});\n\napp.put('/api/users/:id', authenticate, selfOrAdmin, (req, res) => {\n  const u = db.prepare('SELECT * FROM users WHERE id = ?').get(req.params.id);\n  if (!u) return res.status(404).json({ error: 'User not found' });\n  const role = req.user.role === 'admin' && req.body.role ? req.body.role : u.role;   // only admins may change roles\n  db.prepare('UPDATE users SET name=?, role=? WHERE id=?').run(req.body.name ?? u.name, role, u.id);\n  res.json(safe(db.prepare('SELECT * FROM users WHERE id=?').get(u.id)));\n});\n\napp.delete('/api/users/:id', authenticate, adminOnly, (req, res) => {\n  const info = db.prepare('DELETE FROM users WHERE id = ?').run(req.params.id);\n  info.changes ? res.status(204).end() : res.status(404).json({ error: 'User not found' });\n});\n\napp.listen(3000, () => console.log('http://localhost:3000'));\n", "nodejs-backend/common/crudFactory.js": "// Builds an Express Router with full CRUD + validation for any SQLite table.\nconst express = require('express');\n\n/**\n * @param {object} db      better-sqlite3 database\n * @param {string} table   table name\n * @param {object} fields  { column: (value) => errorMessage | null }  (validators)\n * @param {string[]} hide  columns never returned (e.g. password)\n */\nmodule.exports = function crudFactory(db, table, fields, hide = []) {\n  const router = express.Router();\n  const cols = Object.keys(fields);\n  const strip = (row) => { if (row) hide.forEach((h) => delete row[h]); return row; };\n\n  const validate = (body, partial = false) => {\n    const errors = [];\n    for (const c of cols) {\n      if (body[c] === undefined) { if (!partial) errors.push(`${c} is required`); continue; }\n      const msg = fields[c](body[c]);\n      if (msg) errors.push(`${c}: ${msg}`);\n    }\n    return errors;\n  };\n\n  router.get('/', (req, res) => {\n    res.json(db.prepare(`SELECT * FROM ${table}`).all().map(strip));\n  });\n\n  router.get('/:id', (req, res) => {\n    const row = db.prepare(`SELECT * FROM ${table} WHERE id = ?`).get(req.params.id);\n    if (!row) return res.status(404).json({ error: `${table} record not found` });\n    res.json(strip(row));\n  });\n\n  router.post('/', (req, res) => {\n    const errors = validate(req.body);\n    if (errors.length) return res.status(400).json({ errors });\n    try {\n      const info = db.prepare(\n        `INSERT INTO ${table} (${cols.join(',')}) VALUES (${cols.map(() => '?').join(',')})`\n      ).run(...cols.map((c) => req.body[c]));\n      res.status(201).json(strip(db.prepare(`SELECT * FROM ${table} WHERE id=?`).get(info.lastInsertRowid)));\n    } catch (e) { res.status(409).json({ error: e.message }); }\n  });\n\n  router.put('/:id', (req, res) => {\n    const errors = validate(req.body, true);\n    if (errors.length) return res.status(400).json({ errors });\n    const keys = cols.filter((c) => req.body[c] !== undefined);\n    if (!keys.length) return res.status(400).json({ error: 'nothing to update' });\n    try {\n      const info = db.prepare(`UPDATE ${table} SET ${keys.map((k) => `${k}=?`).join(',')} WHERE id=?`)\n        .run(...keys.map((k) => req.body[k]), req.params.id);\n      if (!info.changes) return res.status(404).json({ error: 'record not found' });\n      res.json(strip(db.prepare(`SELECT * FROM ${table} WHERE id=?`).get(req.params.id)));\n    } catch (e) { res.status(409).json({ error: e.message }); }\n  });\n\n  router.delete('/:id', (req, res) => {\n    const info = db.prepare(`DELETE FROM ${table} WHERE id=?`).run(req.params.id);\n    if (!info.changes) return res.status(404).json({ error: 'record not found' });\n    res.status(204).end();\n  });\n\n  return router;\n};\n", "nodejs-backend/common/db.js": "// Shared SQLite connection (better-sqlite3) used by the DB / CRUD / REST experiments.\nconst Database = require('better-sqlite3');\nconst path = require('path');\nconst fs = require('fs');\n\nfs.mkdirSync(path.join(__dirname, '..', 'data'), { recursive: true });\nconst db = new Database(path.join(__dirname, '..', 'data', 'app.db'));\ndb.pragma('journal_mode = WAL');\n\ndb.exec(`\nCREATE TABLE IF NOT EXISTS students (\n  id INTEGER PRIMARY KEY AUTOINCREMENT,\n  name TEXT NOT NULL,\n  email TEXT NOT NULL UNIQUE,\n  course TEXT NOT NULL,\n  age INTEGER CHECK (age >= 15)\n);\nCREATE TABLE IF NOT EXISTS users (\n  id INTEGER PRIMARY KEY AUTOINCREMENT,\n  name TEXT NOT NULL,\n  email TEXT NOT NULL UNIQUE,\n  password TEXT,\n  role TEXT NOT NULL DEFAULT 'user'\n);\nCREATE TABLE IF NOT EXISTS products (\n  id INTEGER PRIMARY KEY AUTOINCREMENT,\n  name TEXT NOT NULL,\n  price REAL NOT NULL CHECK (price >= 0),\n  stock INTEGER NOT NULL DEFAULT 0\n);\n`);\n\nmodule.exports = db;\n", "nodejs-backend/common/validators.js": "// Small reusable validators for crudFactory (each returns an error message or null).\nexports.string = (min = 1) => (v) => (typeof v === 'string' && v.trim().length >= min ? null : `must be a string (min ${min} chars)`);\nexports.email = () => (v) => (typeof v === 'string' && /^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/.test(v) ? null : 'must be a valid email');\nexports.int = (min = 0) => (v) => (Number.isInteger(v) && v >= min ? null : `must be an integer >= ${min}`);\nexports.number = (min = 0) => (v) => (typeof v === 'number' && v >= min ? null : `must be a number >= ${min}`);\nexports.oneOf = (list) => (v) => (list.includes(v) ? null : `must be one of ${list.join(', ')}`);\n", "supabase/README.md": "# Supabase Lab\n\n## One-time setup\n1. Create a project at https://supabase.com (experiment 01-01 walks through it).\n2. `cp .env.example .env` and fill in the URL + keys (Project Settings > API).\n3. Run `00-setup/schema.sql` in the SQL Editor.\n4. `npm install`\n\n## Folder guide\n| Folder | Content |\n|---|---|\n| 01-supabase-fundamentals | Project, dashboard, keys, editors, connecting an app |\n| 02-database-and-tables | DDL, data types, keys, constraints, insert/select/update/delete (SQL) |\n| 03-sql-operations | WHERE, ORDER BY, GROUP BY, aggregates, joins, subqueries, views |\n| 04-supabase-crud | CRUD with `supabase-js` + Student / Product / User CRUD apps |\n| 05-authentication | Sign up, sign in, sign out, reset password, sessions, protected routes |\n| 06-authorization-and-security | RLS, policies, roles, RBAC |\n| 07-storage | Buckets, upload, download, public URLs, authenticated uploads |\n| 08-realtime | INSERT/UPDATE/DELETE listeners, chat app, realtime records |\n| 09-edge-functions | Deno/TypeScript functions (create, deploy, auth, DB) |\n| 10-rest-api | PostgREST endpoints (GET/POST/PATCH/DELETE, filters, pagination, Postman) |\n| 11-project-based-experiments | 7 complete mini-projects |\n\nRun any Node experiment with `node <folder>/<file>.js`.\n`.sql` files are pasted into the SQL Editor. `.html` files can be opened in a browser (fill in the two keys at the top of the file).\n", "supabase/package.json": "{\n  \"name\": \"supabase-lab\",\n  \"version\": \"1.0.0\",\n  \"private\": true,\n  \"description\": \"Supabase experiments (topic-wise)\",\n  \"dependencies\": {\n    \"@supabase/supabase-js\": \"^2.45.0\",\n    \"dotenv\": \"^16.4.5\",\n    \"express\": \"^4.19.2\",\n    \"ws\": \"^8.18.0\"\n  }\n}\n", "supabase/00-setup/schema.sql": "-- =====================================================================\n-- Run this ONCE in  Supabase Dashboard > SQL Editor > New query\n-- Creates the tables used by the experiments in this project.\n-- =====================================================================\n\n-- Students ------------------------------------------------------------\ncreate table if not exists public.students (\n  id          bigint generated always as identity primary key,\n  name        text        not null,\n  email       text        not null unique,\n  course      text        not null,\n  age         int         check (age >= 15),\n  user_id     uuid        default auth.uid() references auth.users(id) on delete set null,\n  created_at  timestamptz not null default now()\n);\n\n-- Products ------------------------------------------------------------\ncreate table if not exists public.products (\n  id          bigint generated always as identity primary key,\n  name        text          not null,\n  price       numeric(10,2) not null check (price >= 0),\n  stock       int           not null default 0 check (stock >= 0),\n  category    text,\n  created_by  uuid          default auth.uid() references auth.users(id) on delete set null,\n  created_at  timestamptz   not null default now()\n);\n\n-- Profiles (one row per auth user, holds the role) --------------------\ncreate table if not exists public.profiles (\n  id         uuid primary key references auth.users(id) on delete cascade,\n  full_name  text,\n  role       text not null default 'user' check (role in ('user','admin')),\n  created_at timestamptz not null default now()\n);\n\n-- auto-create a profile whenever a user signs up\ncreate or replace function public.handle_new_user()\nreturns trigger language plpgsql security definer set search_path = public as $$\nbegin\n  insert into public.profiles (id, full_name)\n  values (new.id, coalesce(new.raw_user_meta_data->>'full_name', split_part(new.email,'@',1)));\n  return new;\nend; $$;\n\ndrop trigger if exists on_auth_user_created on auth.users;\ncreate trigger on_auth_user_created after insert on auth.users\n  for each row execute function public.handle_new_user();\n\n-- Chat messages -------------------------------------------------------\ncreate table if not exists public.messages (\n  id          bigint generated always as identity primary key,\n  user_id     uuid default auth.uid() references auth.users(id) on delete set null,\n  username    text not null default 'anonymous',\n  content     text not null check (char_length(content) between 1 and 500),\n  created_at  timestamptz not null default now()\n);\n\n-- Temporary open access so experiments 01-05 work before RLS is taught.\n-- (Experiment folder 06 replaces these with proper policies.)\nalter table public.students enable row level security;\nalter table public.products enable row level security;\nalter table public.profiles enable row level security;\nalter table public.messages enable row level security;\n\ncreate policy \"lab: open read students\"   on public.students for select using (true);\ncreate policy \"lab: open write students\"  on public.students for all    using (true) with check (true);\ncreate policy \"lab: open read products\"   on public.products for select using (true);\ncreate policy \"lab: open write products\"  on public.products for all    using (true) with check (true);\ncreate policy \"lab: read profiles\"        on public.profiles for select using (true);\ncreate policy \"lab: open read messages\"   on public.messages for select using (true);\ncreate policy \"lab: open write messages\"  on public.messages for insert with check (true);\n", "supabase/01-supabase-fundamentals/01-create-supabase-project.md": "# Experiment: Create a Supabase project\n\n**Aim:** Create a new Supabase project (hosted PostgreSQL + Auth + Storage + Realtime + Edge Functions).\n\n## Steps\n1. Open <https://supabase.com> -> **Start your project** -> sign in with GitHub / email.\n2. Create an **Organization** (free plan) if asked.\n3. Click **New project** and fill in:\n   - **Name:** `student-lab`\n   - **Database password:** use *Generate a password*, then **copy and save it** (needed for direct DB connections; not shown again)\n   - **Region:** nearest to you (for India choose *Mumbai / ap-south-1*, if listed)\n   - **Pricing plan:** Free\n4. Click **Create new project** and wait 1-2 minutes until the status shows *Healthy*.\n5. Note the **Project URL** `https://<project-ref>.supabase.co` (Home page or Project Settings > API).\n\n## What you get\n| Service | Purpose |\n|---|---|\n| Postgres database | Relational data |\n| Auth (GoTrue) | Users, passwords, OAuth |\n| Storage | Files and images |\n| Realtime | Live database changes |\n| Edge Functions | Server-side TypeScript (Deno) |\n| Auto-generated REST API | PostgREST over your tables |\n\n## Result\nA running project with its own URL, API keys and empty `public` schema.\n", "supabase/01-supabase-fundamentals/02-explore-supabase-dashboard.md": "# Experiment: Explore the Supabase Dashboard\n\n**Aim:** Identify every major area of the dashboard and what it is used for.\n\n| Sidebar item | What to explore |\n|---|---|\n| **Project Overview / Home** | Project URL, API status, usage graphs |\n| **Table Editor** | Spreadsheet-like view: create tables, add/edit/delete rows, define relations |\n| **SQL Editor** | Run SQL, save snippets, use templates |\n| **Database** | Tables, Views, Functions, Triggers, Roles, Extensions, Replication, Backups, Migrations |\n| **Authentication** | Users list, Policies (RLS), Providers (email, Google...), URL Configuration, Email Templates |\n| **Storage** | Buckets, files, storage policies |\n| **Edge Functions** | Deployed functions, logs, secrets |\n| **Realtime** | Inspector, channel monitoring |\n| **Advisors** | Security and performance recommendations |\n| **Logs** | API, Postgres, Auth, Storage, Edge logs |\n| **API Docs** | Auto-generated documentation with ready-to-copy code for **each of your tables** |\n| **Project Settings** | General, Database (connection strings), API (URL + keys), Auth, Billing |\n\n## Activity\n1. Open **Authentication > Users** and add a test user (Add user > Create new user).\n2. Open **API Docs > Tables and Views**, pick a table, and copy the JavaScript and cURL snippets.\n3. Open **Logs > API** and find your own request.\n4. Open **Advisors > Security** and read any warnings (for example \"RLS disabled\").\n", "supabase/01-supabase-fundamentals/03-configure-project-settings-and-api-keys.md": "# Experiment: Configure project settings and API keys\n\n**Aim:** Find the project settings and understand the different API keys.\n\n## Where\n**Project Settings** (gear icon) > **General / API / Database / Authentication**.\n\n## Keys\n| Key | Use | Safe in browser? |\n|---|---|---|\n| **Project URL** | Base URL of every API call | Yes |\n| **anon (public / publishable) key** | Client-side calls; access is limited by **RLS policies** | Yes |\n| **service_role (secret) key** | Full access, **bypasses RLS** | **NEVER** - server only |\n| **JWT secret** | Signs/verifies tokens | Never |\n\n## Steps\n1. Project Settings > **API** -> copy **Project URL** and **anon key**.\n2. Reveal and copy the **service_role key** (store only in a server-side `.env`).\n3. Save them in `supabase/.env` (see `.env.example`); make sure `.env` is in `.gitignore`.\n4. Project Settings > **General**: rename the project, set the region info, pause/restore options.\n5. Project Settings > **Database**: view the connection string, reset the DB password, SSL settings.\n6. Project Settings > **Authentication** (or Authentication > URL Configuration): set **Site URL** (e.g. `http://localhost:3000`) and **Redirect URLs**.\n7. Authentication > Providers > **Email**: toggle *Confirm email* (turn OFF during lab testing so sign-ups work immediately).\n\n## Security rules\n- Never commit keys to Git; rotate a key if it leaks.\n- Use anon key + RLS in the frontend; use service_role only in trusted servers/Edge Functions.\n", "supabase/01-supabase-fundamentals/04-create-and-manage-postgresql-database.sql": "-- Experiment: Create and manage a PostgreSQL database using Supabase\n-- Run in: Dashboard > SQL Editor. (Every project already has one database called \"postgres\".)\n\n-- 1. Inspect the server and current database\nselect version();\nselect current_database(), current_user, now();\n\n-- 2. Schemas (logical folders inside the database)\ncreate schema if not exists lab;\nselect schema_name from information_schema.schemata order by 1;\n\n-- 3. Create a table in the new schema\ncreate table lab.courses (\n  id     serial primary key,\n  code   text unique not null,\n  title  text not null\n);\ninsert into lab.courses (code, title) values ('CS101', 'Intro to Programming'), ('CS201', 'Databases');\nselect * from lab.courses;\n\n-- 4. Manage: list tables and sizes\nselect table_schema, table_name\nfrom information_schema.tables\nwhere table_schema in ('public', 'lab') order by 1, 2;\n\nselect pg_size_pretty(pg_database_size(current_database())) as database_size;\n\n-- 5. Extensions\nselect extname, extversion from pg_extension;\ncreate extension if not exists pgcrypto;\n\n-- 6. Backups (dashboard: Database > Backups) - free plan keeps daily backups for a limited time\n\n-- 7. Clean up\ndrop table lab.courses;\ndrop schema lab;\n", "supabase/01-supabase-fundamentals/05-demonstrate-table-editor.md": "# Experiment: Demonstrate the Supabase Table Editor\n\n**Aim:** Create and manage tables without writing SQL.\n\n## Create a table\n1. Dashboard > **Table Editor** > **New table**.\n2. Name: `books`; keep **Enable Row Level Security (RLS)** ticked.\n3. Columns (use *Add column*):\n\n| Name | Type | Default | Options |\n|---|---|---|---|\n| `id` | `int8` | identity | Primary key |\n| `title` | `text` | - | uncheck *Allow nullable* |\n| `author` | `text` | - | |\n| `price` | `numeric` | `0` | |\n| `created_at` | `timestamptz` | `now()` | |\n\n4. Click **Save**.\n\n## Work with data\n- **Insert row:** *Insert > Insert row*, fill the form, Save.\n- **Import CSV:** *Insert > Import data from CSV*.\n- **Edit cell:** double-click a cell, type, press Enter.\n- **Filter / Sort:** toolbar *Filter* (e.g. `price > 100`) and *Sort*.\n- **Delete row:** tick the checkbox > *Delete rows*.\n- **Export:** *Export > Download as CSV*.\n\n## Relations\n1. In `books` add column `publisher_id` (`int8`).\n2. Click the link icon > **Add foreign key relation** -> choose `publishers.id`.\n\n## Other actions\n- **Edit table:** rename, add/remove columns, change types.\n- **View definition:** the *Definition* tab shows the generated SQL.\n- **Realtime:** toggle *Enable Realtime* on the table menu.\n\n## Result\nAnything done in the Table Editor is plain PostgreSQL - it is the same table you can query in the SQL Editor.\n", "supabase/01-supabase-fundamentals/06-demonstrate-sql-editor.sql": "-- Experiment: Demonstrate the Supabase SQL Editor\n-- Dashboard > SQL Editor > \"New query\". Run with the Run button or Ctrl/Cmd + Enter.\n-- Tips: select part of the script to run only that part; click \"Save\" to keep a snippet;\n--       \"Templates\" (Quickstarts) give ready-made schemas; results show as a grid and can be exported to CSV.\n\n-- 1. Create a table\ncreate table if not exists public.notes (\n  id bigint generated always as identity primary key,\n  title text not null,\n  body text,\n  created_at timestamptz default now()\n);\n\n-- 2. Insert rows\ninsert into public.notes (title, body) values\n  ('First note', 'Hello Supabase'),\n  ('Second note', 'SQL Editor is great');\n\n-- 3. Query\nselect * from public.notes order by id;\n\n-- 4. Update and delete\nupdate public.notes set body = 'Updated body' where title = 'First note';\ndelete from public.notes where title = 'Second note';\n\n-- 5. EXPLAIN shows how Postgres runs a query\nexplain analyze select * from public.notes where id = 1;\n\n-- 6. Useful catalog queries\nselect column_name, data_type, is_nullable\nfrom information_schema.columns\nwhere table_schema = 'public' and table_name = 'notes';\n\n-- 7. Clean up\ndrop table public.notes;\n", "supabase/01-supabase-fundamentals/07-connect-application-to-supabase.js": "/**\n * Experiment: Connect an application to Supabase.\n * Setup: npm install  ->  fill .env  ->  run 00-setup/schema.sql\n * Run  : node 01-supabase-fundamentals/07-connect-application-to-supabase.js\n * (the client itself is created in config/supabaseClient.js using createClient(url, anonKey))\n */\nconst { supabase } = require('../config/supabaseClient');\n\n(async () => {\n  // 1. Simple connectivity test: count rows in a table\n  const { count, error } = await supabase.from('students').select('*', { count: 'exact', head: true });\n  if (error) {\n    console.error('Connection or query failed:', error.message);\n    process.exit(1);\n  }\n  console.log('Connected to Supabase successfully. students rows =', count);\n\n  // 2. Check the Auth service is reachable\n  const { data } = await supabase.auth.getSession();\n  console.log('Auth reachable. Current session:', data.session ? 'signed in' : 'none (anonymous)');\n})();\n\n/* Browser usage (CDN):\n   <script src=\"https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2\"></script>\n   <script>const supabase = window.supabase.createClient('URL', 'ANON_KEY');</script>\n*/\n", "supabase/02-database-and-tables/01-create-tables.sql": "-- Experiment: Create tables using Supabase (SQL Editor)\n\ncreate table if not exists public.departments (\n  id   bigint generated always as identity primary key,\n  name text not null unique\n);\n\ncreate table if not exists public.employees (\n  id            bigint generated always as identity primary key,\n  first_name    text not null,\n  last_name     text not null,\n  email         text unique,\n  salary        numeric(10,2) default 0,\n  department_id bigint references public.departments(id),\n  hired_on      date default current_date\n);\n\n-- create table as / like\ncreate table public.employees_backup (like public.employees including all);\n\n-- modify tables\nalter table public.employees add column phone text;\nalter table public.employees rename column phone to mobile;\nalter table public.employees drop column mobile;\nalter table public.employees_backup rename to employees_archive;\n\n-- inspect\nselect table_name from information_schema.tables where table_schema = 'public' order by 1;\n\n-- remove\ndrop table public.employees_archive;\n", "supabase/02-database-and-tables/02-columns-and-postgresql-data-types.sql": "-- Experiment: Define columns and PostgreSQL data types\n\ncreate table public.data_types_demo (\n  -- numbers\n  id            bigint generated always as identity primary key,\n  small_number  smallint,                 -- -32768 .. 32767\n  whole_number  integer,                  -- 4 bytes\n  big_number    bigint,                   -- 8 bytes\n  price         numeric(10,2),            -- exact decimals (use for money)\n  ratio         double precision,         -- floating point\n  -- text\n  short_text    varchar(50),\n  any_text      text,                     -- unlimited length (preferred in Postgres)\n  fixed_code    char(3),\n  -- boolean / dates\n  is_active     boolean default true,\n  birth_date    date,\n  start_time    time,\n  created_at    timestamptz default now(), -- always prefer timestamptz\n  duration      interval,\n  -- special\n  external_id   uuid default gen_random_uuid(),\n  settings      jsonb default '{}'::jsonb,          -- JSON with indexing support\n  tags          text[],                              -- arrays\n  ip            inet,\n  status        text check (status in ('draft','published'))\n);\n\ninsert into public.data_types_demo (small_number, whole_number, price, short_text, birth_date, settings, tags, ip)\nvalues (10, 100000, 99.50, 'hello', '2004-05-17', '{\"theme\":\"dark\",\"lang\":\"en\"}', array['a','b'], '192.168.1.1');\n\nselect settings->>'theme' as theme, tags[1] as first_tag, extract(year from age(birth_date)) as age\nfrom public.data_types_demo;\n\nselect column_name, data_type, character_maximum_length\nfrom information_schema.columns where table_name = 'data_types_demo';\n\ndrop table public.data_types_demo;\n", "supabase/02-database-and-tables/03-primary-and-foreign-keys.sql": "-- Experiment: Implement primary keys and foreign keys\n\ncreate table public.teachers (\n  id   bigint generated always as identity primary key,       -- PRIMARY KEY (unique + not null)\n  name text not null\n);\n\ncreate table public.classes (\n  id         bigint generated always as identity,\n  title      text not null,\n  teacher_id bigint,\n  constraint pk_classes primary key (id),\n  constraint fk_classes_teacher foreign key (teacher_id)\n    references public.teachers(id)\n    on delete set null                                         -- when teacher is deleted, keep class\n    on update cascade\n);\n\n-- composite primary key + cascade delete\ncreate table public.enrollments (\n  class_id   bigint references public.classes(id) on delete cascade,\n  student_no int,\n  enrolled_on date default current_date,\n  primary key (class_id, student_no)\n);\n\ninsert into public.teachers (name) values ('Dr. Rao');\ninsert into public.classes (title, teacher_id) values ('Databases', 1);\ninsert into public.enrollments values (1, 101), (1, 102);\n\n-- Violations (uncomment one at a time to see the errors)\n-- insert into public.classes (title, teacher_id) values ('Bad', 999);   -- FK violation\n-- insert into public.enrollments values (1, 101);                         -- duplicate PK\n\ndelete from public.classes where id = 1;                                    -- cascades to enrollments\nselect count(*) as enrollments_left from public.enrollments;               -- 0\n\ndrop table public.enrollments; drop table public.classes; drop table public.teachers;\n", "supabase/02-database-and-tables/04-constraints-not-null-unique-default-check.sql": "-- Experiment: Implement NOT NULL, UNIQUE, DEFAULT and CHECK constraints\n\ncreate table public.accounts (\n  id         bigint generated always as identity primary key,\n  username   text        not null,                                    -- NOT NULL\n  email      text        not null unique,                             -- UNIQUE\n  role       text        not null default 'member',                   -- DEFAULT\n  age        int         check (age between 13 and 120),              -- CHECK (column)\n  balance    numeric     not null default 0,\n  created_at timestamptz not null default now(),\n  constraint chk_username_len check (char_length(username) >= 3),     -- CHECK (named)\n  constraint chk_balance      check (balance >= 0),\n  constraint uq_username      unique (username)\n);\n\n-- valid row (role, balance, created_at come from DEFAULT)\ninsert into public.accounts (username, email, age) values ('asha', 'asha@x.com', 20);\nselect * from public.accounts;\n\n-- each of these fails; run individually\n-- insert into public.accounts (username, email) values (null, 'a@x.com');       -- NOT NULL violation\n-- insert into public.accounts (username, email) values ('ravi', 'asha@x.com');  -- UNIQUE violation\n-- insert into public.accounts (username, email, age) values ('kid', 'k@x.com', 5);   -- CHECK violation\n-- insert into public.accounts (username, email, balance) values ('neg', 'n@x.com', -10);  -- CHECK violation\n\n-- add / drop constraints later\nalter table public.accounts add constraint chk_role check (role in ('member','admin'));\nalter table public.accounts drop constraint chk_role;\n\nselect conname, contype from pg_constraint where conrelid = 'public.accounts'::regclass;\n\ndrop table public.accounts;\n", "supabase/02-database-and-tables/05-insert-records.sql": "-- Experiment: Insert records into Supabase tables\n-- Uses the \"students\" table from 00-setup/schema.sql\n\n-- single row\ninsert into public.students (name, email, course, age)\nvalues ('Asha', 'asha@example.com', 'CSE', 20);\n\n-- multiple rows\ninsert into public.students (name, email, course, age) values\n  ('Ravi',  'ravi@example.com',  'ECE',  21),\n  ('Meena', 'meena@example.com', 'CSE',  19),\n  ('Kiran', 'kiran@example.com', 'MECH', 22);\n\n-- return the inserted rows\ninsert into public.students (name, email, course, age)\nvalues ('Divya', 'divya@example.com', 'IT', 20)\nreturning id, name, created_at;\n\n-- ignore duplicates / upsert\ninsert into public.students (name, email, course, age) values ('Asha', 'asha@example.com', 'CSE', 20)\non conflict (email) do nothing;\n\ninsert into public.students (name, email, course, age) values ('Asha K', 'asha@example.com', 'CSE', 21)\non conflict (email) do update set name = excluded.name, age = excluded.age;\n\n-- insert from a select\n-- insert into public.students_archive select * from public.students where age > 21;\n\nselect * from public.students order by id;\n", "supabase/02-database-and-tables/06-retrieve-records.sql": "-- Experiment: Retrieve records from Supabase tables\n\nselect * from public.students;                                    -- everything\nselect name, course from public.students;                         -- chosen columns\nselect name as student_name, age as years from public.students;   -- aliases\nselect distinct course from public.students;                      -- unique values\nselect * from public.students order by created_at desc limit 3;   -- latest 3\nselect * from public.students limit 2 offset 2;                   -- pagination\nselect * from public.students where id = 1;                       -- single row\nselect count(*) as total from public.students;\nselect course, count(*) from public.students group by course;\n", "supabase/02-database-and-tables/07-update-records.sql": "-- Experiment: Update records in Supabase tables\n\nupdate public.students set age = 21 where email = 'asha@example.com';               -- one row\nupdate public.students set course = 'CSE', age = age + 1 where name = 'Ravi';         -- many columns\nupdate public.students set age = age + 1 where course = 'CSE';                         -- many rows\nupdate public.students set name = upper(name) where id in (1, 2)                      -- IN list\nreturning id, name, age;                                                                 -- see the result\n\n-- Always use WHERE. Preview first:\nselect * from public.students where course = 'MECH';\nupdate public.students set course = 'MECHANICAL' where course = 'MECH';\n\n-- conditional update\nupdate public.students\nset course = case when age >= 22 then 'Final Year' else course end;\n", "supabase/02-database-and-tables/08-delete-records.sql": "-- Experiment: Delete records from Supabase tables\n\n-- preview what will be deleted\nselect * from public.students where email = 'kiran@example.com';\n\ndelete from public.students where email = 'kiran@example.com';        -- one row\ndelete from public.students where age > 60 returning *;               -- return deleted rows\ndelete from public.students where id in (select id from public.students order by id desc limit 1);\n\n-- remove ALL rows (keeps the table)\n-- delete from public.students;           -- logged, can be filtered\n-- truncate table public.students restart identity;    -- fast, resets ids\n\n-- drop the table itself\n-- drop table public.students;\n", "supabase/03-sql-operations/00-sample-data.sql": "-- Sample dataset used by experiments 01-08 in this folder. Run this first.\ndrop table if exists public.emp cascade;\ndrop table if exists public.dept cascade;\n\ncreate table public.dept (\n  id   int primary key,\n  name text not null,\n  city text\n);\ncreate table public.emp (\n  id      int primary key,\n  name    text not null,\n  salary  numeric(10,2) not null,\n  dept_id int references public.dept(id),\n  manager_id int,\n  hired   date\n);\n\ninsert into public.dept values (1,'IT','Chennai'), (2,'HR','Mumbai'), (3,'Sales','Delhi'), (4,'Legal','Pune');\ninsert into public.emp values\n  (1,'Asha',   60000, 1, null, '2019-01-15'),\n  (2,'Ravi',   45000, 1, 1,    '2020-03-10'),\n  (3,'Meena',  52000, 2, 1,    '2018-07-01'),\n  (4,'Kiran',  38000, 3, 3,    '2021-11-20'),\n  (5,'Divya',  70000, 3, 1,    '2017-05-05'),\n  (6,'Suresh', 41000, 1, 2,    '2022-02-14'),\n  (7,'Latha',  47000, null, 3, '2023-01-09');\n", "supabase/03-sql-operations/01-basic-select-queries.sql": "-- Experiment: Execute basic SELECT queries using the Supabase SQL Editor (run 00-sample-data.sql first)\n\nselect * from public.emp;\nselect name, salary from public.emp;\nselect name, salary * 12 as annual_salary from public.emp;\nselect distinct dept_id from public.emp;\nselect name || ' works in dept ' || dept_id as info from public.emp where dept_id is not null;\nselect count(*) from public.emp;\nselect * from public.emp limit 3;\nselect now() as current_time, 2 + 3 as five;\n", "supabase/03-sql-operations/02-filtering-with-where.sql": "-- Experiment: Implement filtering using WHERE\n\nselect * from public.emp where salary > 50000;\nselect * from public.emp where dept_id = 1 and salary >= 45000;\nselect * from public.emp where dept_id = 2 or dept_id = 3;\nselect * from public.emp where dept_id in (1, 3);\nselect * from public.emp where salary between 40000 and 50000;\nselect * from public.emp where name like 'M%';          -- starts with M\nselect * from public.emp where name ilike '%a';         -- ends with a (case-insensitive)\nselect * from public.emp where dept_id is null;         -- NULL check (never \"= null\")\nselect * from public.emp where not dept_id = 1;\nselect * from public.emp where hired >= '2020-01-01';\n", "supabase/03-sql-operations/03-sorting-with-order-by.sql": "-- Experiment: Implement sorting using ORDER BY\n\nselect * from public.emp order by salary;                    -- ascending (default)\nselect * from public.emp order by salary desc;               -- descending\nselect * from public.emp order by dept_id asc, salary desc;  -- multiple columns\nselect * from public.emp order by dept_id nulls last;        -- control NULL position\nselect * from public.emp order by hired desc limit 3;        -- 3 newest employees\nselect name, salary from public.emp order by 2 desc;         -- by column number\nselect name, length(name) as len from public.emp order by len desc, name;\n", "supabase/03-sql-operations/04-group-by-and-having.sql": "-- Experiment: Implement GROUP BY and HAVING\n\n-- employees per department\nselect dept_id, count(*) as employees from public.emp group by dept_id;\n\n-- average salary per department\nselect dept_id, round(avg(salary), 2) as avg_salary from public.emp group by dept_id order by avg_salary desc;\n\n-- HAVING filters groups (WHERE filters rows BEFORE grouping)\nselect dept_id, count(*) as employees\nfrom public.emp\ngroup by dept_id\nhaving count(*) >= 2;\n\nselect dept_id, sum(salary) as payroll\nfrom public.emp\nwhere dept_id is not null              -- row filter\ngroup by dept_id\nhaving sum(salary) > 90000             -- group filter\norder by payroll desc;\n\n-- group by year\nselect extract(year from hired) as year, count(*) from public.emp group by 1 order by 1;\n", "supabase/03-sql-operations/05-aggregate-functions.sql": "-- Experiment: Use aggregate functions COUNT, SUM, AVG, MIN and MAX\n\nselect\n  count(*)                 as total_rows,\n  count(dept_id)           as rows_with_dept,      -- ignores NULLs\n  count(distinct dept_id)  as distinct_depts,\n  sum(salary)              as total_salary,\n  round(avg(salary), 2)    as average_salary,\n  min(salary)              as lowest_salary,\n  max(salary)              as highest_salary,\n  min(hired)               as first_hire,\n  max(hired)               as last_hire\nfrom public.emp;\n\n-- with groups\nselect dept_id, count(*), sum(salary), avg(salary), min(salary), max(salary)\nfrom public.emp group by dept_id;\n\n-- other useful aggregates\nselect string_agg(name, ', ' order by name) as all_names,\n       array_agg(salary order by salary)   as salaries\nfrom public.emp;\n", "supabase/03-sql-operations/06-sql-joins.sql": "-- Experiment: Implement SQL joins (run 00-sample-data.sql first)\n\n-- INNER JOIN: only rows that match on both sides\nselect e.name, d.name as department, d.city\nfrom public.emp e\ninner join public.dept d on d.id = e.dept_id;\n\n-- LEFT JOIN: all employees, even those without a department (Latha)\nselect e.name, d.name as department\nfrom public.emp e left join public.dept d on d.id = e.dept_id;\n\n-- RIGHT JOIN: all departments, even those with no employees (Legal)\nselect d.name as department, e.name\nfrom public.emp e right join public.dept d on d.id = e.dept_id;\n\n-- FULL OUTER JOIN: everything from both tables\nselect e.name, d.name as department\nfrom public.emp e full outer join public.dept d on d.id = e.dept_id;\n\n-- CROSS JOIN: every combination\nselect e.name, d.name from public.emp e cross join public.dept d limit 10;\n\n-- SELF JOIN: employee with their manager\nselect e.name as employee, m.name as manager\nfrom public.emp e left join public.emp m on m.id = e.manager_id;\n\n-- join + aggregate: employees per department (including empty departments)\nselect d.name, count(e.id) as employees\nfrom public.dept d left join public.emp e on e.dept_id = d.id\ngroup by d.name order by employees desc;\n", "supabase/03-sql-operations/07-subqueries.sql": "-- Experiment: Implement subqueries\n\n-- scalar subquery: earns more than the company average\nselect name, salary from public.emp\nwhere salary > (select avg(salary) from public.emp);\n\n-- IN subquery: employees of departments located in Chennai or Delhi\nselect name from public.emp\nwhere dept_id in (select id from public.dept where city in ('Chennai', 'Delhi'));\n\n-- NOT EXISTS: departments with no employees\nselect d.name from public.dept d\nwhere not exists (select 1 from public.emp e where e.dept_id = d.id);\n\n-- correlated subquery: earns the highest salary in own department\nselect name, dept_id, salary from public.emp e\nwhere salary = (select max(salary) from public.emp where dept_id = e.dept_id);\n\n-- subquery in FROM (derived table)\nselect dept_id, avg_sal from (\n  select dept_id, avg(salary) as avg_sal from public.emp group by dept_id\n) t where avg_sal > 50000;\n\n-- subquery in SELECT\nselect name, salary, (select round(avg(salary),0) from public.emp) as company_avg from public.emp;\n\n-- ANY / ALL\nselect name from public.emp where salary > all (select salary from public.emp where dept_id = 2);\n", "supabase/03-sql-operations/08-database-views.sql": "-- Experiment: Create and use database views\n\n-- simple view\ncreate or replace view public.emp_details as\nselect e.id, e.name, e.salary, d.name as department, d.city\nfrom public.emp e left join public.dept d on d.id = e.dept_id;\n\nselect * from public.emp_details where department = 'IT';\n\n-- aggregate view\ncreate or replace view public.dept_summary as\nselect d.name as department, count(e.id) as employees,\n       coalesce(round(avg(e.salary),2), 0) as avg_salary\nfrom public.dept d left join public.emp e on e.dept_id = d.id\ngroup by d.name;\n\nselect * from public.dept_summary order by employees desc;\n\n-- security_invoker makes the view obey the RLS of the person querying it (Postgres 15+)\nalter view public.dept_summary set (security_invoker = true);\n\n-- materialized view = stored result, refresh manually\ncreate materialized view public.high_earners as select * from public.emp where salary > 50000;\nrefresh materialized view public.high_earners;\nselect * from public.high_earners;\n\n-- cleanup\ndrop materialized view public.high_earners;\ndrop view public.dept_summary;\ndrop view public.emp_details;\n", "supabase/04-supabase-crud/01-create-operation.js": "/**\n * Experiment: Implement Create operations using the Supabase Client.\n * Run: node 04-supabase-crud/01-create-operation.js\n */\nconst { supabase } = require('../config/supabaseClient');\n\n(async () => {\n  // 1. insert one row and return it\n  const { data: one, error: e1 } = await supabase\n    .from('students')\n    .insert({ name: 'Asha', email: `asha${Date.now()}@example.com`, course: 'CSE', age: 20 })\n    .select()\n    .single();\n  console.log('insert one  :', e1 ? e1.message : one);\n\n  // 2. insert many rows\n  const stamp = Date.now();\n  const { data: many, error: e2 } = await supabase\n    .from('students')\n    .insert([\n      { name: 'Ravi', email: `ravi${stamp}@example.com`, course: 'ECE', age: 21 },\n      { name: 'Meena', email: `meena${stamp}@example.com`, course: 'IT', age: 19 },\n    ])\n    .select();\n  console.log('insert many :', e2 ? e2.message : many.map((r) => r.id));\n\n  // 3. upsert: insert, or update when the unique email already exists\n  const { data: up, error: e3 } = await supabase\n    .from('students')\n    .upsert({ name: 'Asha Updated', email: one?.email, course: 'CSE', age: 21 }, { onConflict: 'email' })\n    .select();\n  console.log('upsert      :', e3 ? e3.message : up);\n\n  // 4. error handling: violates CHECK (age >= 15)\n  const { error: e4 } = await supabase.from('students').insert({ name: 'Kid', email: 'kid@x.com', course: 'CSE', age: 5 });\n  console.log('invalid row :', e4 ? `${e4.code} - ${e4.message}` : 'unexpectedly inserted');\n})();\n", "supabase/04-supabase-crud/02-read-operation.js": "/**\n * Experiment: Implement Read operations using the Supabase Client.\n * Run: node 04-supabase-crud/02-read-operation.js   (insert some students first)\n */\nconst { supabase } = require('../config/supabaseClient');\n\n(async () => {\n  const show = (label, { data, error }) => console.log(label, error ? error.message : data);\n\n  show('all            :', await supabase.from('students').select('*'));\n  show('columns        :', await supabase.from('students').select('id, name, course'));\n  show('eq             :', await supabase.from('students').select('*').eq('course', 'CSE'));\n  show('gt + lt        :', await supabase.from('students').select('name, age').gt('age', 18).lt('age', 25));\n  show('in             :', await supabase.from('students').select('name').in('course', ['CSE', 'IT']));\n  show('ilike          :', await supabase.from('students').select('name').ilike('name', '%a%'));\n  show('or             :', await supabase.from('students').select('name, age, course').or('age.gte.22,course.eq.IT'));\n  show('order + limit  :', await supabase.from('students').select('name, age').order('age', { ascending: false }).limit(3));\n  show('range (page 2) :', await supabase.from('students').select('id, name').order('id').range(3, 5));\n  show('single         :', await supabase.from('students').select('*').limit(1).single());\n  show('maybeSingle    :', await supabase.from('students').select('*').eq('id', -1).maybeSingle());\n\n  const { count } = await supabase.from('students').select('*', { count: 'exact', head: true });\n  console.log('total count    :', count);\n})();\n", "supabase/04-supabase-crud/03-update-operation.js": "/**\n * Experiment: Implement Update operations using the Supabase Client.\n * Run: node 04-supabase-crud/03-update-operation.js\n */\nconst { supabase } = require('../config/supabaseClient');\n\n(async () => {\n  const email = `update${Date.now()}@example.com`;\n  const { data: created } = await supabase.from('students').insert({ name: 'Before', email, course: 'CSE', age: 20 }).select().single();\n  console.log('created  :', created);\n\n  // update a single row by id and return it\n  const { data: one, error } = await supabase\n    .from('students').update({ name: 'After', age: 21 }).eq('id', created.id).select().single();\n  console.log('updated  :', error ? error.message : one);\n\n  // update many rows with a filter\n  const { data: many } = await supabase.from('students').update({ course: 'Computer Science' }).eq('course', 'CSE').select('id');\n  console.log('rows updated by filter:', many?.length);\n\n  // update that matches nothing returns an empty array (not an error)\n  const { data: none } = await supabase.from('students').update({ age: 30 }).eq('id', -1).select();\n  console.log('no match :', none);\n\n  // ALWAYS add a filter: update() without eq()/match() is rejected by PostgREST when \"safe update\" is on\n})();\n", "supabase/04-supabase-crud/04-delete-operation.js": "/**\n * Experiment: Implement Delete operations using the Supabase Client.\n * Run: node 04-supabase-crud/04-delete-operation.js\n */\nconst { supabase } = require('../config/supabaseClient');\n\n(async () => {\n  const { data: created } = await supabase.from('students')\n    .insert({ name: 'Temp', email: `temp${Date.now()}@example.com`, course: 'CSE', age: 20 }).select().single();\n  console.log('created :', created.id);\n\n  // delete one row, return the deleted row\n  const { data: deleted, error } = await supabase.from('students').delete().eq('id', created.id).select().single();\n  console.log('deleted :', error ? error.message : deleted);\n\n  // delete many rows\n  const { data: many } = await supabase.from('students').delete().like('email', 'temp%').select('id');\n  console.log('bulk deleted count:', many?.length ?? 0);\n\n  // confirm it is gone\n  const { data: check } = await supabase.from('students').select('id').eq('id', created.id);\n  console.log('still exists?', check.length > 0);\n})();\n", "supabase/04-supabase-crud/05-student-management-crud.js": "/**\n * Experiment: Create a Student Management CRUD application.\n * Run: node 04-supabase-crud/05-student-management-crud.js  -> http://localhost:3000\n * API: GET/POST /api/students , GET/PUT/DELETE /api/students/:id\n */\nrequire('../config/crudApp')({\n  table: 'students',\n  title: 'Student Management (Supabase)',\n  fields: [\n    { name: 'name', type: 'text' },\n    { name: 'email', type: 'email' },\n    { name: 'course', type: 'text' },\n    { name: 'age', type: 'number' },\n  ],\n});\n", "supabase/04-supabase-crud/06-product-management-crud.js": "/**\n * Experiment: Create a Product Management CRUD application.\n * Run: node 04-supabase-crud/06-product-management-crud.js  -> http://localhost:3000\n */\nrequire('../config/crudApp')({\n  table: 'products',\n  title: 'Product Management (Supabase)',\n  fields: [\n    { name: 'name', type: 'text' },\n    { name: 'price', type: 'number' },\n    { name: 'stock', type: 'number' },\n    { name: 'category', type: 'text' },\n  ],\n});\n", "supabase/04-supabase-crud/07-user-management-crud.js": "/**\n * Experiment: Create a User Management CRUD application.\n * Manages the public.profiles table (one row per auth user). Creating auth users needs the admin key,\n * so this app uses the admin client for create/delete; profile edits use the normal table API.\n * Run: node 04-supabase-crud/07-user-management-crud.js -> http://localhost:3000\n */\nconst express = require('express');\nconst { supabaseAdmin } = require('../config/supabaseClient');\n\nif (!supabaseAdmin) { console.error('SUPABASE_SERVICE_ROLE_KEY is required for this experiment'); process.exit(1); }\n\nconst app = express();\napp.use(express.json());\n\n// CREATE user (auth.users) - the trigger in schema.sql creates the profile row automatically\napp.post('/api/users', async (req, res) => {\n  const { email, password, full_name, role = 'user' } = req.body;\n  const { data, error } = await supabaseAdmin.auth.admin.createUser({\n    email, password, email_confirm: true, user_metadata: { full_name },\n  });\n  if (error) return res.status(400).json({ error: error.message });\n  await supabaseAdmin.from('profiles').update({ role, full_name }).eq('id', data.user.id);\n  res.status(201).json({ id: data.user.id, email, full_name, role });\n});\n\n// READ\napp.get('/api/users', async (req, res) => {\n  const { data: profiles, error } = await supabaseAdmin.from('profiles').select('*').order('created_at');\n  if (error) return res.status(400).json({ error: error.message });\n  const { data: list } = await supabaseAdmin.auth.admin.listUsers();\n  const emails = Object.fromEntries((list?.users || []).map((u) => [u.id, u.email]));\n  res.json(profiles.map((p) => ({ ...p, email: emails[p.id] })));\n});\n\n// UPDATE\napp.put('/api/users/:id', async (req, res) => {\n  const { full_name, role } = req.body;\n  const { data, error } = await supabaseAdmin.from('profiles').update({ full_name, role }).eq('id', req.params.id).select().single();\n  error ? res.status(400).json({ error: error.message }) : res.json(data);\n});\n\n// DELETE (removes auth user; profile row is removed by ON DELETE CASCADE)\napp.delete('/api/users/:id', async (req, res) => {\n  const { error } = await supabaseAdmin.auth.admin.deleteUser(req.params.id);\n  error ? res.status(400).json({ error: error.message }) : res.status(204).end();\n});\n\napp.get('/', (req, res) => res.send(`<h2>User Management</h2><p>Use the API:</p>\n<pre>POST /api/users {email,password,full_name,role}\nGET /api/users\nPUT /api/users/:id {full_name,role}\nDELETE /api/users/:id</pre>\n<div id=o></div><script>fetch('/api/users').then(r=>r.json()).then(d=>o.innerHTML='<table border=1 cellpadding=6><tr><th>email</th><th>name</th><th>role</th></tr>'+d.map(u=>'<tr><td>'+u.email+'</td><td>'+(u.full_name||'')+'</td><td>'+u.role+'</td></tr>').join('')+'</table>')</script>`));\n\napp.listen(process.env.PORT || 3000, () => console.log('User management on http://localhost:3000'));\n", "supabase/05-authentication/01-configure-supabase-authentication.md": "# Experiment: Configure Supabase Authentication\n\n**Aim:** Prepare a project so users can register and log in with e-mail and password.\n\n## Steps\n1. Dashboard > **Authentication > Providers** -> make sure **Email** is enabled.\n2. Under Email provider settings:\n   - **Confirm email:** OFF for the lab (users can log in immediately). Turn ON in production.\n   - **Minimum password length:** 8 (or more).\n   - **Secure password change / Prevent leaked passwords:** optional hardening.\n3. **Authentication > URL Configuration**\n   - **Site URL:** `http://localhost:3000`\n   - **Redirect URLs:** add `http://localhost:3000/**` (used by password-reset and magic-link mails).\n4. **Authentication > Email Templates**: customise *Confirm signup*, *Reset password*, *Magic link*.\n5. (Optional) **Providers > Google/GitHub**: paste the OAuth client ID and secret from the provider's console.\n6. **Authentication > Users**: after sign-up tests, users appear here (you can also *Add user*, *Send password recovery*, *Delete*).\n7. Run `00-setup/schema.sql` - it adds a trigger that creates a `profiles` row for every new user.\n8. Create the test accounts from `.env.example` (`TEST_EMAIL`, `TEST_EMAIL_2`) by running `02-user-registration.js`.\n\n## Notes\n- The built-in mailer is rate-limited (a few e-mails per hour) - configure custom SMTP for heavy testing.\n- Passwords are hashed (bcrypt) by Supabase; they are never visible.\n- Sessions are JWT access tokens (default 1 hour) plus a refresh token.\n", "supabase/05-authentication/02-user-registration.js": "/**\n * Experiment: Implement user registration.\n * Run: node 05-authentication/02-user-registration.js\n * Set TEST_EMAIL / TEST_PASSWORD in .env (or defaults are used).\n */\nconst { supabase } = require('../config/supabaseClient');\n\n(async () => {\n  const email = process.env.TEST_EMAIL || 'student1@example.com';\n  const password = process.env.TEST_PASSWORD || 'Passw0rd!123';\n\n  const { data, error } = await supabase.auth.signUp({\n    email,\n    password,\n    options: { data: { full_name: 'Student One' } },      // stored in user_metadata\n  });\n\n  if (error) return console.error('Registration failed:', error.message);\n\n  console.log('User id     :', data.user?.id);\n  console.log('Email       :', data.user?.email);\n  console.log('Session     :', data.session ? 'created (confirm-email is OFF)' : 'none - confirm the e-mail first');\n  // Registering an existing e-mail returns a user with no identities (to avoid leaking who is registered)\n  if (data.user && data.user.identities?.length === 0) console.log('This e-mail is already registered.');\n})();\n", "supabase/05-authentication/03-user-login.js": "/**\n * Experiment: Implement user login.\n * Run: node 05-authentication/03-user-login.js\n */\nconst { supabase } = require('../config/supabaseClient');\n\n(async () => {\n  const { data, error } = await supabase.auth.signInWithPassword({\n    email: process.env.TEST_EMAIL || 'student1@example.com',\n    password: process.env.TEST_PASSWORD || 'Passw0rd!123',\n  });\n\n  if (error) return console.error('Login failed:', error.status, error.message);\n\n  console.log('Logged in as :', data.user.email);\n  console.log('Access token :', data.session.access_token.slice(0, 30) + '...');\n  console.log('Expires at   :', new Date(data.session.expires_at * 1000).toLocaleString());\n  console.log('Refresh token:', data.session.refresh_token);\n})();\n", "supabase/05-authentication/04-user-logout.js": "/**\n * Experiment: Implement user logout.\n * Run: node 05-authentication/04-user-logout.js\n */\nconst { supabase } = require('../config/supabaseClient');\n\n(async () => {\n  await supabase.auth.signInWithPassword({\n    email: process.env.TEST_EMAIL || 'student1@example.com',\n    password: process.env.TEST_PASSWORD || 'Passw0rd!123',\n  });\n  console.log('Session before logout:', (await supabase.auth.getSession()).data.session ? 'active' : 'none');\n\n  const { error } = await supabase.auth.signOut();          // scope: 'local' | 'global' | 'others'\n  if (error) return console.error('Logout failed:', error.message);\n\n  console.log('Session after logout :', (await supabase.auth.getSession()).data.session ? 'active' : 'none');\n  // await supabase.auth.signOut({ scope: 'global' })  -> signs the user out on ALL devices\n})();\n", "supabase/05-authentication/05-get-current-user.js": "/**\n * Experiment: Retrieve the currently authenticated user.\n * Run: node 05-authentication/05-get-current-user.js\n */\nconst { supabase } = require('../config/supabaseClient');\n\n(async () => {\n  let { data: { user } } = await supabase.auth.getUser();\n  console.log('Before login :', user);                         // null - not signed in\n\n  await supabase.auth.signInWithPassword({\n    email: process.env.TEST_EMAIL || 'student1@example.com',\n    password: process.env.TEST_PASSWORD || 'Passw0rd!123',\n  });\n\n  // getUser() asks the Auth server to validate the token -> use it for security decisions\n  ({ data: { user } } = await supabase.auth.getUser());\n  console.log('After login  :', { id: user.id, email: user.email, metadata: user.user_metadata });\n\n  // getSession() reads the locally stored session (fast, but NOT re-validated)\n  const { data: { session } } = await supabase.auth.getSession();\n  console.log('Session user :', session.user.email);\n\n  // matching profile row (created by the trigger)\n  const { data: profile } = await supabase.from('profiles').select('*').eq('id', user.id).single();\n  console.log('Profile      :', profile);\n})();\n", "supabase/05-authentication/06-email-password-authentication.js": "/**\n * Experiment: Implement email/password authentication (complete flow).\n * Run: node 05-authentication/06-email-password-authentication.js\n */\nconst { supabase } = require('../config/supabaseClient');\n\nconst email = `flow${Date.now()}@example.com`;\nconst password = 'Passw0rd!123';\nconst step = (n, t) => console.log(`\\n${n}. ${t}`);\n\n(async () => {\n  step(1, 'Sign up');\n  const su = await supabase.auth.signUp({ email, password, options: { data: { full_name: 'Flow User' } } });\n  console.log(su.error ? su.error.message : `created ${su.data.user.id}`);\n  if (su.error || !su.data.session) return console.log('E-mail confirmation is ON - confirm the mail, then run the next steps manually.');\n\n  step(2, 'Wrong password is rejected');\n  const bad = await supabase.auth.signInWithPassword({ email, password: 'wrong-password' });\n  console.log(bad.error?.message);\n\n  step(3, 'Sign in with correct password');\n  const si = await supabase.auth.signInWithPassword({ email, password });\n  console.log(si.error ? si.error.message : 'signed in as ' + si.data.user.email);\n\n  step(4, 'Update metadata + password while signed in');\n  const up = await supabase.auth.updateUser({ password: 'NewPassw0rd!456', data: { full_name: 'Renamed User' } });\n  console.log(up.error ? up.error.message : 'updated: ' + up.data.user.user_metadata.full_name);\n\n  step(5, 'Sign out');\n  await supabase.auth.signOut();\n\n  step(6, 'Old password no longer works, new one does');\n  console.log('old:', (await supabase.auth.signInWithPassword({ email, password })).error?.message);\n  console.log('new:', (await supabase.auth.signInWithPassword({ email, password: 'NewPassw0rd!456' })).data.user?.email);\n  await supabase.auth.signOut();\n})();\n", "supabase/05-authentication/07-password-reset.js": "/**\n * Experiment: Implement password reset.\n * Flow: 1) request e-mail  2) user clicks the link  3) page below sets the new password\n * Run : node 05-authentication/07-password-reset.js  -> http://localhost:3000\n * Prereq: Authentication > URL Configuration has Redirect URL  http://localhost:3000/**\n */\nconst express = require('express');\nconst { supabase } = require('../config/supabaseClient');\nconst app = express();\napp.use(express.json());\n\n// Step 1 - send the reset e-mail\napp.post('/forgot-password', async (req, res) => {\n  const { error } = await supabase.auth.resetPasswordForEmail(req.body.email, {\n    redirectTo: 'http://localhost:3000/reset-password',\n  });\n  // same answer either way so attackers cannot discover registered e-mails\n  if (error) console.error(error.message);\n  res.json({ message: 'If that e-mail exists, a reset link has been sent.' });\n});\n\n// Step 2/3 - the link lands here; supabase-js in the browser reads the token from the URL hash\napp.get('/reset-password', (req, res) => res.send(`<!DOCTYPE html><html><body style=\"font-family:Arial;max-width:420px;margin:40px auto\">\n<h3>Set a new password</h3><input id=\"pw\" type=\"password\" placeholder=\"New password\" style=\"width:100%;padding:8px\">\n<button onclick=\"save()\" style=\"margin-top:10px;padding:8px 14px\">Update password</button><p id=\"m\"></p>\n<script src=\"https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2\"></script>\n<script>\nconst sb = window.supabase.createClient('${process.env.SUPABASE_URL}', '${process.env.SUPABASE_ANON_KEY}');\nsb.auth.onAuthStateChange((event) => { if (event === 'PASSWORD_RECOVERY') m.textContent = 'Recovery link verified. Enter a new password.'; });\nasync function save(){ const {error}=await sb.auth.updateUser({password:pw.value}); m.textContent = error?error.message:'Password updated! You can log in now.'; }\n</script></body></html>`));\n\napp.get('/', (req, res) => res.send(`<!DOCTYPE html><html><body style=\"font-family:Arial;max-width:420px;margin:40px auto\">\n<h3>Forgot password</h3><input id=\"e\" type=\"email\" placeholder=\"you@example.com\" style=\"width:100%;padding:8px\">\n<button onclick=\"go()\" style=\"margin-top:10px;padding:8px 14px\">Send reset link</button><p id=\"m\"></p>\n<script>async function go(){const r=await fetch('/forgot-password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:e.value})});m.textContent=(await r.json()).message}</script></body></html>`));\n\napp.listen(3000, () => console.log('http://localhost:3000'));\n", "supabase/05-authentication/08-manage-authentication-sessions.js": "/**\n * Experiment: Manage authentication sessions.\n * Run: node 05-authentication/08-manage-authentication-sessions.js\n */\nconst { supabase } = require('../config/supabaseClient');\n\n(async () => {\n  // listen for auth events (SIGNED_IN, TOKEN_REFRESHED, SIGNED_OUT, USER_UPDATED, PASSWORD_RECOVERY)\n  const { data: { subscription } } = supabase.auth.onAuthStateChange((event, session) => {\n    console.log(`[event] ${event} ->`, session ? session.user.email : 'no session');\n  });\n\n  const { data } = await supabase.auth.signInWithPassword({\n    email: process.env.TEST_EMAIL || 'student1@example.com',\n    password: process.env.TEST_PASSWORD || 'Passw0rd!123',\n  });\n  const s = data.session;\n  console.log('access token valid until :', new Date(s.expires_at * 1000).toLocaleTimeString());\n  console.log('seconds remaining        :', s.expires_at - Math.floor(Date.now() / 1000));\n\n  // refresh: exchanges the refresh token for a new access token\n  const { data: refreshed } = await supabase.auth.refreshSession();\n  console.log('new token differs        :', refreshed.session.access_token !== s.access_token);\n\n  // restore a saved session (e.g. from a cookie / database)\n  const saved = { access_token: refreshed.session.access_token, refresh_token: refreshed.session.refresh_token };\n  await supabase.auth.signOut({ scope: 'local' });\n  const { data: restored } = await supabase.auth.setSession(saved);\n  console.log('restored session for     :', restored.user?.email);\n\n  await supabase.auth.signOut();\n  subscription.unsubscribe();\n  // Browser tips: supabase-js stores the session in localStorage and auto-refreshes it before expiry.\n})();\n", "supabase/05-authentication/09-protected-routes.js": "/**\n * Experiment: Implement protected pages/routes using Supabase Authentication.\n * The browser signs in with supabase-js and sends the JWT to this Express server,\n * which verifies it with supabase.auth.getUser(token) before serving private data.\n * Run: node 05-authentication/09-protected-routes.js -> http://localhost:3000\n */\nconst express = require('express');\nconst { supabase } = require('../config/supabaseClient');\nconst app = express();\napp.use(express.json());\n\n// ---------- middleware ----------\nasync function requireAuth(req, res, next) {\n  const token = (req.headers.authorization || '').replace('Bearer ', '');\n  if (!token) return res.status(401).json({ error: 'Missing token' });\n  const { data, error } = await supabase.auth.getUser(token);      // validated by Supabase Auth\n  if (error || !data.user) return res.status(401).json({ error: 'Invalid or expired token' });\n  req.user = data.user;\n  next();\n}\n\n// ---------- API ----------\napp.get('/api/public', (req, res) => res.json({ message: 'Anyone can read this' }));\napp.get('/api/private', requireAuth, (req, res) => res.json({ message: `Secret data for ${req.user.email}`, userId: req.user.id }));\n\n// ---------- pages ----------\napp.get('/', (req, res) => res.send(`<!DOCTYPE html><html><body style=\"font-family:Arial;max-width:520px;margin:40px auto\">\n<div id=\"loginBox\"><h3>Login</h3><input id=\"e\" placeholder=\"email\"><input id=\"p\" type=\"password\" placeholder=\"password\">\n<button onclick=\"login()\">Login</button><button onclick=\"signup()\">Sign up</button></div>\n<div id=\"appBox\" style=\"display:none\"><h3>Dashboard (protected)</h3><pre id=\"out\"></pre><button onclick=\"logout()\">Logout</button></div><p id=\"m\"></p>\n<script src=\"https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2\"></script>\n<script>\nconst sb = window.supabase.createClient('${process.env.SUPABASE_URL}', '${process.env.SUPABASE_ANON_KEY}');\nasync function show(session){\n  loginBox.style.display = session ? \"none\" : \"block\"; appBox.style.display = session ? \"block\" : \"none\";\n  if(!session) return;\n  const r = await fetch('/api/private',{headers:{Authorization:'Bearer '+session.access_token}});\n  out.textContent = JSON.stringify(await r.json(),null,2);\n}\nasync function login(){const {error}=await sb.auth.signInWithPassword({email:e.value,password:p.value}); if(error) m.textContent=error.message;}\nasync function signup(){const {error}=await sb.auth.signUp({email:e.value,password:p.value}); m.textContent=error?error.message:'Registered. Now login.';}\nasync function logout(){await sb.auth.signOut();}\nsb.auth.onAuthStateChange((_e, session) => show(session));           // route guard: UI follows session state\nsb.auth.getSession().then(({data}) => show(data.session));\n</script></body></html>`));\n\napp.listen(3000, () => console.log('http://localhost:3000'));\n", "supabase/06-authorization-and-security/01-enable-row-level-security.sql": "-- Experiment: Enable Row Level Security (RLS)\n-- RLS makes Postgres check a policy for EVERY row a user tries to read or write.\n\n-- 1. Create a demo table\ncreate table if not exists public.notes (\n  id         bigint generated always as identity primary key,\n  user_id    uuid not null default auth.uid() references auth.users(id) on delete cascade,\n  title      text not null,\n  content    text,\n  created_at timestamptz default now()\n);\n\n-- 2. Enable RLS. With RLS ON and NO policies => nobody (anon/authenticated) can see or change anything.\nalter table public.notes enable row level security;\n-- Also apply RLS to the table owner (optional, stricter):\n-- alter table public.notes force row level security;\n\n-- 3. Check which tables have RLS\nselect schemaname, tablename, rowsecurity\nfrom pg_tables where schemaname = 'public' order by tablename;\n\n-- 4. Test: this returns 0 rows for API users until a policy exists\n--    (the SQL Editor runs as the postgres role, which bypasses RLS)\n-- curl \"$SUPABASE_URL/rest/v1/notes\" -H \"apikey: $ANON_KEY\"   ->   []\n\n-- 5. Disable (only for experiments)\n-- alter table public.notes disable row level security;\n", "supabase/06-authorization-and-security/02-create-rls-policies.sql": "-- Experiment: Create RLS policies (run 01-enable-row-level-security.sql first)\n-- Syntax: create policy \"name\" on table for [select|insert|update|delete|all] to role using (...) with check (...);\n--   USING       = which EXISTING rows can be seen / updated / deleted\n--   WITH CHECK  = which NEW or changed row values are allowed\n\ndrop policy if exists \"notes are public to read\"  on public.notes;\ndrop policy if exists \"signed-in users can insert\" on public.notes;\n\n-- everyone (even anonymous) can read\ncreate policy \"notes are public to read\"\non public.notes for select\nto anon, authenticated\nusing (true);\n\n-- only signed-in users can insert, and only rows that list themselves as the owner\ncreate policy \"signed-in users can insert\"\non public.notes for insert\nto authenticated\nwith check (auth.uid() = user_id);\n\n-- list policies\nselect policyname, cmd, roles, qual, with_check\nfrom pg_policies where tablename = 'notes';\n\n-- change / remove a policy\n-- alter policy \"notes are public to read\" on public.notes to authenticated;\n-- drop policy \"notes are public to read\" on public.notes;\n", "supabase/06-authorization-and-security/03-restrict-users-to-own-records.sql": "-- Experiment: Restrict users to their own records\n-- Each user may only see and change rows where user_id = their own id.\n\nalter table public.notes enable row level security;\n\n-- remove the earlier public policies from experiment 02\ndrop policy if exists \"notes are public to read\"  on public.notes;\ndrop policy if exists \"signed-in users can insert\" on public.notes;\n\ncreate policy \"own notes: select\" on public.notes for select to authenticated\nusing ((select auth.uid()) = user_id);                                  -- (select ...) is cached => faster\n\ncreate policy \"own notes: insert\" on public.notes for insert to authenticated\nwith check ((select auth.uid()) = user_id);\n\ncreate policy \"own notes: update\" on public.notes for update to authenticated\nusing ((select auth.uid()) = user_id)\nwith check ((select auth.uid()) = user_id);                             -- cannot hand the row to someone else\n\ncreate policy \"own notes: delete\" on public.notes for delete to authenticated\nusing ((select auth.uid()) = user_id);\n\n-- Same idea for the students table (rows created by the current user)\ndrop policy if exists \"lab: open read students\"  on public.students;\ndrop policy if exists \"lab: open write students\" on public.students;\ncreate policy \"own students\" on public.students for all to authenticated\nusing ((select auth.uid()) = user_id) with check ((select auth.uid()) = user_id);\n\n-- index the column used by the policy for speed\ncreate index if not exists notes_user_id_idx on public.notes(user_id);\n", "supabase/06-authorization-and-security/04-create-admin-and-user-roles.sql": "-- Experiment: Create Admin and User roles\n-- Roles live in public.profiles.role ('user' | 'admin') - created by 00-setup/schema.sql.\n\n-- 1. Helper functions (SECURITY DEFINER avoids infinite recursion when a policy reads profiles)\ncreate or replace function public.current_role_name()\nreturns text language sql stable security definer set search_path = public as $$\n  select role from public.profiles where id = auth.uid();\n$$;\n\ncreate or replace function public.is_admin()\nreturns boolean language sql stable security definer set search_path = public as $$\n  select coalesce((select role = 'admin' from public.profiles where id = auth.uid()), false);\n$$;\n\n-- 2. Make a user an admin (replace the e-mail with a registered account)\nupdate public.profiles set role = 'admin'\nwhere id = (select id from auth.users where email = 'admin@example.com');\n\n-- 3. Users may read their own profile; admins can read all\ndrop policy if exists \"lab: read profiles\" on public.profiles;\ncreate policy \"profiles: read own or admin\" on public.profiles for select to authenticated\nusing (id = (select auth.uid()) or public.is_admin());\n\n-- 4. Users can edit their own name but must NOT be able to promote themselves\ncreate policy \"profiles: update own\" on public.profiles for update to authenticated\nusing (id = (select auth.uid()))\nwith check (id = (select auth.uid()) and role = (select role from public.profiles where id = auth.uid()));\n\n-- 5. Admin can change any profile (including role)\ncreate policy \"profiles: admin update all\" on public.profiles for update to authenticated\nusing (public.is_admin()) with check (public.is_admin());\n\n-- 6. Check\nselect p.id, u.email, p.role from public.profiles p join auth.users u on u.id = p.id;\nselect public.is_admin() as am_i_admin;      -- run from the client to test\n", "supabase/06-authorization-and-security/05-role-based-access-control.sql": "-- Experiment: Implement role-based access control (RBAC)\n-- Requires: 04-create-admin-and-user-roles.sql  (is_admin())\n--   admin -> full access to all products & students\n--   user  -> read all products, manage only rows they created\n\n-- PRODUCTS ----------------------------------------------------------\ndrop policy if exists \"lab: open read products\"  on public.products;\ndrop policy if exists \"lab: open write products\" on public.products;\n\ncreate policy \"products: everyone signed-in can read\" on public.products\nfor select to authenticated using (true);\n\ncreate policy \"products: only admin can insert\" on public.products\nfor insert to authenticated with check (public.is_admin());\n\ncreate policy \"products: only admin can update\" on public.products\nfor update to authenticated using (public.is_admin()) with check (public.is_admin());\n\ncreate policy \"products: only admin can delete\" on public.products\nfor delete to authenticated using (public.is_admin());\n\n-- STUDENTS: owner OR admin --------------------------------------------\ndrop policy if exists \"own students\" on public.students;\n\ncreate policy \"students: owner or admin select\" on public.students for select to authenticated\nusing (user_id = (select auth.uid()) or public.is_admin());\n\ncreate policy \"students: owner inserts\" on public.students for insert to authenticated\nwith check (user_id = (select auth.uid()) or public.is_admin());\n\ncreate policy \"students: owner or admin update\" on public.students for update to authenticated\nusing (user_id = (select auth.uid()) or public.is_admin())\nwith check (user_id = (select auth.uid()) or public.is_admin());\n\ncreate policy \"students: owner or admin delete\" on public.students for delete to authenticated\nusing (user_id = (select auth.uid()) or public.is_admin());\n\n-- (Advanced) roles inside the JWT: put role in app_metadata and read it with\n--   (auth.jwt() -> 'app_metadata' ->> 'role') = 'admin'\n", "supabase/06-authorization-and-security/06-authenticated-user-policies.sql": "-- Experiment: Implement authenticated-user policies\n-- Difference between the Postgres roles used by Supabase:\n--   anon           -> not logged in (only the anon key)\n--   authenticated  -> logged in (valid JWT)\n--   service_role   -> server key, bypasses RLS\n\n-- Public read for anonymous visitors, write only for logged-in users\nalter table public.messages enable row level security;\ndrop policy if exists \"lab: open read messages\"  on public.messages;\ndrop policy if exists \"lab: open write messages\" on public.messages;\n\ncreate policy \"messages: anyone can read\" on public.messages\nfor select to anon, authenticated using (true);\n\ncreate policy \"messages: only authenticated can post as themselves\" on public.messages\nfor insert to authenticated with check (user_id = (select auth.uid()));\n\ncreate policy \"messages: authors can delete own\" on public.messages\nfor delete to authenticated using (user_id = (select auth.uid()));\n\n-- Extra conditions using the JWT\n-- only users with a confirmed e-mail:\ncreate policy \"messages: verified e-mail only (example)\" on public.messages\nas restrictive for insert to authenticated\nwith check ((auth.jwt() ->> 'email_confirmed_at') is not null or true);   -- replace \"or true\" to enforce\n\n-- only users who signed in with a specific provider\n--   (auth.jwt() -> 'app_metadata' ->> 'provider') = 'email'\n\n-- Multi-factor: require AAL2 for sensitive tables\n--   (auth.jwt() ->> 'aal') = 'aal2'\n\nselect policyname, roles, cmd from pg_policies where tablename = 'messages';\n", "supabase/06-authorization-and-security/07-test-database-access-with-different-users.js": "/**\n * Experiment: Test database access with different users.\n * Prereq: run 03 (own records) SQL, create TEST_EMAIL and TEST_EMAIL_2 users (05-authentication/02).\n * Run   : node 06-authorization-and-security/07-test-database-access-with-different-users.js\n */\nconst { supabase, signedInClient } = require('../config/supabaseClient');\n\nconst u1 = { email: process.env.TEST_EMAIL || 'student1@example.com', password: process.env.TEST_PASSWORD || 'Passw0rd!123' };\nconst u2 = { email: process.env.TEST_EMAIL_2 || 'student2@example.com', password: process.env.TEST_PASSWORD_2 || 'Passw0rd!123' };\n\n(async () => {\n  const ok = (label, { data, error }) => console.log(`${label.padEnd(46)}`, error ? `BLOCKED (${error.message})` : `allowed -> ${JSON.stringify(data)}`);\n\n  const anon = supabase;                                  // not signed in\n  const c1 = await signedInClient(u1.email, u1.password);\n  const c2 = await signedInClient(u2.email, u2.password);\n\n  // 1. anonymous\n  ok('anon  reads notes', await anon.from('notes').select('id,title'));\n  ok('anon  inserts note', await anon.from('notes').insert({ title: 'anon note' }).select());\n\n  // 2. user 1 creates a note\n  const { data: n1 } = await c1.from('notes').insert({ title: `note by ${u1.email}` }).select().single();\n  console.log('user1 created note id', n1?.id);\n\n  // 3. user 2 tries to see / edit / delete user 1's note\n  ok('user2 reads notes (should not see user1 note)', await c2.from('notes').select('id,title'));\n  ok('user2 updates user1 note', await c2.from('notes').update({ title: 'hacked' }).eq('id', n1.id).select());\n  ok('user2 deletes user1 note', await c2.from('notes').delete().eq('id', n1.id).select());\n\n  // 4. user 1 can see and change own note\n  ok('user1 reads own notes', await c1.from('notes').select('id,title'));\n  ok('user1 updates own note', await c1.from('notes').update({ title: 'edited by owner' }).eq('id', n1.id).select());\n\n  // 5. user tries to insert a row on behalf of another user\n  const { data: { user } } = await c2.auth.getUser();\n  ok('user1 inserts note owned by user2', await c1.from('notes').insert({ title: 'spoof', user_id: user.id }).select());\n\n  // 6. user tries to make himself admin (needs role policies from 04)\n  ok('user1 promotes self to admin', await c1.from('profiles').update({ role: 'admin' }).eq('id', (await c1.auth.getUser()).data.user.id).select());\n\n  // cleanup\n  await c1.from('notes').delete().eq('id', n1.id);\n})().catch((e) => console.error('Test failed:', e.message));\n", "supabase/07-storage/00-storage-setup-and-policies.sql": "-- Run once before the Storage experiments.\n-- Creates two buckets and the access policies (Storage is protected by RLS on storage.objects).\n\ninsert into storage.buckets (id, name, public, file_size_limit, allowed_mime_types)\nvalues\n  ('uploads',       'uploads',       true,  5242880, array['image/png','image/jpeg','image/webp','application/pdf','text/plain']),\n  ('private-files', 'private-files', false, 5242880, null)\non conflict (id) do nothing;\n\n-- PUBLIC bucket \"uploads\": anyone can read, only authenticated users can upload/change/delete\ndrop policy if exists \"uploads: public read\"          on storage.objects;\ndrop policy if exists \"uploads: authenticated insert\" on storage.objects;\ndrop policy if exists \"uploads: owner update\"         on storage.objects;\ndrop policy if exists \"uploads: owner delete\"         on storage.objects;\n\ncreate policy \"uploads: public read\" on storage.objects\nfor select to anon, authenticated using (bucket_id = 'uploads');\n\ncreate policy \"uploads: authenticated insert\" on storage.objects\nfor insert to authenticated with check (bucket_id = 'uploads');\n\ncreate policy \"uploads: owner update\" on storage.objects\nfor update to authenticated using (bucket_id = 'uploads' and owner = (select auth.uid()));\n\ncreate policy \"uploads: owner delete\" on storage.objects\nfor delete to authenticated using (bucket_id = 'uploads' and owner = (select auth.uid()));\n\n-- PRIVATE bucket: each user only touches files inside a folder named after their user id  ->  <uid>/file.ext\ndrop policy if exists \"private: own folder\" on storage.objects;\ncreate policy \"private: own folder\" on storage.objects\nfor all to authenticated\nusing      (bucket_id = 'private-files' and (storage.foldername(name))[1] = (select auth.uid())::text)\nwith check (bucket_id = 'private-files' and (storage.foldername(name))[1] = (select auth.uid())::text);\n", "supabase/07-storage/01-create-storage-bucket.js": "/**\n * Experiment: Create a Supabase Storage bucket.\n * Dashboard way: Storage > New bucket > name, Public on/off, size limit, allowed MIME types.\n * Code way (needs the service_role key): node 07-storage/01-create-storage-bucket.js\n */\nconst { supabaseAdmin } = require('../config/supabaseClient');\nif (!supabaseAdmin) { console.error('SUPABASE_SERVICE_ROLE_KEY required'); process.exit(1); }\n\n(async () => {\n  const { data, error } = await supabaseAdmin.storage.createBucket('lab-bucket', {\n    public: true,                               // files reachable through a public URL\n    fileSizeLimit: 2 * 1024 * 1024,             // 2 MB\n    allowedMimeTypes: ['image/png', 'image/jpeg', 'text/plain'],\n  });\n  console.log('createBucket :', error ? error.message : data);\n\n  const { data: buckets } = await supabaseAdmin.storage.listBuckets();\n  console.log('all buckets  :', buckets.map((b) => `${b.name} (${b.public ? 'public' : 'private'})`));\n\n  console.log('getBucket    :', (await supabaseAdmin.storage.getBucket('lab-bucket')).data);\n  await supabaseAdmin.storage.updateBucket('lab-bucket', { public: false });   // change settings\n  console.log('after update :', (await supabaseAdmin.storage.getBucket('lab-bucket')).data.public ? 'public' : 'private');\n\n  // await supabaseAdmin.storage.emptyBucket('lab-bucket'); await supabaseAdmin.storage.deleteBucket('lab-bucket');\n})();\n", "supabase/07-storage/02-upload-files.js": "/**\n * Experiment: Upload files using Supabase Storage.\n * Prereq: 00-storage-setup-and-policies.sql, a registered TEST_EMAIL user.\n * Run   : node 07-storage/02-upload-files.js\n */\nconst fs = require('fs');\nconst path = require('path');\nconst { signedInClient } = require('../config/supabaseClient');\n\n(async () => {\n  const client = await signedInClient(process.env.TEST_EMAIL || 'student1@example.com', process.env.TEST_PASSWORD || 'Passw0rd!123');\n  const bucket = client.storage.from('uploads');\n\n  // 1. upload a text buffer\n  const up1 = await bucket.upload('demo/hello.txt', Buffer.from('Hello Supabase Storage!'), {\n    contentType: 'text/plain', cacheControl: '3600', upsert: true,          // upsert:true overwrites an existing file\n  });\n  console.log('upload text :', up1.error ? up1.error.message : up1.data);\n\n  // 2. upload a real file from disk\n  const file = path.join(__dirname, 'sample.txt');\n  fs.writeFileSync(file, `Sample created ${new Date().toISOString()}`);\n  const up2 = await bucket.upload(`demo/${Date.now()}-sample.txt`, fs.readFileSync(file), { contentType: 'text/plain' });\n  console.log('upload file :', up2.error ? up2.error.message : up2.data);\n\n  // 3. duplicate name without upsert -> error\n  const up3 = await bucket.upload('demo/hello.txt', Buffer.from('again'), { contentType: 'text/plain' });\n  console.log('duplicate   :', up3.error ? up3.error.message : 'uploaded');\n\n  // Browser: <input type=\"file\" id=\"f\"> ->  bucket.upload('folder/' + file.name, file)\n  fs.unlinkSync(file);\n})();\n", "supabase/07-storage/03-retrieve-files.js": "/**\n * Experiment: Retrieve files from Storage (list, metadata, info).\n * Run: node 07-storage/03-retrieve-files.js   (run 02-upload-files.js first)\n */\nconst { supabase } = require('../config/supabaseClient');\n\n(async () => {\n  const bucket = supabase.storage.from('uploads');          // public bucket -> anon may list\n\n  // list the folder \"demo\"\n  const { data: files, error } = await bucket.list('demo', {\n    limit: 20, offset: 0, sortBy: { column: 'created_at', order: 'desc' },\n  });\n  if (error) return console.error('list failed:', error.message);\n  console.table(files.map((f) => ({ name: f.name, size: f.metadata?.size, type: f.metadata?.mimetype, created: f.created_at })));\n\n  // search by name\n  const { data: found } = await bucket.list('demo', { search: 'hello' });\n  console.log('search \"hello\":', found.map((f) => f.name));\n\n  // does a file exist?\n  const { data: exists } = await bucket.exists('demo/hello.txt');\n  console.log('exists demo/hello.txt:', exists);\n\n  // list root\n  const { data: root } = await bucket.list('');\n  console.log('root entries :', root.map((f) => f.name));\n})();\n", "supabase/07-storage/04-download-files.js": "/**\n * Experiment: Download files from Storage.\n * Run: node 07-storage/04-download-files.js   (needs demo/hello.txt from experiment 02)\n */\nconst fs = require('fs');\nconst path = require('path');\nconst { supabase, signedInClient } = require('../config/supabaseClient');\n\n(async () => {\n  // 1. download(): returns a Blob\n  const { data: blob, error } = await supabase.storage.from('uploads').download('demo/hello.txt');\n  if (error) return console.error('download failed:', error.message);\n  const text = await blob.text();\n  console.log('content :', text);\n\n  // 2. save to disk\n  const out = path.join(__dirname, 'downloaded-hello.txt');\n  fs.writeFileSync(out, Buffer.from(await blob.arrayBuffer()));\n  console.log('saved   :', out);\n\n  // 3. private bucket: create a time-limited signed URL (60 s)\n  const client = await signedInClient(process.env.TEST_EMAIL || 'student1@example.com', process.env.TEST_PASSWORD || 'Passw0rd!123');\n  const { data: { user } } = await client.auth.getUser();\n  const priv = client.storage.from('private-files');\n  await priv.upload(`${user.id}/secret.txt`, Buffer.from('private data'), { contentType: 'text/plain', upsert: true });\n  const { data: signed } = await priv.createSignedUrl(`${user.id}/secret.txt`, 60, { download: true });   // forces browser download\n  console.log('signed URL (60s):', signed.signedUrl);\n})();\n", "supabase/07-storage/05-delete-files.js": "/**\n * Experiment: Delete files from Storage.\n * Run: node 07-storage/05-delete-files.js\n */\nconst { signedInClient } = require('../config/supabaseClient');\n\n(async () => {\n  const client = await signedInClient(process.env.TEST_EMAIL || 'student1@example.com', process.env.TEST_PASSWORD || 'Passw0rd!123');\n  const bucket = client.storage.from('uploads');\n\n  await bucket.upload('trash/a.txt', Buffer.from('a'), { contentType: 'text/plain', upsert: true });\n  await bucket.upload('trash/b.txt', Buffer.from('b'), { contentType: 'text/plain', upsert: true });\n  console.log('before :', (await bucket.list('trash')).data.map((f) => f.name));\n\n  // remove takes an ARRAY of full paths\n  const { data, error } = await bucket.remove(['trash/a.txt', 'trash/b.txt']);\n  console.log('remove :', error ? error.message : data.map((f) => f.name));\n  console.log('after  :', (await bucket.list('trash')).data.map((f) => f.name));\n\n  // \"folders\" are just path prefixes - deleting all files removes the folder\n  // Tip: remove() on a missing path does NOT throw; it returns an empty array.\n})();\n", "supabase/07-storage/06-generate-public-file-urls.js": "/**\n * Experiment: Generate public file URLs.\n * Run: node 07-storage/06-generate-public-file-urls.js\n */\nconst { supabase } = require('../config/supabaseClient');\n\nconst bucket = supabase.storage.from('uploads');\n\n// 1. Public URL (bucket must be public). Purely computed locally - no request is made.\nconst { data } = bucket.getPublicUrl('demo/hello.txt');\nconsole.log('public URL         :', data.publicUrl);\n//   https://<ref>.supabase.co/storage/v1/object/public/uploads/demo/hello.txt\n\n// 2. Forces \"Save as\" in the browser\nconsole.log('download URL       :', bucket.getPublicUrl('demo/hello.txt', { download: 'my-file.txt' }).data.publicUrl);\n\n// 3. Image transformation (resize on the fly; Pro plan feature)\nconsole.log('transformed image  :', bucket.getPublicUrl('photos/pic.jpg', { transform: { width: 300, height: 200, resize: 'cover' } }).data.publicUrl);\n\n// 4. Check that the URL works\n(async () => {\n  const res = await fetch(data.publicUrl);\n  console.log('HTTP status        :', res.status, res.ok ? '(file is reachable)' : '(upload demo/hello.txt first)');\n})();\n// For PRIVATE buckets use createSignedUrl(path, seconds) instead of getPublicUrl.\n", "supabase/07-storage/07-image-upload-with-authentication.html": "<!DOCTYPE html>\n<!--\n  Experiment: Implement image/file upload with authentication.\n  Prereq : 00-storage-setup-and-policies.sql, registered user. Fill the two constants below and open the file.\n  Behaviour: only logged-in users see the upload form; files are stored at  private-files/<user_id>/<name>\n             (private bucket, visible only to the owner) and uploaded avatars go to the public \"uploads\" bucket.\n-->\n<html>\n<head>\n  <meta charset=\"utf-8\"><title>Authenticated Upload</title>\n  <style>\n    body{font-family:Arial;max-width:640px;margin:30px auto;padding:0 12px}\n    input,button{padding:8px;margin:4px 0} .card{border:1px solid #ddd;padding:12px;border-radius:8px;margin-top:14px}\n    img{max-width:120px;border-radius:6px;margin:4px} #msg{color:#b00}\n  </style>\n</head>\n<body>\n<h2>Authenticated file upload</h2><p id=\"msg\"></p>\n\n<div id=\"authBox\" class=\"card\">\n  <input id=\"email\" type=\"email\" placeholder=\"email\"><br><input id=\"password\" type=\"password\" placeholder=\"password\"><br>\n  <button onclick=\"signIn()\">Login</button> <button onclick=\"signUp()\">Register</button>\n</div>\n\n<div id=\"appBox\" class=\"card\" style=\"display:none\">\n  <p>Signed in as <b id=\"who\"></b> <button onclick=\"sb.auth.signOut()\">Logout</button></p>\n  <input type=\"file\" id=\"file\" accept=\"image/*,.pdf,.txt\"> <button onclick=\"upload()\">Upload</button>\n  <h4>My private files</h4><div id=\"list\"></div>\n</div>\n\n<script src=\"https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2\"></script>\n<script>\n  const SUPABASE_URL = 'https://YOUR-PROJECT-REF.supabase.co';\n  const SUPABASE_ANON_KEY = 'YOUR-ANON-KEY';\n  const sb = window.supabase.createClient(SUPABASE_URL, SUPABASE_ANON_KEY);\n  const $ = (id) => document.getElementById(id); let user = null;\n  const say = (t) => ($('msg').textContent = t || '');\n\n  async function signIn(){ const {error}=await sb.auth.signInWithPassword({email:$('email').value,password:$('password').value}); say(error?.message); }\n  async function signUp(){ const {error}=await sb.auth.signUp({email:$('email').value,password:$('password').value}); say(error?error.message:'Registered - now login'); }\n\n  async function upload(){\n    const f = $('file').files[0]; if(!f) return say('Choose a file first');\n    if (f.size > 5*1024*1024) return say('Max size is 5 MB');\n    const path = `${user.id}/${Date.now()}-${f.name.replace(/[^\\w.-]/g,'_')}`;\n    const { error } = await sb.storage.from('private-files').upload(path, f, { contentType: f.type });\n    say(error ? error.message : 'Uploaded!'); loadFiles();\n  }\n\n  async function loadFiles(){\n    const { data, error } = await sb.storage.from('private-files').list(user.id, { sortBy:{column:'created_at',order:'desc'} });\n    if (error) return say(error.message);\n    $('list').innerHTML = '';\n    for (const f of data) {\n      const { data: s } = await sb.storage.from('private-files').createSignedUrl(`${user.id}/${f.name}`, 300);\n      const row = document.createElement('div');\n      row.innerHTML = `<a href=\"${s.signedUrl}\" target=\"_blank\">${f.name}</a> <button>Delete</button>`;\n      row.querySelector('button').onclick = async () => { await sb.storage.from('private-files').remove([`${user.id}/${f.name}`]); loadFiles(); };\n      $('list').appendChild(row);\n    }\n  }\n\n  sb.auth.onAuthStateChange((_e, session) => {\n    user = session?.user || null;\n    $('authBox').style.display = user ? 'none' : 'block'; $('appBox').style.display = user ? 'block' : 'none';\n    if (user) { $('who').textContent = user.email; loadFiles(); }\n  });\n</script>\n</body>\n</html>\n", "supabase/08-realtime/01-enable-supabase-realtime.sql": "-- Experiment: Enable Supabase Realtime\n-- Realtime streams row changes from the Postgres WAL to connected clients.\n-- Dashboard way: Database > Publications > supabase_realtime > toggle tables   (or Table Editor > table menu > Enable Realtime)\n\n-- 1. Add tables to the realtime publication\nalter publication supabase_realtime add table public.students;\nalter publication supabase_realtime add table public.messages;\n\n-- 2. Verify\nselect * from pg_publication_tables where pubname = 'supabase_realtime';\n\n-- 3. Include the OLD row on UPDATE / DELETE events (otherwise only the primary key is sent)\nalter table public.students replica identity full;\nalter table public.messages replica identity full;\n\n-- 4. Remove a table from realtime\n-- alter publication supabase_realtime drop table public.messages;\n\n-- Note: realtime respects RLS - a client only receives changes for rows its SELECT policy allows.\n", "supabase/08-realtime/02-listen-for-insert-events.js": "/**\n * Experiment: Listen for database INSERT events.\n * Prereq: 01-enable-supabase-realtime.sql\n * Run   : node 08-realtime/02-listen-for-insert-events.js\n *         (the script inserts a row itself after subscribing; you can also insert from the dashboard)\n */\nconst { supabase } = require('../config/supabaseClient');\n\nconst channel = supabase\n  .channel('students-inserts')\n  .on('postgres_changes', { event: 'INSERT', schema: 'public', table: 'students' }, (payload) => {\n    console.log('INSERT received:', payload.new);\n  })\n  .subscribe(async (status, err) => {\n    console.log('channel status:', status, err?.message || '');\n    if (status === 'SUBSCRIBED') {\n      const { error } = await supabase.from('students')\n        .insert({ name: 'Realtime Tester', email: `rt${Date.now()}@example.com`, course: 'CSE', age: 20 });\n      if (error) console.error('insert error:', error.message);\n      setTimeout(async () => { await supabase.removeChannel(channel); process.exit(0); }, 3000);\n    }\n  });\n", "supabase/08-realtime/03-listen-for-update-events.js": "/**\n * Experiment: Listen for database UPDATE events.\n * Run: node 08-realtime/03-listen-for-update-events.js\n */\nconst { supabase } = require('../config/supabaseClient');\n\n(async () => {\n  const { data: row, error } = await supabase.from('students')\n    .insert({ name: 'Before Update', email: `up${Date.now()}@example.com`, course: 'CSE', age: 20 }).select().single();\n  if (error) return console.error(error.message);\n\n  const channel = supabase\n    .channel('students-updates')\n    .on('postgres_changes', { event: 'UPDATE', schema: 'public', table: 'students', filter: `id=eq.${row.id}` }, (payload) => {\n      console.log('UPDATE received');\n      console.log('  old:', payload.old);          // full row only if REPLICA IDENTITY FULL\n      console.log('  new:', payload.new);\n    })\n    .subscribe(async (status) => {\n      if (status !== 'SUBSCRIBED') return;\n      await supabase.from('students').update({ name: 'After Update', age: 21 }).eq('id', row.id);\n      setTimeout(async () => {\n        await supabase.from('students').delete().eq('id', row.id);\n        await supabase.removeChannel(channel);\n        process.exit(0);\n      }, 3000);\n    });\n})();\n", "supabase/08-realtime/04-listen-for-delete-events.js": "/**\n * Experiment: Listen for database DELETE events.\n * Run: node 08-realtime/04-listen-for-delete-events.js\n */\nconst { supabase } = require('../config/supabaseClient');\n\n(async () => {\n  const { data: row, error } = await supabase.from('students')\n    .insert({ name: 'To Delete', email: `del${Date.now()}@example.com`, course: 'IT', age: 22 }).select().single();\n  if (error) return console.error(error.message);\n\n  const channel = supabase\n    .channel('students-deletes')\n    .on('postgres_changes', { event: 'DELETE', schema: 'public', table: 'students' }, (payload) => {\n      console.log('DELETE received. Old record:', payload.old);   // id always present; other columns need REPLICA IDENTITY FULL\n    })\n    // one channel can hold many listeners; event '*' catches INSERT, UPDATE and DELETE\n    .on('postgres_changes', { event: '*', schema: 'public', table: 'students' }, (p) => console.log(`[any] ${p.eventType}`))\n    .subscribe(async (status) => {\n      if (status !== 'SUBSCRIBED') return;\n      await supabase.from('students').delete().eq('id', row.id);\n      setTimeout(async () => { await supabase.removeChannel(channel); process.exit(0); }, 3000);\n    });\n})();\n", "supabase/08-realtime/05-realtime-chat-application.html": "<!DOCTYPE html>\n<!--\n  Experiment: Create a realtime chat application.\n  Prereq : schema.sql (messages table) + 01-enable-supabase-realtime.sql. Fill in the two constants and open in TWO browser tabs.\n  Note   : uses the open policies from schema.sql. After running 06-authorization/06-*.sql, posting requires login (see project 4).\n-->\n<html>\n<head>\n  <meta charset=\"utf-8\"><title>Realtime Chat</title>\n  <style>\n    body{font-family:Arial;background:#eef2f7;display:flex;justify-content:center}\n    #chat{width:460px;margin-top:24px;background:#fff;border-radius:10px;box-shadow:0 2px 8px #0002;display:flex;flex-direction:column;height:80vh}\n    header{padding:12px;background:#3b82f6;color:#fff;border-radius:10px 10px 0 0}\n    #log{flex:1;overflow:auto;padding:10px}\n    .m{margin:6px 0;padding:8px 10px;border-radius:10px;background:#f1f5f9;max-width:80%}\n    .me{background:#dbeafe;margin-left:auto}.m small{color:#666;display:block}\n    footer{display:flex;padding:8px;border-top:1px solid #ddd}footer input{flex:1;padding:8px}footer button{padding:8px 14px}\n  </style>\n</head>\n<body>\n<div id=\"chat\">\n  <header>Realtime Chat - <span id=\"status\">connecting...</span></header>\n  <div id=\"log\"></div>\n  <footer><input id=\"text\" placeholder=\"Type a message...\" onkeydown=\"if(event.key==='Enter')send()\"><button onclick=\"send()\">Send</button></footer>\n</div>\n<script src=\"https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2\"></script>\n<script>\n  const SUPABASE_URL = 'https://YOUR-PROJECT-REF.supabase.co';\n  const SUPABASE_ANON_KEY = 'YOUR-ANON-KEY';\n  const sb = window.supabase.createClient(SUPABASE_URL, SUPABASE_ANON_KEY);\n  const username = prompt('Your name?') || 'guest-' + Math.floor(Math.random() * 1000);\n  const $ = (id) => document.getElementById(id);\n\n  function render(m) {\n    const d = document.createElement('div');\n    d.className = 'm' + (m.username === username ? ' me' : '');\n    d.innerHTML = `<small>${m.username} - ${new Date(m.created_at).toLocaleTimeString()}</small>`;\n    d.appendChild(document.createTextNode(m.content));          // textContent => prevents XSS\n    $('log').appendChild(d); $('log').scrollTop = $('log').scrollHeight;\n  }\n\n  async function send() {\n    const content = $('text').value.trim(); if (!content) return;\n    $('text').value = '';\n    const { error } = await sb.from('messages').insert({ username, content });\n    if (error) alert(error.message);\n  }\n\n  (async () => {\n    const { data } = await sb.from('messages').select('*').order('created_at', { ascending: false }).limit(50);\n    (data || []).reverse().forEach(render);                      // history\n    sb.channel('chat-room')\n      .on('postgres_changes', { event: 'INSERT', schema: 'public', table: 'messages' }, (p) => render(p.new))   // live\n      .subscribe((s) => ($('status').textContent = s === 'SUBSCRIBED' ? 'online' : s.toLowerCase()));\n  })();\n</script>\n</body>\n</html>\n", "supabase/08-realtime/06-realtime-student-record-application.html": "<!DOCTYPE html>\n<!--\n  Experiment: Create a realtime student record application.\n  Open this page in two tabs / two computers: adding, editing or deleting a student in one appears instantly in the other.\n  Prereq: schema.sql + 01-enable-supabase-realtime.sql. Fill in the two constants below.\n-->\n<html>\n<head>\n  <meta charset=\"utf-8\"><title>Realtime Students</title>\n  <style>\n    body{font-family:Arial;max-width:820px;margin:30px auto;padding:0 12px}\n    input{padding:6px;margin:3px}button{padding:6px 12px;cursor:pointer}\n    table{width:100%;border-collapse:collapse;margin-top:14px}th,td{border:1px solid #ccc;padding:6px;text-align:left}th{background:#eef}\n    .flash{animation:f 1.2s}@keyframes f{from{background:#fef08a}to{background:transparent}}#live{color:green}\n  </style>\n</head>\n<body>\n<h2>Student Records <small id=\"live\">(connecting...)</small></h2>\n<div>\n  <input id=\"name\" placeholder=\"name\"><input id=\"email\" placeholder=\"email\"><input id=\"course\" placeholder=\"course\"><input id=\"age\" type=\"number\" placeholder=\"age\">\n  <button onclick=\"add()\">Add</button>\n</div>\n<table><thead><tr><th>ID</th><th>Name</th><th>Email</th><th>Course</th><th>Age</th><th></th></tr></thead><tbody id=\"rows\"></tbody></table>\n\n<script src=\"https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2\"></script>\n<script>\n  const SUPABASE_URL = 'https://YOUR-PROJECT-REF.supabase.co';\n  const SUPABASE_ANON_KEY = 'YOUR-ANON-KEY';\n  const sb = window.supabase.createClient(SUPABASE_URL, SUPABASE_ANON_KEY);\n  const $ = (id) => document.getElementById(id);\n\n  function rowHtml(s) {\n    const esc = (v) => String(v ?? '').replace(/[&<>\"]/g, (c) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '\"': '&quot;' }[c]));\n    return `<td>${s.id}</td><td>${esc(s.name)}</td><td>${esc(s.email)}</td><td>${esc(s.course)}</td><td>${s.age ?? ''}</td>\n            <td><button onclick=\"edit(${s.id})\">Edit</button> <button onclick=\"del(${s.id})\">Delete</button></td>`;\n  }\n  function upsertRow(s) {\n    let tr = $('r' + s.id);\n    if (!tr) { tr = document.createElement('tr'); tr.id = 'r' + s.id; $('rows').appendChild(tr); }\n    tr.innerHTML = rowHtml(s); tr.className = ''; void tr.offsetWidth; tr.className = 'flash';\n  }\n\n  async function add() {\n    const { error } = await sb.from('students').insert({ name: $('name').value, email: $('email').value, course: $('course').value, age: +$('age').value || null });\n    if (error) alert(error.message);\n  }\n  async function edit(id) {\n    const name = prompt('New name?'); if (!name) return;\n    const { error } = await sb.from('students').update({ name }).eq('id', id); if (error) alert(error.message);\n  }\n  async function del(id) { if (confirm('Delete?')) { const { error } = await sb.from('students').delete().eq('id', id); if (error) alert(error.message); } }\n\n  (async () => {\n    const { data } = await sb.from('students').select('*').order('id'); (data || []).forEach(upsertRow);\n    sb.channel('students-live')\n      .on('postgres_changes', { event: 'INSERT', schema: 'public', table: 'students' }, (p) => upsertRow(p.new))\n      .on('postgres_changes', { event: 'UPDATE', schema: 'public', table: 'students' }, (p) => upsertRow(p.new))\n      .on('postgres_changes', { event: 'DELETE', schema: 'public', table: 'students' }, (p) => $('r' + p.old.id)?.remove())\n      .subscribe((s) => ($('live').textContent = s === 'SUBSCRIBED' ? '(live)' : '(' + s.toLowerCase() + ')'));\n  })();\n</script>\n</body>\n</html>\n", "supabase/09-edge-functions/00-edge-functions-setup.md": "# Edge Functions - one-time setup\n\nEdge Functions are server-side **TypeScript/Deno** functions that run close to your users.\n\n```bash\n# 1. Install the Supabase CLI (any one)\nnpm install -g supabase            # or: brew install supabase/tap/supabase\nsupabase --version\n\n# 2. Log in and link this folder to your project\nsupabase login\nsupabase init                      # creates ./supabase/ (run in a working folder)\nsupabase link --project-ref YOUR-PROJECT-REF\n\n# 3. Copy a function into the CLI layout:   supabase/functions/<name>/index.ts\n#    Example: cp -r 09-edge-functions/01-create-edge-function/hello-world  <workdir>/supabase/functions/\n```\n\nLocal testing needs **Docker**: `supabase start` then `supabase functions serve <name>`.\nEach folder in this directory is one experiment and contains a ready `index.ts`.\n", "supabase/09-edge-functions/01-create-edge-function/hello-world/index.ts": "// Experiment: Create a Supabase Edge Function.\n// Create : supabase functions new hello-world      (then paste this into supabase/functions/hello-world/index.ts)\n// Serve  : supabase functions serve hello-world    -> http://localhost:54321/functions/v1/hello-world\n// Test   : curl http://localhost:54321/functions/v1/hello-world -H \"Authorization: Bearer <ANON_KEY>\"\n\nDeno.serve((_req: Request) => {\n  const body = JSON.stringify({\n    message: \"Hello from a Supabase Edge Function!\",\n    runtime: `Deno ${Deno.version.deno}`,\n    time: new Date().toISOString(),\n  });\n  return new Response(body, { headers: { \"Content-Type\": \"application/json\" } });\n});\n", "supabase/09-edge-functions/02-deploy-edge-function/deploy-guide.md": "# Experiment: Deploy an Edge Function\n\n```bash\n# 1. Make sure the function exists at supabase/functions/hello-world/index.ts\nsupabase functions deploy hello-world              # deploy one function\nsupabase functions deploy                          # deploy ALL functions\nsupabase functions deploy hello-world --no-verify-jwt   # allow calls WITHOUT a login token (public webhooks)\n\n# 2. List and inspect\nsupabase functions list\n\n# 3. Secrets (environment variables)\nsupabase secrets set MY_API_KEY=abc123\nsupabase secrets list\n\n# 4. Call the live function\ncurl -i https://YOUR-PROJECT-REF.supabase.co/functions/v1/hello-world \\\n  -H \"Authorization: Bearer YOUR-ANON-KEY\"\n\n# 5. Logs: Dashboard > Edge Functions > hello-world > Logs   (or Invocations tab)\n```\n\n## From the browser / Node\n```js\nconst { data, error } = await supabase.functions.invoke('hello-world');\n```\n\n## Notes\n- `SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY` are injected automatically.\n- Functions verify the JWT by default; `--no-verify-jwt` disables that.\n- No CLI? Dashboard > Edge Functions > **Deploy a new function** > paste code in the editor (or use the AI assistant).\n", "supabase/09-edge-functions/03-pass-data-to-edge-function/greet/index.ts": "// Experiment: Pass data to an Edge Function (query string, JSON body, headers).\n// Deploy : supabase functions deploy greet\n// Test   : curl -X POST \"https://<ref>.supabase.co/functions/v1/greet?lang=en\" \\\n//            -H \"Authorization: Bearer <ANON_KEY>\" -H \"Content-Type: application/json\" \\\n//            -H \"x-client: curl\" -d '{\"name\":\"Asha\",\"age\":20}'\n// JS     : supabase.functions.invoke('greet', { body: { name: 'Asha', age: 20 } })\n\nconst corsHeaders = {\n  \"Access-Control-Allow-Origin\": \"*\",\n  \"Access-Control-Allow-Headers\": \"authorization, x-client-info, apikey, content-type, x-client\",\n};\n\nDeno.serve(async (req: Request) => {\n  if (req.method === \"OPTIONS\") return new Response(\"ok\", { headers: corsHeaders });   // browser pre-flight\n\n  const url = new URL(req.url);\n  const lang = url.searchParams.get(\"lang\") ?? \"en\";           // 1. query string\n  const client = req.headers.get(\"x-client\") ?? \"unknown\";      // 2. header\n\n  let body: { name?: string; age?: number } = {};               // 3. JSON body\n  try { body = await req.json(); } catch { /* no body */ }\n\n  if (!body.name) {\n    return new Response(JSON.stringify({ error: \"name is required\" }), {\n      status: 400, headers: { ...corsHeaders, \"Content-Type\": \"application/json\" },\n    });\n  }\n\n  const greeting = lang === \"ta\" ? \"Vanakkam\" : \"Hello\";\n  return new Response(\n    JSON.stringify({ message: `${greeting}, ${body.name}!`, age: body.age ?? null, lang, client }),\n    { headers: { ...corsHeaders, \"Content-Type\": \"application/json\" } },\n  );\n});\n", "supabase/09-edge-functions/04-return-json-responses/json-api/index.ts": "// Experiment: Return JSON responses from an Edge Function (status codes, headers, routing).\n// Deploy : supabase functions deploy json-api --no-verify-jwt\n// Test   : curl https://<ref>.supabase.co/functions/v1/json-api/products\n//          curl https://<ref>.supabase.co/functions/v1/json-api/products/2\n//          curl https://<ref>.supabase.co/functions/v1/json-api/unknown      (404)\n\nconst json = (data: unknown, status = 200) =>\n  new Response(JSON.stringify(data), {\n    status,\n    headers: { \"Content-Type\": \"application/json\", \"Access-Control-Allow-Origin\": \"*\", \"Cache-Control\": \"no-store\" },\n  });\n\nconst products = [\n  { id: 1, name: \"Laptop\", price: 55000 },\n  { id: 2, name: \"Phone\", price: 20000 },\n];\n\nDeno.serve((req: Request) => {\n  const { pathname } = new URL(req.url);\n  const path = pathname.replace(/^\\/(functions\\/v1\\/)?json-api/, \"\") || \"/\";\n\n  if (req.method === \"GET\" && path === \"/\") return json({ service: \"json-api\", endpoints: [\"/products\", \"/products/:id\"] });\n  if (req.method === \"GET\" && path === \"/products\") return json({ count: products.length, data: products });\n\n  const m = path.match(/^\\/products\\/(\\d+)$/);\n  if (req.method === \"GET\" && m) {\n    const item = products.find((p) => p.id === Number(m[1]));\n    return item ? json(item) : json({ error: \"Product not found\" }, 404);\n  }\n  return json({ error: \"Not found\", path }, 404);\n});\n", "supabase/09-edge-functions/05-authentication-inside-edge-function/whoami/index.ts": "// Experiment: Implement authentication inside an Edge Function.\n// The caller sends the user's access token in the Authorization header; the function verifies it and reads the user.\n// Deploy : supabase functions deploy whoami          (JWT verification stays ON)\n// JS     : const { data } = await supabase.functions.invoke('whoami')   // token is added automatically when signed in\n// Test   : curl https://<ref>.supabase.co/functions/v1/whoami -H \"Authorization: Bearer <USER_ACCESS_TOKEN>\"\nimport { createClient } from \"npm:@supabase/supabase-js@2\";\n\nconst cors = {\n  \"Access-Control-Allow-Origin\": \"*\",\n  \"Access-Control-Allow-Headers\": \"authorization, x-client-info, apikey, content-type\",\n};\nconst reply = (data: unknown, status = 200) =>\n  new Response(JSON.stringify(data), { status, headers: { ...cors, \"Content-Type\": \"application/json\" } });\n\nDeno.serve(async (req: Request) => {\n  if (req.method === \"OPTIONS\") return new Response(\"ok\", { headers: cors });\n\n  const authHeader = req.headers.get(\"Authorization\");\n  if (!authHeader) return reply({ error: \"Missing Authorization header\" }, 401);\n\n  // client that acts AS the caller (RLS applies)\n  const supabase = createClient(\n    Deno.env.get(\"SUPABASE_URL\")!,\n    Deno.env.get(\"SUPABASE_ANON_KEY\")!,\n    { global: { headers: { Authorization: authHeader } } },\n  );\n\n  const { data: { user }, error } = await supabase.auth.getUser();\n  if (error || !user) return reply({ error: \"Invalid or expired token\" }, 401);\n\n  const { data: profile } = await supabase.from(\"profiles\").select(\"full_name, role\").eq(\"id\", user.id).single();\n\n  // simple role check inside the function\n  if (new URL(req.url).searchParams.get(\"admin\") === \"1\" && profile?.role !== \"admin\") {\n    return reply({ error: \"Admins only\" }, 403);\n  }\n\n  return reply({ id: user.id, email: user.email, profile });\n});\n", "supabase/09-edge-functions/06-connect-edge-function-with-database/students-api/index.ts": "// Experiment: Connect an Edge Function with a Supabase database.\n// GET  -> list students (as the caller: RLS applies)      POST -> insert {name,email,course,age}\n// GET ?stats=1 -> uses the service-role client for an admin-style aggregate\n// Deploy : supabase functions deploy students-api\n// JS     : supabase.functions.invoke('students-api', { method: 'POST', body: {...} })\nimport { createClient } from \"npm:@supabase/supabase-js@2\";\n\nconst cors = {\n  \"Access-Control-Allow-Origin\": \"*\",\n  \"Access-Control-Allow-Headers\": \"authorization, x-client-info, apikey, content-type\",\n};\nconst reply = (data: unknown, status = 200) =>\n  new Response(JSON.stringify(data), { status, headers: { ...cors, \"Content-Type\": \"application/json\" } });\n\nDeno.serve(async (req: Request) => {\n  if (req.method === \"OPTIONS\") return new Response(\"ok\", { headers: cors });\n\n  // 1) client that carries the caller's JWT -> respects RLS\n  const userClient = createClient(Deno.env.get(\"SUPABASE_URL\")!, Deno.env.get(\"SUPABASE_ANON_KEY\")!, {\n    global: { headers: { Authorization: req.headers.get(\"Authorization\") ?? \"\" } },\n  });\n\n  const url = new URL(req.url);\n\n  if (req.method === \"GET\" && url.searchParams.get(\"stats\") === \"1\") {\n    // 2) service-role client -> bypasses RLS (keep this logic server-side only!)\n    const admin = createClient(Deno.env.get(\"SUPABASE_URL\")!, Deno.env.get(\"SUPABASE_SERVICE_ROLE_KEY\")!);\n    const { count, error } = await admin.from(\"students\").select(\"*\", { count: \"exact\", head: true });\n    return error ? reply({ error: error.message }, 500) : reply({ totalStudents: count });\n  }\n\n  if (req.method === \"GET\") {\n    const { data, error } = await userClient.from(\"students\").select(\"*\").order(\"id\").limit(50);\n    return error ? reply({ error: error.message }, 400) : reply(data);\n  }\n\n  if (req.method === \"POST\") {\n    const body = await req.json().catch(() => null);\n    if (!body?.name || !body?.email || !body?.course) return reply({ error: \"name, email and course are required\" }, 400);\n    const { data, error } = await userClient.from(\"students\").insert(body).select().single();\n    return error ? reply({ error: error.message }, 400) : reply(data, 201);\n  }\n\n  return reply({ error: \"Method not allowed\" }, 405);\n});\n", "supabase/10-rest-api/01-access-tables-through-generated-rest-api.js": "/**\n * Experiment: Access Supabase tables through the generated REST API (PostgREST).\n * Every table/view in the \"public\" schema is exposed at  https://<ref>.supabase.co/rest/v1/<table>\n * Required headers:  apikey: <anon key>   and   Authorization: Bearer <anon key or user JWT>\n * Run: node 10-rest-api/01-access-tables-through-generated-rest-api.js\n */\nconst { rest, BASE } = require('../config/rest');\n\n(async () => {\n  console.log('Base URL:', BASE);\n\n  // 1. Auto-generated OpenAPI description of ALL tables\n  const spec = await rest('GET', '/');\n  console.log('Tables exposed :', Object.keys(spec.data?.definitions || spec.data?.components?.schemas || {}));\n\n  // 2. Read a table\n  const r = await rest('GET', '/students?select=id,name,course&limit=3');\n  console.log('GET /students  :', r.status, r.data);\n\n  // 3. Equivalent curl\n  console.log(`\\ncurl \"${BASE}/students?select=*&limit=3\" \\\\\\n  -H \"apikey: $SUPABASE_ANON_KEY\" -H \"Authorization: Bearer $SUPABASE_ANON_KEY\"`);\n\n  // 4. Without keys the API refuses\n  const noKey = await fetch(`${BASE}/students`);\n  console.log('\\nwithout apikey :', noKey.status);\n})();\n", "supabase/10-rest-api/02-get-operations.js": "/**\n * Experiment: Implement GET operations using the Supabase REST API.\n * Run: node 10-rest-api/02-get-operations.js\n */\nconst { rest } = require('../config/rest');\n\n(async () => {\n  const show = async (label, path) => { const r = await rest('GET', path); console.log(label.padEnd(20), r.status, JSON.stringify(r.data).slice(0, 150)); };\n\n  await show('all rows',          '/students');\n  await show('choose columns',    '/students?select=id,name');\n  await show('by id',             '/students?id=eq.1');\n  await show('single object',     '/students?id=eq.1&select=name');      // still an array; add header Accept: application/vnd.pgrst.object+json for an object\n  await show('filter eq',         '/students?course=eq.CSE');\n  await show('filter gt',         '/students?age=gt.20');\n  await show('in list',           '/students?course=in.(CSE,IT)');\n  await show('pattern (ilike)',   '/students?name=ilike.*a*');\n  await show('order',             '/students?order=age.desc');\n  await show('rename column',     '/students?select=student:name,years:age');\n\n  const single = await rest('GET', '/students?id=eq.1', { extra: { Accept: 'application/vnd.pgrst.object+json' } });\n  console.log('as one object'.padEnd(20), single.status, single.data);\n})();\n", "supabase/10-rest-api/03-post-operations.js": "/**\n * Experiment: Implement POST operations using the Supabase REST API.\n * Run: node 10-rest-api/03-post-operations.js\n */\nconst { rest } = require('../config/rest');\n\n(async () => {\n  const stamp = Date.now();\n\n  // 1. Insert one row. \"Prefer: return=representation\" returns the created row (default returns nothing, 201)\n  const one = await rest('POST', '/students', {\n    body: { name: 'REST Asha', email: `rest${stamp}@example.com`, course: 'CSE', age: 20 },\n    prefer: 'return=representation',\n  });\n  console.log('insert one   :', one.status, one.data);\n\n  // 2. Bulk insert = send an array\n  const many = await rest('POST', '/students', {\n    body: [\n      { name: 'REST Ravi', email: `rr${stamp}@example.com`, course: 'ECE', age: 21 },\n      { name: 'REST Meena', email: `rm${stamp}@example.com`, course: 'IT', age: 19 },\n    ],\n    prefer: 'return=representation',\n  });\n  console.log('bulk insert  :', many.status, many.data.map((r) => r.id));\n\n  // 3. Upsert on the unique email\n  const up = await rest('POST', '/students?on_conflict=email', {\n    body: { name: 'REST Asha v2', email: one.data[0].email, course: 'CSE', age: 22 },\n    prefer: 'resolution=merge-duplicates,return=representation',\n  });\n  console.log('upsert       :', up.status, up.data[0].name);\n\n  // 4. Invalid data -> 400/409 with a Postgres error code\n  const bad = await rest('POST', '/students', { body: { name: 'X', email: 'x@x.com', course: 'CSE', age: 3 } });\n  console.log('check fails  :', bad.status, bad.data.message);\n\n  // 5. Call a database function (RPC) - POST /rpc/<function>\n  //    create function public.add_numbers(a int, b int) returns int language sql as $$ select a + b $$;\n  const rpc = await rest('POST', '/rpc/add_numbers', { body: { a: 2, b: 3 } });\n  console.log('rpc          :', rpc.status, rpc.data);\n})();\n", "supabase/10-rest-api/04-patch-update-operations.js": "/**\n * Experiment: Implement PATCH / UPDATE operations using the Supabase REST API.\n * PATCH changes only the columns sent, for every row matching the filter in the query string.\n * Run: node 10-rest-api/04-patch-update-operations.js\n */\nconst { rest } = require('../config/rest');\n\n(async () => {\n  const email = `patch${Date.now()}@example.com`;\n  const created = await rest('POST', '/students', { body: { name: 'Patch Me', email, course: 'CSE', age: 20 }, prefer: 'return=representation' });\n  const id = created.data[0].id;\n\n  // update one row\n  const one = await rest('PATCH', `/students?id=eq.${id}`, { body: { name: 'Patched', age: 21 }, prefer: 'return=representation' });\n  console.log('patch one    :', one.status, one.data);\n\n  // update many rows (filter matches several)\n  const many = await rest('PATCH', '/students?course=eq.CSE&age=lt.18', { body: { course: 'Junior' }, prefer: 'return=representation' });\n  console.log('patch many   :', many.status, `${many.data.length} row(s)`);\n\n  // no match => 200 with []\n  const none = await rest('PATCH', '/students?id=eq.-1', { body: { age: 30 }, prefer: 'return=representation' });\n  console.log('no match     :', none.status, none.data);\n\n  // A PATCH with NO filter would touch every row - PostgREST/RLS should prevent it in production\n  await rest('DELETE', `/students?id=eq.${id}`);\n})();\n", "supabase/10-rest-api/05-delete-operations.js": "/**\n * Experiment: Implement DELETE operations using the Supabase REST API.\n * Run: node 10-rest-api/05-delete-operations.js\n */\nconst { rest } = require('../config/rest');\n\n(async () => {\n  const stamp = Date.now();\n  const made = await rest('POST', '/students', {\n    body: [\n      { name: 'Del A', email: `da${stamp}@example.com`, course: 'TMP', age: 20 },\n      { name: 'Del B', email: `db${stamp}@example.com`, course: 'TMP', age: 21 },\n      { name: 'Del C', email: `dc${stamp}@example.com`, course: 'TMP', age: 22 },\n    ],\n    prefer: 'return=representation',\n  });\n  const ids = made.data.map((r) => r.id);\n\n  // delete one row (204 No Content by default)\n  const one = await rest('DELETE', `/students?id=eq.${ids[0]}`);\n  console.log('delete one   :', one.status);\n\n  // delete and return the removed row\n  const ret = await rest('DELETE', `/students?id=eq.${ids[1]}`, { prefer: 'return=representation' });\n  console.log('delete + rows:', ret.status, ret.data);\n\n  // delete several with a filter\n  const many = await rest('DELETE', '/students?course=eq.TMP', { prefer: 'return=representation' });\n  console.log('delete many  :', many.status, `${many.data.length} row(s)`);\n\n  // an unfiltered DELETE would remove everything - never do that without a filter\n})();\n", "supabase/10-rest-api/06-api-filtering-and-pagination.js": "/**\n * Experiment: Implement API filtering and pagination.\n * Run: node 10-rest-api/06-api-filtering-and-pagination.js\n */\nconst { rest } = require('../config/rest');\n\n(async () => {\n  // ---- FILTER OPERATORS: eq neq gt gte lt lte like ilike in is not.<op> or() and() cs cd fts ----\n  const f = [\n    ['course = CSE',            '/students?course=eq.CSE'],\n    ['age between 19 and 22',   '/students?age=gte.19&age=lte.22'],\n    ['course in (CSE,IT)',      '/students?course=in.(CSE,IT)'],\n    ['course not CSE',          '/students?course=neq.CSE'],\n    ['name starts with A',      '/students?name=like.A*'],\n    ['user_id is null',         '/students?user_id=is.null'],\n    ['OR condition',            '/students?or=(age.gte.22,course.eq.IT)'],\n    ['NOT (age < 20)',          '/students?age=not.lt.20'],\n  ];\n  for (const [label, path] of f) console.log(label.padEnd(24), '->', (await rest('GET', path)).data?.length, 'row(s)');\n\n  // ---- SORT + LIMIT / OFFSET ----\n  const page = 2, size = 3;\n  const p = await rest('GET', `/students?select=id,name&order=id.asc&limit=${size}&offset=${(page - 1) * size}`);\n  console.log(`\\npage ${page} (limit/offset):`, p.data);\n\n  // ---- TOTAL COUNT via headers: Prefer count=exact => Content-Range: 0-2/17 ----\n  const c = await rest('GET', '/students?select=id&limit=3', { prefer: 'count=exact' });\n  console.log('Content-Range      :', c.contentRange);\n  const total = Number(c.contentRange.split('/')[1]);\n  console.log(`total ${total} rows -> ${Math.ceil(total / size)} pages of ${size}`);\n\n  // ---- RANGE header alternative ----\n  const r = await rest('GET', '/students?select=id,name&order=id', { extra: { Range: '0-2', 'Range-Unit': 'items' }, prefer: 'count=exact' });\n  console.log('Range: 0-2         :', r.status, r.data.length, 'rows');\n})();\n", "supabase/10-rest-api/07-supabase-rest.postman_collection.json": "{\n  \"info\": { \"name\": \"Supabase REST API - students\", \"schema\": \"https://schema.getpostman.com/json/collection/v2.1.0/collection.json\" },\n  \"variable\": [\n    { \"key\": \"baseUrl\", \"value\": \"https://YOUR-PROJECT-REF.supabase.co\" },\n    { \"key\": \"anonKey\", \"value\": \"YOUR-ANON-KEY\" },\n    { \"key\": \"accessToken\", \"value\": \"\" },\n    { \"key\": \"studentId\", \"value\": \"\" },\n    { \"key\": \"email\", \"value\": \"student1@example.com\" },\n    { \"key\": \"password\", \"value\": \"Passw0rd!123\" }\n  ],\n  \"item\": [\n    {\n      \"name\": \"Auth - login (get access token)\",\n      \"request\": {\n        \"method\": \"POST\",\n        \"header\": [{ \"key\": \"apikey\", \"value\": \"{{anonKey}}\" }, { \"key\": \"Content-Type\", \"value\": \"application/json\" }],\n        \"body\": { \"mode\": \"raw\", \"raw\": \"{\\\"email\\\":\\\"{{email}}\\\",\\\"password\\\":\\\"{{password}}\\\"}\" },\n        \"url\": \"{{baseUrl}}/auth/v1/token?grant_type=password\"\n      },\n      \"event\": [{ \"listen\": \"test\", \"script\": { \"type\": \"text/javascript\", \"exec\": [\n        \"pm.test('Login 200', () => pm.response.to.have.status(200));\",\n        \"pm.collectionVariables.set('accessToken', pm.response.json().access_token);\"\n      ] } }]\n    },\n    {\n      \"name\": \"GET students\",\n      \"request\": {\n        \"method\": \"GET\",\n        \"header\": [{ \"key\": \"apikey\", \"value\": \"{{anonKey}}\" }, { \"key\": \"Authorization\", \"value\": \"Bearer {{accessToken}}\" }],\n        \"url\": \"{{baseUrl}}/rest/v1/students?select=*&order=id.desc&limit=5\"\n      },\n      \"event\": [{ \"listen\": \"test\", \"script\": { \"type\": \"text/javascript\", \"exec\": [\n        \"pm.test('200 OK', () => pm.response.to.have.status(200));\",\n        \"pm.test('Array returned', () => pm.expect(pm.response.json()).to.be.an('array'));\"\n      ] } }]\n    },\n    {\n      \"name\": \"POST student\",\n      \"request\": {\n        \"method\": \"POST\",\n        \"header\": [\n          { \"key\": \"apikey\", \"value\": \"{{anonKey}}\" }, { \"key\": \"Authorization\", \"value\": \"Bearer {{accessToken}}\" },\n          { \"key\": \"Content-Type\", \"value\": \"application/json\" }, { \"key\": \"Prefer\", \"value\": \"return=representation\" }\n        ],\n        \"body\": { \"mode\": \"raw\", \"raw\": \"{\\\"name\\\":\\\"Postman User\\\",\\\"email\\\":\\\"pm{{$timestamp}}@example.com\\\",\\\"course\\\":\\\"CSE\\\",\\\"age\\\":21}\" },\n        \"url\": \"{{baseUrl}}/rest/v1/students\"\n      },\n      \"event\": [{ \"listen\": \"test\", \"script\": { \"type\": \"text/javascript\", \"exec\": [\n        \"pm.test('201 Created', () => pm.response.to.have.status(201));\",\n        \"pm.collectionVariables.set('studentId', pm.response.json()[0].id);\"\n      ] } }]\n    },\n    {\n      \"name\": \"PATCH student\",\n      \"request\": {\n        \"method\": \"PATCH\",\n        \"header\": [\n          { \"key\": \"apikey\", \"value\": \"{{anonKey}}\" }, { \"key\": \"Authorization\", \"value\": \"Bearer {{accessToken}}\" },\n          { \"key\": \"Content-Type\", \"value\": \"application/json\" }, { \"key\": \"Prefer\", \"value\": \"return=representation\" }\n        ],\n        \"body\": { \"mode\": \"raw\", \"raw\": \"{\\\"age\\\":22}\" },\n        \"url\": \"{{baseUrl}}/rest/v1/students?id=eq.{{studentId}}\"\n      },\n      \"event\": [{ \"listen\": \"test\", \"script\": { \"type\": \"text/javascript\", \"exec\": [\n        \"pm.test('200 OK', () => pm.response.to.have.status(200));\",\n        \"pm.test('age updated', () => pm.expect(pm.response.json()[0].age).to.eql(22));\"\n      ] } }]\n    },\n    {\n      \"name\": \"DELETE student\",\n      \"request\": {\n        \"method\": \"DELETE\",\n        \"header\": [{ \"key\": \"apikey\", \"value\": \"{{anonKey}}\" }, { \"key\": \"Authorization\", \"value\": \"Bearer {{accessToken}}\" }],\n        \"url\": \"{{baseUrl}}/rest/v1/students?id=eq.{{studentId}}\"\n      },\n      \"event\": [{ \"listen\": \"test\", \"script\": { \"type\": \"text/javascript\", \"exec\": [\n        \"pm.test('204 No Content', () => pm.response.to.have.status(204));\"\n      ] } }]\n    },\n    {\n      \"name\": \"GET with count (pagination)\",\n      \"request\": {\n        \"method\": \"GET\",\n        \"header\": [\n          { \"key\": \"apikey\", \"value\": \"{{anonKey}}\" }, { \"key\": \"Authorization\", \"value\": \"Bearer {{accessToken}}\" },\n          { \"key\": \"Prefer\", \"value\": \"count=exact\" }\n        ],\n        \"url\": \"{{baseUrl}}/rest/v1/students?select=id&limit=3&offset=0\"\n      },\n      \"event\": [{ \"listen\": \"test\", \"script\": { \"type\": \"text/javascript\", \"exec\": [\n        \"pm.test('Content-Range header present', () => pm.expect(pm.response.headers.get('Content-Range')).to.be.ok);\"\n      ] } }]\n    }\n  ]\n}\n", "supabase/10-rest-api/07-test-rest-api-with-postman.md": "# Experiment: Test Supabase REST APIs using Postman\n\n1. Import `07-supabase-rest.postman_collection.json` (Postman > Import).\n2. Open the collection **Variables** tab and set:\n   - `baseUrl` = `https://YOUR-PROJECT-REF.supabase.co`\n   - `anonKey` = your anon key\n   - `accessToken` = (optional) `access_token` from a login response - needed once RLS restricts a table\n3. Run the requests in order: **Login -> GET -> POST -> PATCH -> DELETE**. Test scripts assert status codes and store the created `id`.\n4. Use **Runner** (Collection > Run) to run everything automatically.\n\n## Headers every request needs\n| Header | Value |\n|---|---|\n| `apikey` | `{{anonKey}}` |\n| `Authorization` | `Bearer {{anonKey}}` (or `Bearer {{accessToken}}`) |\n| `Content-Type` | `application/json` |\n| `Prefer` | `return=representation` (return the changed row) |\n\n## Things to observe\n- **Status:** `200` read, `201` created, `204` deleted, `400` bad data, `401` bad key, `403/42501` blocked by RLS, `404` unknown table.\n- **Response headers:** `Content-Range` gives the total row count when `Prefer: count=exact`.\n- **Time / size** tabs show latency and payload.\n", "supabase/11-project-based-experiments/01-student-management-system.html": "<!DOCTYPE html>\n<!--\n  Project: Student Management System using Supabase\n  Features: add / edit / delete, search, sort, pagination, form validation.\n  Setup   : run 00-setup/schema.sql (open lab policies), put your URL + anon key below, open in a browser.\n-->\n<html>\n<head>\n  <meta charset=\"utf-8\"><title>Student Management System</title>\n  <style>\n    *{box-sizing:border-box}body{font-family:Segoe UI,Arial;background:#f3f5fa;margin:0}\n    .wrap{max-width:960px;margin:24px auto;padding:0 14px}h1{color:#1e3a8a}\n    .card{background:#fff;border-radius:10px;padding:16px;box-shadow:0 1px 6px #0001;margin-bottom:16px}\n    .row{display:flex;gap:8px;flex-wrap:wrap}input,select{padding:8px;border:1px solid #cbd5e1;border-radius:6px;flex:1;min-width:120px}\n    button{padding:8px 14px;border:0;border-radius:6px;background:#2563eb;color:#fff;cursor:pointer}button.g{background:#64748b}button.d{background:#dc2626}\n    table{width:100%;border-collapse:collapse}th,td{padding:8px;border-bottom:1px solid #e2e8f0;text-align:left}th{cursor:pointer;background:#eff6ff}\n    #err{color:#dc2626;min-height:20px}.pager{display:flex;justify-content:space-between;align-items:center;margin-top:10px}\n  </style>\n</head>\n<body><div class=\"wrap\">\n  <h1>Student Management System</h1>\n  <div class=\"card\">\n    <h3 id=\"formTitle\">Add student</h3>\n    <div class=\"row\">\n      <input id=\"name\" placeholder=\"Full name\"><input id=\"email\" type=\"email\" placeholder=\"Email\">\n      <input id=\"course\" placeholder=\"Course\"><input id=\"age\" type=\"number\" placeholder=\"Age\" min=\"15\">\n    </div>\n    <p id=\"err\"></p>\n    <button onclick=\"save()\">Save</button> <button class=\"g\" onclick=\"resetForm()\">Clear</button>\n  </div>\n  <div class=\"card\">\n    <div class=\"row\"><input id=\"search\" placeholder=\"Search by name...\" oninput=\"page=0;load()\">\n      <select id=\"filterCourse\" onchange=\"page=0;load()\"><option value=\"\">All courses</option></select></div>\n    <table><thead><tr>\n      <th onclick=\"sortBy('id')\">ID</th><th onclick=\"sortBy('name')\">Name</th><th onclick=\"sortBy('email')\">Email</th>\n      <th onclick=\"sortBy('course')\">Course</th><th onclick=\"sortBy('age')\">Age</th><th></th></tr></thead><tbody id=\"rows\"></tbody></table>\n    <div class=\"pager\"><button class=\"g\" onclick=\"go(-1)\">&laquo; Prev</button><span id=\"info\"></span><button class=\"g\" onclick=\"go(1)\">Next &raquo;</button></div>\n  </div>\n</div>\n<script src=\"https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2\"></script>\n<script>\n  const SUPABASE_URL = 'https://YOUR-PROJECT-REF.supabase.co';\n  const SUPABASE_ANON_KEY = 'YOUR-ANON-KEY';\n  const sb = window.supabase.createClient(SUPABASE_URL, SUPABASE_ANON_KEY);\n  const $ = (i) => document.getElementById(i);\n  const PAGE_SIZE = 5; let page = 0, sort = 'id', asc = true, editId = null, cache = [];\n  const esc = (v) => String(v ?? '').replace(/[&<>\"']/g, (c) => ({ '&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',\"'\":'&#39;' }[c]));\n\n  async function load() {\n    let q = sb.from('students').select('*', { count: 'exact' });\n    if ($('search').value) q = q.ilike('name', `%${$('search').value}%`);\n    if ($('filterCourse').value) q = q.eq('course', $('filterCourse').value);\n    const { data, count, error } = await q.order(sort, { ascending: asc }).range(page * PAGE_SIZE, page * PAGE_SIZE + PAGE_SIZE - 1);\n    if (error) return ($('err').textContent = error.message);\n    cache = data;\n    $('rows').innerHTML = data.map((s) => `<tr><td>${s.id}</td><td>${esc(s.name)}</td><td>${esc(s.email)}</td><td>${esc(s.course)}</td><td>${s.age ?? ''}</td>\n      <td><button onclick=\"edit(${s.id})\">Edit</button> <button class=\"d\" onclick=\"del(${s.id})\">Delete</button></td></tr>`).join('');\n    $('info').textContent = `Page ${page + 1} of ${Math.max(1, Math.ceil(count / PAGE_SIZE))} (${count} students)`;\n    window.total = count;\n  }\n  async function loadCourses() {\n    const { data } = await sb.from('students').select('course');\n    const list = [...new Set((data || []).map((r) => r.course))].sort();\n    $('filterCourse').innerHTML = '<option value=\"\">All courses</option>' + list.map((c) => `<option>${esc(c)}</option>`).join('');\n  }\n  function validate(s) {\n    if (s.name.length < 2) return 'Name must have at least 2 characters';\n    if (!/^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/.test(s.email)) return 'Enter a valid email';\n    if (!s.course) return 'Course is required';\n    if (!Number.isInteger(s.age) || s.age < 15) return 'Age must be 15 or more';\n    return null;\n  }\n  async function save() {\n    const s = { name: $('name').value.trim(), email: $('email').value.trim(), course: $('course').value.trim(), age: parseInt($('age').value, 10) };\n    const v = validate(s); if (v) return ($('err').textContent = v);\n    const { error } = editId ? await sb.from('students').update(s).eq('id', editId) : await sb.from('students').insert(s);\n    if (error) return ($('err').textContent = error.message);\n    resetForm(); await load(); loadCourses();\n  }\n  function edit(id) {\n    const s = cache.find((x) => x.id === id); editId = id; $('formTitle').textContent = 'Edit student #' + id;\n    ['name', 'email', 'course', 'age'].forEach((k) => ($(k).value = s[k] ?? ''));\n  }\n  async function del(id) { if (!confirm('Delete student #' + id + '?')) return; await sb.from('students').delete().eq('id', id); load(); loadCourses(); }\n  function resetForm() { editId = null; $('formTitle').textContent = 'Add student'; $('err').textContent = ''; ['name','email','course','age'].forEach((k) => ($(k).value = '')); }\n  function sortBy(c) { asc = sort === c ? !asc : true; sort = c; load(); }\n  function go(d) { const max = Math.ceil((window.total || 0) / PAGE_SIZE) - 1; page = Math.min(Math.max(0, page + d), Math.max(0, max)); load(); }\n  load(); loadCourses();\n</script></body></html>\n", "supabase/11-project-based-experiments/02-user-authentication-system.html": "<!DOCTYPE html>\n<!--\n  Project: User Authentication System using Supabase\n  Features: register, login, logout, forgot / reset password, profile view + update, session persistence.\n  Setup   : Authentication > URL Configuration: add this page's URL to Redirect URLs (e.g. http://localhost:5500/**).\n            Serve with a local server (npx serve .) so password-reset links can return here. Put URL + anon key below.\n-->\n<html>\n<head>\n  <meta charset=\"utf-8\"><title>User Authentication System</title>\n  <style>\n    body{font-family:Segoe UI,Arial;background:#eef2ff;display:flex;justify-content:center;padding-top:40px}\n    .box{background:#fff;width:380px;padding:24px;border-radius:12px;box-shadow:0 2px 12px #0002}\n    input{width:100%;padding:10px;margin:6px 0;border:1px solid #cbd5e1;border-radius:6px;box-sizing:border-box}\n    button{width:100%;padding:10px;margin-top:8px;border:0;border-radius:6px;background:#4f46e5;color:#fff;cursor:pointer}\n    button.link{background:none;color:#4f46e5;padding:4px;width:auto;margin:0}.hide{display:none}#msg{margin-top:10px;min-height:20px}.err{color:#dc2626}.ok{color:#16a34a}\n    dl{margin:0}dt{font-size:12px;color:#666;margin-top:8px}dd{margin:0;font-weight:600;word-break:break-all}\n  </style>\n</head>\n<body><div class=\"box\">\n  <h2 id=\"title\">Login</h2>\n\n  <div id=\"viewAuth\">\n    <input id=\"name\" class=\"hide\" placeholder=\"Full name\">\n    <input id=\"email\" type=\"email\" placeholder=\"Email\"><input id=\"password\" type=\"password\" placeholder=\"Password (min 8)\">\n    <button id=\"mainBtn\" onclick=\"submitAuth()\">Login</button>\n    <p><button class=\"link\" onclick=\"toggleMode()\" id=\"toggle\">Need an account? Register</button>\n       <button class=\"link\" onclick=\"forgot()\">Forgot password?</button></p>\n  </div>\n\n  <div id=\"viewProfile\" class=\"hide\">\n    <dl><dt>User ID</dt><dd id=\"uid\"></dd><dt>Email</dt><dd id=\"pemail\"></dd><dt>Role</dt><dd id=\"prole\"></dd><dt>Last sign-in</dt><dd id=\"plast\"></dd></dl>\n    <input id=\"newName\" placeholder=\"Update full name\"><button onclick=\"updateName()\">Save name</button>\n    <input id=\"newPass\" type=\"password\" placeholder=\"New password\"><button onclick=\"updatePass()\">Change password</button>\n    <button style=\"background:#dc2626\" onclick=\"sb.auth.signOut()\">Logout</button>\n  </div>\n\n  <div id=\"viewRecovery\" class=\"hide\">\n    <p>Choose a new password</p><input id=\"recPass\" type=\"password\" placeholder=\"New password\"><button onclick=\"setRecovery()\">Update password</button>\n  </div>\n  <div id=\"msg\"></div>\n</div>\n<script src=\"https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2\"></script>\n<script>\n  const SUPABASE_URL = 'https://YOUR-PROJECT-REF.supabase.co';\n  const SUPABASE_ANON_KEY = 'YOUR-ANON-KEY';\n  const sb = window.supabase.createClient(SUPABASE_URL, SUPABASE_ANON_KEY);\n  const $ = (i) => document.getElementById(i); let registering = false;\n  const say = (t, ok) => { $('msg').textContent = t || ''; $('msg').className = ok ? 'ok' : 'err'; };\n  const view = (v) => ['viewAuth', 'viewProfile', 'viewRecovery'].forEach((x) => $(x).classList.toggle('hide', x !== v));\n\n  function toggleMode() {\n    registering = !registering;\n    $('name').classList.toggle('hide', !registering);\n    $('title').textContent = $('mainBtn').textContent = registering ? 'Register' : 'Login';\n    $('toggle').textContent = registering ? 'Have an account? Login' : 'Need an account? Register'; say('');\n  }\n  async function submitAuth() {\n    const email = $('email').value.trim(), password = $('password').value;\n    if (!email || password.length < 8) return say('Enter an email and a password of at least 8 characters');\n    if (registering) {\n      const { data, error } = await sb.auth.signUp({ email, password, options: { data: { full_name: $('name').value } } });\n      if (error) return say(error.message);\n      say(data.session ? 'Registered and logged in!' : 'Registered! Check your email to confirm.', true);\n    } else {\n      const { error } = await sb.auth.signInWithPassword({ email, password }); if (error) say(error.message);\n    }\n  }\n  async function forgot() {\n    const email = $('email').value.trim(); if (!email) return say('Type your email first');\n    const { error } = await sb.auth.resetPasswordForEmail(email, { redirectTo: location.href.split('#')[0] });\n    say(error ? error.message : 'If the email exists, a reset link has been sent.', !error);\n  }\n  async function setRecovery() {\n    const { error } = await sb.auth.updateUser({ password: $('recPass').value });\n    say(error ? error.message : 'Password updated!', !error); if (!error) view('viewProfile');\n  }\n  async function showProfile(user) {\n    view('viewProfile'); $('title').textContent = 'My profile';\n    $('uid').textContent = user.id; $('pemail').textContent = user.email; $('plast').textContent = new Date(user.last_sign_in_at).toLocaleString();\n    const { data } = await sb.from('profiles').select('full_name, role').eq('id', user.id).maybeSingle();\n    $('prole').textContent = data?.role ?? 'user'; $('newName').value = data?.full_name ?? '';\n  }\n  async function updateName() {\n    const { data: { user } } = await sb.auth.getUser();\n    const { error } = await sb.from('profiles').update({ full_name: $('newName').value }).eq('id', user.id);\n    say(error ? error.message : 'Name saved', !error);\n  }\n  async function updatePass() {\n    const { error } = await sb.auth.updateUser({ password: $('newPass').value });\n    say(error ? error.message : 'Password changed', !error); $('newPass').value = '';\n  }\n  sb.auth.onAuthStateChange((event, session) => {\n    if (event === 'PASSWORD_RECOVERY') { view('viewRecovery'); $('title').textContent = 'Reset password'; }\n    else if (session) showProfile(session.user);\n    else { view('viewAuth'); $('title').textContent = registering ? 'Register' : 'Login'; }\n  });\n</script></body></html>\n", "supabase/11-project-based-experiments/03-product-management-system.html": "<!DOCTYPE html>\n<!--\n  Project: Product Management System using Supabase\n  Features: CRUD, category filter, price/stock validation, low-stock highlight, inventory value, CSV export.\n  Setup   : schema.sql (open lab policies) + URL/anon key below.  (After RBAC SQL, only admins can write.)\n-->\n<html>\n<head>\n  <meta charset=\"utf-8\"><title>Product Management</title>\n  <style>\n    body{font-family:Segoe UI,Arial;background:#f8fafc;margin:0}.wrap{max-width:980px;margin:24px auto;padding:0 14px}\n    .cards{display:flex;gap:12px;margin-bottom:14px}.stat{flex:1;background:#fff;padding:14px;border-radius:10px;box-shadow:0 1px 5px #0001}.stat b{font-size:22px;display:block;color:#0f766e}\n    .card{background:#fff;padding:14px;border-radius:10px;box-shadow:0 1px 5px #0001;margin-bottom:14px}\n    input,select{padding:8px;margin:3px;border:1px solid #cbd5e1;border-radius:6px}button{padding:8px 12px;border:0;border-radius:6px;background:#0f766e;color:#fff;cursor:pointer}\n    button.d{background:#dc2626}button.g{background:#64748b}table{width:100%;border-collapse:collapse}th,td{padding:8px;border-bottom:1px solid #e5e7eb;text-align:left}\n    .low{background:#fef2f2}#err{color:#dc2626}\n  </style>\n</head>\n<body><div class=\"wrap\">\n  <h1>Product Management</h1>\n  <div class=\"cards\"><div class=\"stat\">Products<b id=\"sCount\">0</b></div><div class=\"stat\">Inventory value<b id=\"sValue\">0</b></div><div class=\"stat\">Low stock (&lt;5)<b id=\"sLow\">0</b></div></div>\n  <div class=\"card\">\n    <input id=\"name\" placeholder=\"Product name\"><input id=\"category\" placeholder=\"Category\" list=\"cats\"><datalist id=\"cats\"></datalist>\n    <input id=\"price\" type=\"number\" step=\"0.01\" min=\"0\" placeholder=\"Price\"><input id=\"stock\" type=\"number\" min=\"0\" placeholder=\"Stock\">\n    <button onclick=\"save()\" id=\"saveBtn\">Add product</button> <button class=\"g\" onclick=\"clearForm()\">Clear</button><p id=\"err\"></p>\n  </div>\n  <div class=\"card\">\n    <select id=\"cat\" onchange=\"render()\"><option value=\"\">All categories</option></select>\n    <input id=\"q\" placeholder=\"Search...\" oninput=\"render()\"> <button class=\"g\" onclick=\"exportCsv()\">Export CSV</button>\n    <table><thead><tr><th>Name</th><th>Category</th><th>Price</th><th>Stock</th><th></th></tr></thead><tbody id=\"rows\"></tbody></table>\n  </div>\n</div>\n<script src=\"https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2\"></script>\n<script>\n  const SUPABASE_URL = 'https://YOUR-PROJECT-REF.supabase.co';\n  const SUPABASE_ANON_KEY = 'YOUR-ANON-KEY';\n  const sb = window.supabase.createClient(SUPABASE_URL, SUPABASE_ANON_KEY);\n  const $ = (i) => document.getElementById(i); let products = [], editId = null;\n  const esc = (v) => String(v ?? '').replace(/[&<>\"']/g, (c) => ({ '&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',\"'\":'&#39;' }[c]));\n  const money = (n) => Number(n).toLocaleString('en-IN', { style: 'currency', currency: 'INR' });\n\n  async function load() {\n    const { data, error } = await sb.from('products').select('*').order('id');\n    if (error) return ($('err').textContent = error.message);\n    products = data; refreshFilters(); render();\n  }\n  function refreshFilters() {\n    const cats = [...new Set(products.map((p) => p.category).filter(Boolean))].sort(), cur = $('cat').value;\n    $('cat').innerHTML = '<option value=\"\">All categories</option>' + cats.map((c) => `<option ${c === cur ? 'selected' : ''}>${esc(c)}</option>`).join('');\n    $('cats').innerHTML = cats.map((c) => `<option value=\"${esc(c)}\">`).join('');\n  }\n  function render() {\n    const q = $('q').value.toLowerCase(), c = $('cat').value;\n    const list = products.filter((p) => (!c || p.category === c) && p.name.toLowerCase().includes(q));\n    $('rows').innerHTML = list.map((p) => `<tr class=\"${p.stock < 5 ? 'low' : ''}\"><td>${esc(p.name)}</td><td>${esc(p.category)}</td><td>${money(p.price)}</td><td>${p.stock}</td>\n      <td><button onclick=\"edit(${p.id})\">Edit</button> <button class=\"d\" onclick=\"del(${p.id})\">Delete</button></td></tr>`).join('');\n    $('sCount').textContent = products.length; $('sLow').textContent = products.filter((p) => p.stock < 5).length;\n    $('sValue').textContent = money(products.reduce((s, p) => s + p.price * p.stock, 0));\n  }\n  async function save() {\n    const p = { name: $('name').value.trim(), category: $('category').value.trim() || null, price: parseFloat($('price').value), stock: parseInt($('stock').value, 10) };\n    if (p.name.length < 2) return ($('err').textContent = 'Name is too short');\n    if (!(p.price >= 0)) return ($('err').textContent = 'Price must be 0 or more');\n    if (!Number.isInteger(p.stock) || p.stock < 0) return ($('err').textContent = 'Stock must be a whole number >= 0');\n    const { error } = editId ? await sb.from('products').update(p).eq('id', editId) : await sb.from('products').insert(p);\n    if (error) return ($('err').textContent = error.message);\n    clearForm(); load();\n  }\n  function edit(id) {\n    const p = products.find((x) => x.id === id); editId = id; $('saveBtn').textContent = 'Update product';\n    $('name').value = p.name; $('category').value = p.category ?? ''; $('price').value = p.price; $('stock').value = p.stock;\n  }\n  async function del(id) { if (confirm('Delete this product?')) { const { error } = await sb.from('products').delete().eq('id', id); if (error) alert(error.message); load(); } }\n  function clearForm() { editId = null; $('saveBtn').textContent = 'Add product'; $('err').textContent = ''; ['name','category','price','stock'].forEach((k) => ($(k).value = '')); }\n  function exportCsv() {\n    const rows = [['id','name','category','price','stock'], ...products.map((p) => [p.id, p.name, p.category ?? '', p.price, p.stock])];\n    const csv = rows.map((r) => r.map((v) => `\"${String(v).replace(/\"/g, '\"\"')}\"`).join(',')).join('\\n');\n    const a = document.createElement('a'); a.href = URL.createObjectURL(new Blob([csv], { type: 'text/csv' })); a.download = 'products.csv'; a.click();\n  }\n  load();\n</script></body></html>\n", "supabase/11-project-based-experiments/04-realtime-chat-application.html": "<!DOCTYPE html>\n<!--\n  Project: Realtime Chat Application using Supabase\n  Features: login required, live messages, online users (Presence), typing indicator (Broadcast), delete own messages.\n  Setup   : schema.sql + 08-realtime/01-enable-supabase-realtime.sql + 06-authorization/06-authenticated-user-policies.sql\n            (policy: only authenticated users can post as themselves). URL + anon key below.\n-->\n<html>\n<head>\n  <meta charset=\"utf-8\"><title>Realtime Chat</title>\n  <style>\n    body{font-family:Segoe UI,Arial;background:#e2e8f0;margin:0;display:flex;justify-content:center}\n    .app{width:760px;height:92vh;margin-top:2vh;background:#fff;border-radius:12px;display:flex;box-shadow:0 3px 14px #0003;overflow:hidden}\n    aside{width:200px;background:#1e293b;color:#fff;padding:14px}aside h4{margin:0 0 10px}aside li{list-style:none;margin:6px 0;font-size:14px}aside li:before{content:\"\\25CF \";color:#22c55e}\n    main{flex:1;display:flex;flex-direction:column}#log{flex:1;overflow:auto;padding:12px}\n    .m{margin:8px 0;padding:8px 12px;background:#f1f5f9;border-radius:12px;max-width:75%}.me{background:#dbeafe;margin-left:auto}\n    .m small{display:block;color:#64748b;font-size:11px}.m .x{float:right;cursor:pointer;color:#dc2626;margin-left:8px}\n    #typing{height:20px;padding:0 12px;color:#64748b;font-size:12px}footer{display:flex;padding:10px;gap:8px;border-top:1px solid #e2e8f0}footer input{flex:1;padding:10px}\n    button{padding:10px 16px;border:0;border-radius:6px;background:#2563eb;color:#fff;cursor:pointer}\n    #login{margin:auto;text-align:center}#login input{display:block;margin:8px auto;padding:10px;width:240px}\n  </style>\n</head>\n<body>\n<div class=\"app\" id=\"login-wrap\" style=\"justify-content:center\"><div id=\"login\">\n  <h2>Join the chat</h2><input id=\"email\" type=\"email\" placeholder=\"email\"><input id=\"password\" type=\"password\" placeholder=\"password\">\n  <button onclick=\"auth(false)\">Login</button> <button style=\"background:#64748b\" onclick=\"auth(true)\">Register</button><p id=\"err\" style=\"color:#dc2626\"></p>\n</div></div>\n\n<div class=\"app\" id=\"chat-wrap\" style=\"display:none\">\n  <aside><h4>Online now</h4><ul id=\"online\" style=\"padding:0\"></ul><button style=\"margin-top:20px;background:#ef4444\" onclick=\"sb.auth.signOut()\">Logout</button></aside>\n  <main><div id=\"log\"></div><div id=\"typing\"></div>\n    <footer><input id=\"text\" placeholder=\"Message...\" oninput=\"typing()\" onkeydown=\"if(event.key==='Enter')send()\"><button onclick=\"send()\">Send</button></footer></main>\n</div>\n\n<script src=\"https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2\"></script>\n<script>\n  const SUPABASE_URL = 'https://YOUR-PROJECT-REF.supabase.co';\n  const SUPABASE_ANON_KEY = 'YOUR-ANON-KEY';\n  const sb = window.supabase.createClient(SUPABASE_URL, SUPABASE_ANON_KEY);\n  const $ = (i) => document.getElementById(i); let me = null, room = null, typingTimer;\n\n  async function auth(register) {\n    const creds = { email: $('email').value, password: $('password').value };\n    const { error } = register ? await sb.auth.signUp(creds) : await sb.auth.signInWithPassword(creds);\n    $('err').textContent = error ? error.message : (register ? 'Registered - now click Login' : '');\n  }\n  const nameOf = () => me.email.split('@')[0];\n\n  function addMessage(m) {\n    if ($('m' + m.id)) return;\n    const d = document.createElement('div'); d.id = 'm' + m.id; d.className = 'm' + (m.user_id === me.id ? ' me' : '');\n    const head = document.createElement('small'); head.textContent = `${m.username} - ${new Date(m.created_at).toLocaleTimeString()}`;\n    if (m.user_id === me.id) { const x = document.createElement('span'); x.className = 'x'; x.textContent = 'x'; x.onclick = () => sb.from('messages').delete().eq('id', m.id); head.appendChild(x); }\n    d.appendChild(head); d.appendChild(document.createTextNode(m.content));\n    $('log').appendChild(d); $('log').scrollTop = $('log').scrollHeight;\n  }\n\n  async function send() {\n    const content = $('text').value.trim(); if (!content) return; $('text').value = '';\n    const { error } = await sb.from('messages').insert({ content, username: nameOf(), user_id: me.id });\n    if (error) alert(error.message);\n  }\n  function typing() { room?.send({ type: 'broadcast', event: 'typing', payload: { user: nameOf() } }); }\n\n  async function start(user) {\n    me = user; $('login-wrap').style.display = 'none'; $('chat-wrap').style.display = 'flex';\n    const { data } = await sb.from('messages').select('*').order('created_at', { ascending: false }).limit(50);\n    $('log').innerHTML = ''; (data || []).reverse().forEach(addMessage);\n\n    room = sb.channel('room-1', { config: { presence: { key: me.id } } })\n      .on('postgres_changes', { event: 'INSERT', schema: 'public', table: 'messages' }, (p) => addMessage(p.new))\n      .on('postgres_changes', { event: 'DELETE', schema: 'public', table: 'messages' }, (p) => $('m' + p.old.id)?.remove())\n      .on('presence', { event: 'sync' }, () => {\n        const names = Object.values(room.presenceState()).flat().map((u) => u.name);\n        $('online').innerHTML = [...new Set(names)].map((n) => `<li>${n.replace(/[<>&]/g, '')}</li>`).join('');\n      })\n      .on('broadcast', { event: 'typing' }, ({ payload }) => {\n        if (payload.user === nameOf()) return;\n        $('typing').textContent = payload.user + ' is typing...'; clearTimeout(typingTimer); typingTimer = setTimeout(() => ($('typing').textContent = ''), 1500);\n      })\n      .subscribe(async (s) => { if (s === 'SUBSCRIBED') await room.track({ name: nameOf() }); });\n  }\n  function stop() { if (room) sb.removeChannel(room); room = me = null; $('login-wrap').style.display = 'flex'; $('chat-wrap').style.display = 'none'; }\n\n  sb.auth.onAuthStateChange((_e, session) => (session ? (!me && start(session.user)) : stop()));\n</script></body></html>\n", "supabase/11-project-based-experiments/05-file-upload-application.html": "<!DOCTYPE html>\n<!--\n  Project: File Upload Application using Supabase Storage\n  Features: login, drag & drop upload with progress-style status, image gallery preview, download, copy public link, delete.\n  Setup   : 07-storage/00-storage-setup-and-policies.sql (creates the public \"uploads\" bucket + policies). URL + anon key below.\n  Files are stored as  uploads/<user_id>/<timestamp>-<name>  so each user manages only his own folder.\n-->\n<html>\n<head>\n  <meta charset=\"utf-8\"><title>File Upload App</title>\n  <style>\n    body{font-family:Segoe UI,Arial;background:#f1f5f9;margin:0}.wrap{max-width:900px;margin:24px auto;padding:0 14px}\n    .card{background:#fff;border-radius:10px;padding:16px;margin-bottom:14px;box-shadow:0 1px 6px #0001}\n    #drop{border:2px dashed #94a3b8;border-radius:10px;padding:34px;text-align:center;color:#64748b;cursor:pointer}#drop.over{background:#e0f2fe;border-color:#0284c7}\n    .grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(180px,1fr));gap:12px}.item{border:1px solid #e2e8f0;border-radius:8px;padding:8px;background:#fafafa;word-break:break-all;font-size:13px}\n    .item img{width:100%;height:110px;object-fit:cover;border-radius:6px}.ic{height:110px;display:flex;align-items:center;justify-content:center;font-size:40px;background:#e2e8f0;border-radius:6px}\n    button{padding:6px 10px;border:0;border-radius:6px;background:#2563eb;color:#fff;cursor:pointer;margin:3px 2px 0 0;font-size:12px}button.d{background:#dc2626}\n    input[type=email],input[type=password]{padding:8px;margin:4px}#msg{color:#0f766e;min-height:20px}\n  </style>\n</head>\n<body><div class=\"wrap\">\n  <h1>File Upload App</h1><p id=\"msg\"></p>\n  <div class=\"card\" id=\"authBox\"><input id=\"email\" type=\"email\" placeholder=\"email\"><input id=\"password\" type=\"password\" placeholder=\"password\">\n    <button onclick=\"auth(false)\">Login</button><button onclick=\"auth(true)\">Register</button></div>\n  <div id=\"appBox\" style=\"display:none\">\n    <div class=\"card\"><b id=\"who\"></b> <button class=\"d\" onclick=\"sb.auth.signOut()\">Logout</button></div>\n    <div class=\"card\"><div id=\"drop\" onclick=\"pick.click()\">Drag &amp; drop files here or click to choose (max 5 MB: png, jpg, webp, pdf, txt)</div>\n      <input type=\"file\" id=\"pick\" multiple hidden></div>\n    <div class=\"card\"><h3>My files</h3><div class=\"grid\" id=\"grid\"></div></div>\n  </div>\n</div>\n<script src=\"https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2\"></script>\n<script>\n  const SUPABASE_URL = 'https://YOUR-PROJECT-REF.supabase.co';\n  const SUPABASE_ANON_KEY = 'YOUR-ANON-KEY';\n  const sb = window.supabase.createClient(SUPABASE_URL, SUPABASE_ANON_KEY);\n  const BUCKET = 'uploads', MAX = 5 * 1024 * 1024, $ = (i) => document.getElementById(i); let user = null;\n  const say = (t) => ($('msg').textContent = t || '');\n  const esc = (v) => String(v).replace(/[&<>\"']/g, (c) => ({ '&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',\"'\":'&#39;' }[c]));\n\n  async function auth(reg) {\n    const c = { email: $('email').value, password: $('password').value };\n    const { error } = reg ? await sb.auth.signUp(c) : await sb.auth.signInWithPassword(c); say(error ? error.message : reg ? 'Registered - login now' : '');\n  }\n  async function uploadFiles(files) {\n    for (const f of files) {\n      if (f.size > MAX) { say(`${f.name}: larger than 5 MB`); continue; }\n      say(`Uploading ${f.name}...`);\n      const path = `${user.id}/${Date.now()}-${f.name.replace(/[^\\w.-]/g, '_')}`;\n      const { error } = await sb.storage.from(BUCKET).upload(path, f, { contentType: f.type, upsert: false });\n      say(error ? `${f.name}: ${error.message}` : `${f.name} uploaded`);\n    }\n    load();\n  }\n  async function load() {\n    const { data, error } = await sb.storage.from(BUCKET).list(user.id, { sortBy: { column: 'created_at', order: 'desc' } });\n    if (error) return say(error.message);\n    $('grid').innerHTML = '';\n    data.filter((f) => f.name !== '.emptyFolderPlaceholder').forEach((f) => {\n      const path = `${user.id}/${f.name}`, url = sb.storage.from(BUCKET).getPublicUrl(path).data.publicUrl;\n      const isImg = /\\.(png|jpe?g|webp|gif)$/i.test(f.name), div = document.createElement('div'); div.className = 'item';\n      div.innerHTML = (isImg ? `<img src=\"${url}\">` : '<div class=\"ic\">&#128196;</div>') + `<div>${esc(f.name.replace(/^\\d+-/, ''))}</div><small>${((f.metadata?.size || 0) / 1024).toFixed(1)} KB</small><br>\n        <a href=\"${url}\" target=\"_blank\"><button>Open</button></a><button data-a=\"copy\">Copy link</button><button data-a=\"del\" class=\"d\">Delete</button>`;\n      div.querySelector('[data-a=copy]').onclick = () => { navigator.clipboard.writeText(url); say('Link copied'); };\n      div.querySelector('[data-a=del]').onclick = async () => { if (confirm('Delete file?')) { await sb.storage.from(BUCKET).remove([path]); load(); } };\n      $('grid').appendChild(div);\n    });\n    if (!$('grid').children.length) $('grid').textContent = 'No files yet.';\n  }\n  const drop = $('drop');\n  drop.ondragover = (e) => { e.preventDefault(); drop.classList.add('over'); }; drop.ondragleave = () => drop.classList.remove('over');\n  drop.ondrop = (e) => { e.preventDefault(); drop.classList.remove('over'); uploadFiles(e.dataTransfer.files); };\n  $('pick').onchange = (e) => { uploadFiles(e.target.files); e.target.value = ''; };\n  sb.auth.onAuthStateChange((_e, s) => {\n    user = s?.user || null; $('authBox').style.display = user ? 'none' : 'block'; $('appBox').style.display = user ? 'block' : 'none';\n    if (user) { $('who').textContent = user.email; load(); }\n  });\n</script></body></html>\n", "supabase/11-project-based-experiments/06-student-rest-api.js": "/**\n * Project: Create a Student REST API using Supabase.\n * An Express API that sits in front of Supabase. If the caller sends \"Authorization: Bearer <user JWT>\",\n * the request runs AS that user (RLS applies); otherwise it runs as anon.\n *\n * Run : node 11-project-based-experiments/06-student-rest-api.js      (port 3000)\n * Docs: GET http://localhost:3000/\n *   GET    /api/students?search=&course=&sort=name&order=asc&page=1&limit=10\n *   GET    /api/students/:id\n *   POST   /api/students          {name,email,course,age}\n *   PUT    /api/students/:id      (all fields)     PATCH /api/students/:id (some fields)\n *   DELETE /api/students/:id\n */\nrequire('dotenv').config({ path: require('path').join(__dirname, '..', '.env') });\nconst express = require('express');\nconst { createClient } = require('@supabase/supabase-js');\n\nconst app = express();\napp.use(express.json());\n\n// per-request client that forwards the caller's token\napp.use((req, res, next) => {\n  const auth = req.headers.authorization;\n  req.sb = createClient(process.env.SUPABASE_URL, process.env.SUPABASE_ANON_KEY, {\n    global: { headers: auth ? { Authorization: auth } : {} },\n    auth: { persistSession: false, autoRefreshToken: false },\n  });\n  next();\n});\n\nconst EMAIL = /^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/;\nfunction validate(b, partial = false) {\n  const errors = [];\n  const check = (k, ok, msg) => { if (b[k] === undefined) { if (!partial) errors.push(`${k} is required`); } else if (!ok(b[k])) errors.push(`${k} ${msg}`); };\n  check('name', (v) => typeof v === 'string' && v.trim().length >= 2, 'must be at least 2 characters');\n  check('email', (v) => typeof v === 'string' && EMAIL.test(v), 'must be a valid email');\n  check('course', (v) => typeof v === 'string' && v.trim().length > 0, 'must be a non-empty string');\n  check('age', (v) => Number.isInteger(v) && v >= 15, 'must be an integer >= 15');\n  return errors;\n}\nconst FIELDS = ['name', 'email', 'course', 'age'];\nconst pick = (b) => Object.fromEntries(FIELDS.filter((k) => b[k] !== undefined).map((k) => [k, b[k]]));\nconst fail = (res, error, fallback = 400) => {\n  const status = error.code === '23505' ? 409 : error.code === 'PGRST116' ? 404 : error.code === '42501' ? 403 : fallback;\n  res.status(status).json({ error: error.message, code: error.code });\n};\n\napp.get('/api/students', async (req, res) => {\n  const { search, course, sort = 'id', order = 'asc', page = 1, limit = 10 } = req.query;\n  const p = Math.max(1, +page), l = Math.min(100, Math.max(1, +limit));\n  if (!['id', 'name', 'email', 'course', 'age', 'created_at'].includes(sort)) return res.status(400).json({ error: 'invalid sort column' });\n\n  let q = req.sb.from('students').select('*', { count: 'exact' });\n  if (search) q = q.ilike('name', `%${search}%`);\n  if (course) q = q.eq('course', course);\n  const { data, count, error } = await q.order(sort, { ascending: order !== 'desc' }).range((p - 1) * l, p * l - 1);\n  if (error) return fail(res, error);\n  res.json({ page: p, limit: l, total: count, totalPages: Math.ceil(count / l), data });\n});\n\napp.get('/api/students/:id', async (req, res) => {\n  const { data, error } = await req.sb.from('students').select('*').eq('id', req.params.id).single();\n  error ? fail(res, error) : res.json(data);\n});\n\napp.post('/api/students', async (req, res) => {\n  const errors = validate(req.body);\n  if (errors.length) return res.status(422).json({ errors });\n  const { data, error } = await req.sb.from('students').insert(pick(req.body)).select().single();\n  error ? fail(res, error) : res.status(201).location(`/api/students/${data.id}`).json(data);\n});\n\nconst update = (partial) => async (req, res) => {\n  const errors = validate(req.body, partial);\n  if (errors.length) return res.status(422).json({ errors });\n  const { data, error } = await req.sb.from('students').update(pick(req.body)).eq('id', req.params.id).select().single();\n  error ? fail(res, error) : res.json(data);\n};\napp.put('/api/students/:id', update(false));\napp.patch('/api/students/:id', update(true));\n\napp.delete('/api/students/:id', async (req, res) => {\n  const { data, error } = await req.sb.from('students').delete().eq('id', req.params.id).select();\n  if (error) return fail(res, error);\n  data.length ? res.status(204).end() : res.status(404).json({ error: 'Student not found' });\n});\n\napp.get('/', (req, res) => res.json({ name: 'Student REST API (Supabase)', endpoints: ['GET /api/students', 'GET /api/students/:id', 'POST /api/students', 'PUT|PATCH /api/students/:id', 'DELETE /api/students/:id'] }));\napp.use((req, res) => res.status(404).json({ error: 'Route not found' }));\n\napp.listen(process.env.PORT || 3000, () => console.log('Student REST API on http://localhost:' + (process.env.PORT || 3000)));\n", "supabase/11-project-based-experiments/07-role-based-admin-dashboard-rls.sql": "-- Project: Role-Based Admin Dashboard using Supabase Authentication and RLS  (database part)\n-- Run AFTER 00-setup/schema.sql. Then open 07-role-based-admin-dashboard.html.\n\n-- 1. role helper (SECURITY DEFINER = no recursion inside policies)\ncreate or replace function public.is_admin()\nreturns boolean language sql stable security definer set search_path = public as $$\n  select coalesce((select role = 'admin' from public.profiles where id = auth.uid()), false);\n$$;\n\n-- 2. PROFILES: users read own, admins read all; only admins change roles\ndrop policy if exists \"lab: read profiles\" on public.profiles;\ndrop policy if exists \"profiles: read own or admin\" on public.profiles;\ndrop policy if exists \"profiles: update own\" on public.profiles;\ndrop policy if exists \"profiles: admin update all\" on public.profiles;\n\ncreate policy \"profiles: read own or admin\" on public.profiles for select to authenticated\nusing (id = (select auth.uid()) or public.is_admin());\n\ncreate policy \"profiles: update own name\" on public.profiles for update to authenticated\nusing (id = (select auth.uid()))\nwith check (id = (select auth.uid()) and role = (select role from public.profiles where id = auth.uid()));\n\ncreate policy \"profiles: admin update all\" on public.profiles for update to authenticated\nusing (public.is_admin()) with check (public.is_admin());\n\n-- 3. PRODUCTS: everyone signed-in reads, admin writes\ndrop policy if exists \"lab: open read products\"  on public.products;\ndrop policy if exists \"lab: open write products\" on public.products;\ndrop policy if exists \"products: everyone signed-in can read\" on public.products;\ndrop policy if exists \"products: only admin can insert\" on public.products;\ndrop policy if exists \"products: only admin can update\" on public.products;\ndrop policy if exists \"products: only admin can delete\" on public.products;\n\ncreate policy \"products: read\" on public.products for select to authenticated using (true);\ncreate policy \"products: admin insert\" on public.products for insert to authenticated with check (public.is_admin());\ncreate policy \"products: admin update\" on public.products for update to authenticated using (public.is_admin()) with check (public.is_admin());\ncreate policy \"products: admin delete\" on public.products for delete to authenticated using (public.is_admin());\n\n-- 4. STUDENTS: user sees own, admin sees all\ndrop policy if exists \"lab: open read students\"  on public.students;\ndrop policy if exists \"lab: open write students\" on public.students;\ndrop policy if exists \"own students\" on public.students;\ndrop policy if exists \"students: owner or admin select\" on public.students;\ndrop policy if exists \"students: owner inserts\" on public.students;\ndrop policy if exists \"students: owner or admin update\" on public.students;\ndrop policy if exists \"students: owner or admin delete\" on public.students;\n\ncreate policy \"students: select own or admin\" on public.students for select to authenticated using (user_id = (select auth.uid()) or public.is_admin());\ncreate policy \"students: insert own\" on public.students for insert to authenticated with check (user_id = (select auth.uid()) or public.is_admin());\ncreate policy \"students: update own or admin\" on public.students for update to authenticated using (user_id = (select auth.uid()) or public.is_admin()) with check (user_id = (select auth.uid()) or public.is_admin());\ncreate policy \"students: delete own or admin\" on public.students for delete to authenticated using (user_id = (select auth.uid()) or public.is_admin());\n\n-- 5. Dashboard statistics function (admin only)\ncreate or replace function public.admin_stats()\nreturns json language plpgsql stable security definer set search_path = public as $$\nbegin\n  if not public.is_admin() then raise exception 'admin only' using errcode = '42501'; end if;\n  return json_build_object(\n    'users',    (select count(*) from public.profiles),\n    'admins',   (select count(*) from public.profiles where role = 'admin'),\n    'students', (select count(*) from public.students),\n    'products', (select count(*) from public.products)\n  );\nend; $$;\nrevoke execute on function public.admin_stats() from anon;\n\n-- 6. Make yourself the first admin (replace the e-mail):\n-- update public.profiles set role = 'admin' where id = (select id from auth.users where email = 'you@example.com');\n", "supabase/11-project-based-experiments/07-role-based-admin-dashboard.html": "<!DOCTYPE html>\n<!--\n  Project: Role-Based Admin Dashboard using Supabase Authentication and RLS\n  Setup  : run 07-role-based-admin-dashboard-rls.sql, register two users, promote one to admin with the SQL at the bottom of that file.\n  Behaviour: the UI hides admin features from normal users, BUT the real protection is RLS in the database\n             (a normal user who calls the API directly still gets blocked).\n-->\n<html>\n<head>\n  <meta charset=\"utf-8\"><title>Admin Dashboard</title>\n  <style>\n    body{font-family:Segoe UI,Arial;margin:0;background:#f1f5f9}nav{background:#0f172a;color:#fff;padding:12px 20px;display:flex;justify-content:space-between;align-items:center}\n    .badge{padding:2px 10px;border-radius:12px;font-size:12px;background:#22c55e}.badge.user{background:#64748b}\n    .wrap{max-width:1000px;margin:20px auto;padding:0 14px}.stats{display:grid;grid-template-columns:repeat(4,1fr);gap:12px;margin-bottom:16px}\n    .stat{background:#fff;padding:16px;border-radius:10px;text-align:center;box-shadow:0 1px 5px #0001}.stat b{display:block;font-size:26px;color:#2563eb}\n    .card{background:#fff;padding:16px;border-radius:10px;margin-bottom:16px;box-shadow:0 1px 5px #0001}table{width:100%;border-collapse:collapse}th,td{padding:8px;border-bottom:1px solid #e2e8f0;text-align:left}\n    input,select{padding:7px;margin:3px}button{padding:7px 12px;border:0;border-radius:6px;background:#2563eb;color:#fff;cursor:pointer}button.d{background:#dc2626}\n    #login{max-width:340px;margin:80px auto;background:#fff;padding:24px;border-radius:12px;box-shadow:0 2px 12px #0002}#login input{width:100%;box-sizing:border-box}.hide{display:none}#err{color:#dc2626}\n  </style>\n</head>\n<body>\n<div id=\"login\"><h2>Sign in</h2><input id=\"email\" placeholder=\"email\"><input id=\"password\" type=\"password\" placeholder=\"password\">\n  <button onclick=\"auth(false)\">Login</button> <button onclick=\"auth(true)\" style=\"background:#64748b\">Register</button><p id=\"err\"></p></div>\n\n<div id=\"dash\" class=\"hide\">\n  <nav><b>Dashboard</b><span><span id=\"who\"></span> <span id=\"role\" class=\"badge\"></span> <button onclick=\"sb.auth.signOut()\">Logout</button></span></nav>\n  <div class=\"wrap\">\n    <div class=\"stats admin hide\"><div class=\"stat\">Users<b id=\"s_users\">-</b></div><div class=\"stat\">Admins<b id=\"s_admins\">-</b></div>\n      <div class=\"stat\">Students<b id=\"s_students\">-</b></div><div class=\"stat\">Products<b id=\"s_products\">-</b></div></div>\n\n    <div class=\"card admin hide\"><h3>User management (admin only)</h3>\n      <table><thead><tr><th>Name</th><th>ID</th><th>Role</th></tr></thead><tbody id=\"users\"></tbody></table></div>\n\n    <div class=\"card\"><h3>Products</h3>\n      <div class=\"admin hide\"><input id=\"pname\" placeholder=\"name\"><input id=\"pprice\" type=\"number\" placeholder=\"price\"><input id=\"pstock\" type=\"number\" placeholder=\"stock\"><button onclick=\"addProduct()\">Add</button></div>\n      <table><thead><tr><th>Name</th><th>Price</th><th>Stock</th><th class=\"admin hide\"></th></tr></thead><tbody id=\"products\"></tbody></table></div>\n\n    <div class=\"card\"><h3>Try to break the rules (RLS test)</h3>\n      <button onclick=\"tryPromote()\">Promote myself to admin</button> <button onclick=\"tryDelete()\">Delete first product</button> <pre id=\"test\"></pre></div>\n  </div>\n</div>\n<script src=\"https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2\"></script>\n<script>\n  const SUPABASE_URL = 'https://YOUR-PROJECT-REF.supabase.co';\n  const SUPABASE_ANON_KEY = 'YOUR-ANON-KEY';\n  const sb = window.supabase.createClient(SUPABASE_URL, SUPABASE_ANON_KEY);\n  const $ = (i) => document.getElementById(i); let me = null, isAdmin = false;\n  const esc = (v) => String(v ?? '').replace(/[&<>\"']/g, (c) => ({ '&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',\"'\":'&#39;' }[c]));\n\n  async function auth(reg) {\n    const c = { email: $('email').value, password: $('password').value };\n    const { error } = reg ? await sb.auth.signUp(c) : await sb.auth.signInWithPassword(c);\n    $('err').textContent = error ? error.message : reg ? 'Registered - now login' : '';\n  }\n  async function init(user) {\n    me = user; $('login').classList.add('hide'); $('dash').classList.remove('hide'); $('who').textContent = user.email;\n    const { data: p } = await sb.from('profiles').select('role').eq('id', user.id).single();\n    isAdmin = p?.role === 'admin'; $('role').textContent = p?.role || 'user'; $('role').className = 'badge ' + (isAdmin ? '' : 'user');\n    document.querySelectorAll('.admin').forEach((el) => el.classList.toggle('hide', !isAdmin));\n    loadProducts(); if (isAdmin) { loadStats(); loadUsers(); }\n  }\n  async function loadStats() {\n    const { data, error } = await sb.rpc('admin_stats'); if (error) return console.error(error.message);\n    for (const k of ['users', 'admins', 'students', 'products']) $('s_' + k).textContent = data[k];\n  }\n  async function loadUsers() {\n    const { data } = await sb.from('profiles').select('*').order('created_at');\n    $('users').innerHTML = data.map((u) => `<tr><td>${esc(u.full_name)}</td><td><small>${u.id}</small></td>\n      <td><select onchange=\"setRole('${u.id}', this.value)\"><option ${u.role === 'user' ? 'selected' : ''}>user</option><option ${u.role === 'admin' ? 'selected' : ''}>admin</option></select></td></tr>`).join('');\n  }\n  async function setRole(id, role) { const { error } = await sb.from('profiles').update({ role }).eq('id', id); if (error) alert(error.message); loadUsers(); loadStats(); }\n  async function loadProducts() {\n    const { data } = await sb.from('products').select('*').order('id');\n    $('products').innerHTML = (data || []).map((p) => `<tr><td>${esc(p.name)}</td><td>${p.price}</td><td>${p.stock}</td>${isAdmin ? `<td><button class=\"d\" onclick=\"delProduct(${p.id})\">Delete</button></td>` : ''}</tr>`).join('');\n  }\n  async function addProduct() {\n    const { error } = await sb.from('products').insert({ name: $('pname').value, price: +$('pprice').value, stock: +$('pstock').value }); if (error) alert(error.message); loadProducts(); loadStats();\n  }\n  async function delProduct(id) { const { error } = await sb.from('products').delete().eq('id', id); if (error) alert(error.message); loadProducts(); loadStats(); }\n\n  async function tryPromote() {\n    const { data, error } = await sb.from('profiles').update({ role: 'admin' }).eq('id', me.id).select();\n    $('test').textContent = error ? 'BLOCKED: ' + error.message : data.length ? 'Allowed! (you are admin already?)' : 'BLOCKED by RLS: 0 rows updated';\n  }\n  async function tryDelete() {\n    const { data: first } = await sb.from('products').select('id').limit(1).maybeSingle(); if (!first) return ($('test').textContent = 'No products');\n    const { data, error } = await sb.from('products').delete().eq('id', first.id).select();\n    $('test').textContent = error ? 'BLOCKED: ' + error.message : data.length ? 'Deleted (you are admin)' : 'BLOCKED by RLS: 0 rows deleted'; loadProducts();\n  }\n  sb.auth.onAuthStateChange((_e, s) => { if (s) { if (!me) init(s.user); } else { me = null; $('login').classList.remove('hide'); $('dash').classList.add('hide'); } });\n</script></body></html>\n", "supabase/config/crudApp.js": "// Builds a tiny Express app: JSON CRUD API (supabase-js) + a generic HTML UI, for any table.\nconst express = require('express');\nconst { supabase } = require('./supabaseClient');\n\nmodule.exports = function createCrudApp({ table, title, fields, orderBy = 'id' }) {\n  const app = express();\n  app.use(express.json());\n  const wrap = (fn) => async (req, res) => {\n    const { data, error, status } = await fn(req);\n    if (error) return res.status(status && status >= 400 ? status : 400).json({ error: error.message });\n    res.status(status === 201 ? 201 : 200).json(data);\n  };\n  const pick = (body) => Object.fromEntries(fields.filter((f) => body[f.name] !== undefined && body[f.name] !== '')\n    .map((f) => [f.name, f.type === 'number' ? Number(body[f.name]) : body[f.name]]));\n\n  // ---- CRUD API ----\n  app.get(`/api/${table}`, wrap(async (req) => {\n    const q = supabase.from(table).select('*').order(orderBy);\n    return req.query.search ? q.ilike(fields[0].name, `%${req.query.search}%`) : q;\n  }));\n  app.get(`/api/${table}/:id`, wrap((req) => supabase.from(table).select('*').eq('id', req.params.id).single()));\n  app.post(`/api/${table}`, wrap((req) => supabase.from(table).insert(pick(req.body)).select().single().then((r) => ({ ...r, status: r.error ? r.status : 201 }))));\n  app.put(`/api/${table}/:id`, wrap((req) => supabase.from(table).update(pick(req.body)).eq('id', req.params.id).select().single()));\n  app.delete(`/api/${table}/:id`, wrap((req) => supabase.from(table).delete().eq('id', req.params.id).select().single()));\n\n  // ---- UI ----\n  app.get('/', (req, res) => res.send(`<!DOCTYPE html><html><head><meta charset=\"utf-8\"><title>${title}</title>\n<style>body{font-family:Arial;max-width:900px;margin:30px auto;padding:0 12px}input,select{padding:6px;margin:3px}button{padding:6px 12px;cursor:pointer}\ntable{width:100%;border-collapse:collapse;margin-top:16px}th,td{border:1px solid #ccc;padding:6px;text-align:left}th{background:#f0f4ff}#msg{color:#b00}</style></head><body>\n<h2>${title}</h2><p id=\"msg\"></p>\n<div id=\"form\">${fields.map((f) => `<input id=\"f_${f.name}\" placeholder=\"${f.name}\" type=\"${f.type === 'number' ? 'number' : f.type === 'email' ? 'email' : 'text'}\">`).join('')}\n<button onclick=\"save()\">Save</button> <button onclick=\"reset()\">Clear</button>\n<input id=\"search\" placeholder=\"search ${fields[0].name}\" oninput=\"load()\"></div>\n<table><thead><tr><th>ID</th>${fields.map((f) => `<th>${f.name}</th>`).join('')}<th>Actions</th></tr></thead><tbody id=\"rows\"></tbody></table>\n<script>\nconst F=${JSON.stringify(fields.map((f) => f.name))}; let editId=null; const api='/api/${table}';\nconst msg=t=>document.getElementById('msg').textContent=t||'';\nasync function load(){const s=document.getElementById('search').value;const r=await fetch(api+(s?'?search='+encodeURIComponent(s):''));const d=await r.json();\n if(!r.ok)return msg(d.error);msg('');\n document.getElementById('rows').innerHTML=d.map(x=>'<tr><td>'+x.id+'</td>'+F.map(f=>'<td>'+(x[f]??'')+'</td>').join('')+\n '<td><button onclick=\\\\'edit('+JSON.stringify(x)+')\\\\'>Edit</button> <button onclick=\"del('+x.id+')\">Delete</button></td></tr>').join('');}\nfunction edit(x){editId=x.id;F.forEach(f=>document.getElementById('f_'+f).value=x[f]??'');}\nfunction reset(){editId=null;F.forEach(f=>document.getElementById('f_'+f).value='');}\nasync function save(){const b={};F.forEach(f=>b[f]=document.getElementById('f_'+f).value);\n const r=await fetch(editId?api+'/'+editId:api,{method:editId?'PUT':'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(b)});\n const d=await r.json();if(!r.ok)return msg(d.error);reset();load();}\nasync function del(id){if(!confirm('Delete record '+id+'?'))return;const r=await fetch(api+'/'+id,{method:'DELETE'});if(!r.ok)msg((await r.json()).error);load();}\nload();</script></body></html>`));\n\n  const port = process.env.PORT || 3000;\n  app.listen(port, () => console.log(`${title} running on http://localhost:${port}`));\n  return app;\n};\n", "supabase/config/rest.js": "// Tiny helper for calling the auto-generated PostgREST API with fetch (Node 18+).\nrequire('dotenv').config({ path: require('path').join(__dirname, '..', '.env') });\n\nconst BASE = `${process.env.SUPABASE_URL}/rest/v1`;\nconst headers = (extra = {}, token) => ({\n  apikey: process.env.SUPABASE_ANON_KEY,\n  Authorization: `Bearer ${token || process.env.SUPABASE_ANON_KEY}`,   // user JWT if logged in, otherwise anon key\n  'Content-Type': 'application/json',\n  ...extra,\n});\n\nasync function rest(method, pathAndQuery, { body, prefer, token, extra } = {}) {\n  const res = await fetch(`${BASE}${pathAndQuery}`, {\n    method,\n    headers: headers({ ...(prefer ? { Prefer: prefer } : {}), ...extra }, token),\n    body: body ? JSON.stringify(body) : undefined,\n  });\n  const text = await res.text();\n  let data; try { data = text ? JSON.parse(text) : null; } catch { data = text; }\n  return { status: res.status, ok: res.ok, data, contentRange: res.headers.get('content-range') };\n}\n\nmodule.exports = { rest, BASE, headers };\n", "supabase/config/supabaseClient.js": "// Shared Supabase clients used by every Node experiment.\nrequire('dotenv').config({ path: require('path').join(__dirname, '..', '.env') });\nconst { createClient } = require('@supabase/supabase-js');\nconst ws = require('ws');\n\nconst { SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY } = process.env;\n\nif (!SUPABASE_URL || !SUPABASE_ANON_KEY) {\n  console.error('Missing SUPABASE_URL / SUPABASE_ANON_KEY. Copy .env.example to .env and fill it in.');\n  process.exit(1);\n}\n\nconst options = { realtime: { transport: ws } };\n\n// Public client: respects Row Level Security, acts as anon or as the signed-in user.\nconst supabase = createClient(SUPABASE_URL, SUPABASE_ANON_KEY, options);\n\n// Admin client: bypasses RLS. SERVER SIDE ONLY.\nconst supabaseAdmin = SUPABASE_SERVICE_ROLE_KEY\n  ? createClient(SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, { ...options, auth: { autoRefreshToken: false, persistSession: false } })\n  : null;\n\n// Helper: a fresh client that is signed in as the given user (for RLS tests)\nasync function signedInClient(email, password) {\n  const client = createClient(SUPABASE_URL, SUPABASE_ANON_KEY, { ...options, auth: { persistSession: false } });\n  const { error } = await client.auth.signInWithPassword({ email, password });\n  if (error) throw error;\n  return client;\n}\n\nmodule.exports = { supabase, supabaseAdmin, signedInClient };\n"}};